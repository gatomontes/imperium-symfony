# Courtyard identity compatibility — CY0 contract

Written before changing production consumers, from preparation merge
`b1a8378668ecd9f4e4c1502411a2713111915535`, tree
`eae698493f2f679774c7e91975e05bbaf4cae039`. FC0–FC3 source is integrated;
its independent acceptance remains pending. No installed state is input.

Citadel is the enclosing jurisdiction. Courtyard is its reception/formation
function. Courtthane is the permanent Office-bound LEGATE at
`courtyard.courtthane`; `artifact_class: officer` and `generic-officer` stay intact.
Castellan oversight is deferred and receives no implementation or authority.

## Exact chain

Nine current institutional witnesses from FormationInstitution support separate
Garrison admission, Guildhall suitability, Laboratorium immutable Officer Profile,
four Senate findings, Lord Speaker reconciliation and Conscription qualification.
Every Seat-bearing source targets `courtyard.courtthane`. Exact owner
`APPROVE_FORMATION_PROFILE` binds Profile, examination, Citadel scope and Seat;
qualification binds that approval and every criterion. FormationPersonnel's
`appointCourtthane` verifies `APPOINT_COURTTHANE` over candidate, original Citadel
scope, exact Seat and next Courtthane generation. Currentness rebuilds the binding,
verifies its full identity and signed appointment, and checks all original sources.
No Castellan decision, candidate substitution or relabeled signed bytes can pass.

FormationSessionAuthority and FormationCognition consume that exact incumbent for
interview and drafting; CuriaFormationService and MasterMason consume it for fresh
dossier/constitution work. GovernanceProviderResourceDecisionService retains the
exact source and separate typed planning decision. Clavium's lease and custody
consume the same holder/source plus the independently current Locksmith. The
receiving phase continues to use the mission's exact Seneschal and own approval.

## Compatibility disposition by surface

| Old surface | Disposition and implemented boundary |
| --- | --- |
| `imperium:citadel:intake`, `:formation`, `:prepare` | CLI aliases of canonical `imperium:courtyard:*`, same command object, validation, DI and fixed root; never a signature translator |
| Citadel command class names, `Runtime/Citadel/Formation` namespace | Retained source compatibility names; no source moves or second service graph |
| `imperium:citadel:public-institutions`, native authority and unrelated Citadel cognition | Retained institutional jurisdiction, not reception renamed by proximity |
| `appoint-castellan`, `appointCastellan` | Explicit refused fresh mutation: `CY001_LEGACY_CASTELLAN_FRESH_USE_REFUSED`; no generation or other state effect |
| `currentCastellan` | Retained refusing API; no fresh authority. Historical consumers read exact retained records independently |
| `APPOINT_CASTELLAN` preparation | Unsupported fresh effect (`CRP004`); public inspect and cryptographic original-byte verification remain possible |
| `state.castellan`, signed old Profile targets/approvals/appointments | Retained original evidence, never copied into `state.courtthane`; old claims cannot grant fresh reception, drafting or child publication |
| Old active interview/drafting, pending sealed but unadmitted response | Refuse fresh use/admission; retain all attempts, reservations and unknown maxima. A new Courtthane appointment cannot revive an old holder digest |
| Already admitted response, v1 or v2 | Exact original recovery through `recover-response` before fresh currentness; v2 still requires validated retained custody and exact envelope; no provider retry/refund |
| Existing child receipt after interrupted publication | Existing CF02 `historical`/`reconcile` verifies original frame, fence, signatures and receipt. Parent reconciliation may record the completed fact once; no new Curia, receiving acceptance or execution authority |
| Existing handoff, receiving and Step 1 | Handoff remains readable; new receiving authority remains exact Seneschal authority. Step 1 retains current approval/dossier checks and may refuse an old workflow; recognition is not renewed authority |
| `citadel_id`, trust competence, parent/child IDs, registry | Retain original enclosing jurisdiction and lineage; no new principal or root |
| Journal directory, frame v1, `citadel-formation` lock | Retain exactly one shared storage/lock/generation domain across both CLI spellings |
| Owner decision v1, effects other than new appointment, session/claim v1/v2, lease, custody, reservation, receipt schemas and IDs | Retain exact wire names, strict validators and original accounting. No alternate budget/replay namespace |
| `castellan-response` | Historical records retained byte-for-byte; fresh responses use `courtthane-response` |
| Drafting request v1 | Historical read only; fresh v2 request binds the exact question alongside its original Charter/holder/understanding fields |
| CMF/FC/NA/CRP errors, original public proof schema names, frozen scanners and successor inventory | Retain established compatibility identities. No weakened scanner, frozen snapshot edit or broadened allowlist |
| Historical reports, decisions, acceptances, sealed bootstrap and old packet tools | Preserve; current documentation links to the new report and this contract |

The fresh drafting question is exactly: **“I understand. I am ready to draft a
proposal. Do you approve?”** The v2 request adds `approval_question` to the v1
fields, uses `imperium.citadel-drafting-request/v2`, and is constructed by the
same production service. Its complete digest is signed in the session source.
Understanding produces no proposal. Separate drafting approval, mission approval,
legitimate constitution/appointments, receiving assessment and execution gates remain.

## Frozen evidence and bounded proof

`tests/Fixtures/courtyard-baseline/manifest.json` binds two compressed public-only
fixtures generated using unchanged entry production consumers: real interrupted
child receipt publication, and admitted v2 custody plus an unknown old v1 attempt.
Every exported file retains original bytes and its digest; no ephemeral private
key is serialized. The producer is intentionally entry-only and refuses new code.
Tests restore those exact bytes only to disposable generated roots, verify all
digests, recover original admitted records and reconcile the old child receipt.
No old signature is regenerated by new code to claim compatibility.

Both spellings contend through the same actual Console application and journal.
Process proof covers admission, competing reservations, uncertain custody and
interrupted child publication. Existing FC crash checkpoints and assertions remain
in force. Unknown remote effects keep full exposure; no retry, refund or second
credential issue/dispatch follows from an alias.

Trusted storage and cooperating lock users remain assumptions. This proves
process interruption and offline synthetic authority, not administrator rollback
resistance, power-loss durability, independent timestamps, genuine competence,
remote billing/cancellation or live readiness. Default transports refuse, pure
preparation has no credential/provider dependencies, B1 is unchanged, and
DEFER_ENROLLMENT remains selected. All operational authorization flags stay false.
