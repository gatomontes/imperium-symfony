# O3-B1 — FRESH Augur binding and governed cognition

Status: SELECTED_FOR_LOCAL_OFFLINE_IMPLEMENTATION. Entry is the [accepted O3-B0 correction](../docs/reviews/provider-onboarding-o3-b0-correction-acceptance.md), integrated through PR #792 at `32bb8d76424d6b7447cc423cb7932af35b1f074e`. This contract commissions one bounded implementation run. It does not claim the new bridge exists or authorize an actual installation or provider call.

## Finish line

Complete the selected FRESH route from authentic retained base-selection inputs through exact runtime mapping, legitimate constitutional founding/holder binding, and sequential W1/W2/W3 assessment using the actual O2 coordinator and O3 DeepSeek transport with a mock HTTP client. Return attributable original responses, trusted usage and validated group outcomes. Stop before O4 target-assignment application. Missing genuine production evidence continues to refuse.

Governing originals: [P1–P9 approval](../docs/provider-onboarding-policy-approval.md), [current decision card](../docs/provider-onboarding-current-decision-evidence-card.md), [authority admission](provider-onboarding-authority-admission.md), [F1/F2](provider-onboarding-continuation.md), [ledger/custody](provider-onboarding-ledger-custody.md), [base selection](provider-onboarding-base-selection.md), [response validation](provider-onboarding-response-validation.md), [retries](provider-onboarding-retries.md), [DeepSeek adapter](provider-onboarding-deepseek-adapter.md), and the [prerequisite map](../docs/provider-onboarding-prerequisites.md). Historical unselected/unimplemented labels do not reopen settled decisions. Read actual source before reusing an old consumer.

## Existing seams and required closure

| Existing surface | Current limitation | Required O3-B1 closure |
| --- | --- | --- |
| BaseSelection/BaseSelector and DeepSeek/AccessAdapter::select | Pure selector exists; operational select refuses a missing authentic projection | Derive exact O1 inputs from admitted originals; run the real selector and retain its attributable proposal through the owning ledger |
| CurrentAuthority::verify and CommandLedger::authority | Derived terms currently require `kind: exact` | Resolve the approved `eligible_binding` founding rule from the completed base step and its finite permitted originals, with exact derivation refs; keep unsupported rules refusing |
| CommandLedger::advance and CompletionResolver | Founding effects have no producer/consumer; admission is not completion | Add the bounded FRESH transition and producer-backed completion validation; do not merely add founding to an allowlist or manufacture a completion |
| Operator-root installation/window owners; legacy FoundingAugurModelAssignmentService and AugurResidentActivationService | Distinct stores/occupancy/approval schemas; legacy checksums and ACTIVE labels do not prove D2 competence or globally one-use founding | Implement an explicit original-backed FRESH bridge and current-holder resolver; preserve original provenance and serialize root/window consumption and binding |
| DeepSeek Runtime, AccessAdapter, TokenEvidence, Tariff and ProviderResponse | Runtime binds one exact KeySource, but operational dispatch only admits GET; POST is private component-only | Add a constructor-selected assessment authority path and POST integration that retain the same actual-source binding, private custody ports and coordinator checks |
| AssessmentGroups and ResponseValidation | Group sequencing/frozen-input machinery and pure W1/W2/W3 validation exist | Connect the actual response envelope, frozen inputs and competent commission/holder to semantic validation and group completion |

This table names source inspected at the entry; it is not a claim that missing producers exist. Declare the exact new internal record shapes, producers, consumers, versioning and lock ownership before coding within this same run. No additional planning-only batch is required.

## FRESH authority and one-use founding

Implement the selected constitutional FRESH path. It is distinct from the legacy standing-personnel route. Do not require standing-route ceremonies merely to substitute for genuine constitutional founding provenance; do not relax those ceremonies for standing or existing installations. `APPROVE_STANDING_AUGUR_PROFILE`, `DESIGNATE_STANDING_AUGUR_PROFILE`, `QUALIFY_BIND_AUGUR` and `AUTHORIZE_AUGUR_CUTOVER` remain unsupported unless separately selected. FRESH does not mean a missing directory, empty map or caller-supplied boolean.

Resolve genuine instance/root/window originals, authenticated Operator authority, exact Charter/Profile/Persona or equivalent constitutional founding provenance, selected base tuple, approved runtime mapping, credential/account scope and current heads. Produce a typed holder for the exact `oracle.augur` seat and model/configuration/Profile binding. A signature over arbitrary facts, a checksum-valid development act, a prewritten fixture occupancy or copied legacy approval is insufficient. Preserve old signed records and their interpretation; declare explicit typed adapters where shapes differ.

Make consumption of the exact founding right and publication of its complete binding/receipt share the actual owning transaction. One FormationJournal remains the onboarding authority and exposure aggregate. Inspect the real root/window owner before designing the seam: an imported open-window observation plus a second independent consumption map cannot prevent the native producer from consuming the same right. Use a narrowly justified common-owner seam or a fully specified protocol that preserves one-use authority; refuse an unsupported split-store transition rather than announce atomicity from a label. Never hold the aggregate lock over credential/network I/O or a second store lock.

Demonstrate two contenders for the same root/window, stale heads, crash before/after publication, exact replay and restart. A second policy, sequence, command, alias or new instance label cannot reopen consumed constitutional authority. Sealed, occupied, ambiguous, foreign and existing-installation routes refuse without changing their originals. Do not delete a seal, reset a window, replay installation or create another root to get a positive test.

The resulting holder is not itself paid cognition authority. Each assessment must resolve the exact competent bootstrap commission/effect, current holder/generation, source chain, immutable prepared operation, budget and earliest expiry. Revalidate those originals at every O2 checkpoint. Holder revocation, replacement or expiry before a boundary prevents that boundary; after possible dispatch preserve uncertainty. Historical recognition grants no fresh execution right. Augur cannot approve its own replacement or broaden its own scope.

## Base selection, mapping and assessments

Use the O1 least-cost eligible base algorithm with its original predicates, exact rational arithmetic, freshness and deterministic exclusions/ties. Target medium-capacity selection is a separate later application concern. Derive projections from the original approved requirements, candidate/model/configuration/Profile tuples, account invoke evidence, tariff and tokenizer/framing evidence. No hardcoded winner or newly invented default fills missing evidence. A GET listing proves only its scoped observation, not invoke entitlement.

Resolve only the approved finite `eligible_binding` objects that correspond to the completed base proposal. Bind the mapping to that same selected tuple, exact adapter identity and configuration; do not allow a different permitted model to slip in after selection. Keep all existing exact-rule validation and refusal of O4 `assessed_assignment_set` application intact. Completed-step refs must trace to actual retained producer outputs under the same policy and original lineage.

Use the bound base model for W1 public provider evidence, W2 exact Courtthane Profile fit and W3 exact formation Locksmith Profile fit. A valid empty/no-fit result must remain representable under the O1 contracts; do not turn it into eligibility or assignment. Each advance admits at most one provider operation. W2 consumes only the validated W1 success and W3 only the validated W2 success, using immutable original result refs. Freeze exact semantic workload, public evidence, holder, model/configuration, Profile and predecessor outcomes at the group boundary. Caller/model text cannot provide its own evidence authority, completion refs or safe-retry classification.

Feed exact canonical approved wire through the actual corrected O3 Runtime custody path, not a new direct HTTP helper or reflection in the positive integration test. Preserve same-KeySource object binding and current generation against the original credential binding. No public issue/consume/dispatch entry, arbitrary transport decorator, endpoint override, secret source or callback from ingress. Keep operation/envelope/metadata schemas and provenance unchanged unless a concrete indispensable typed extension is specified with all consumers and compatibility tests.

Before reservation and again at dispatch, authenticate or derive the tokenizer/framing bound and tariff/account/model/effective interval from competent original evidence. TokenEvidence and Tariff are structural projections, not certificates. Synthetic verifiers may exercise positive offline cases only and must be explicitly selected by test infrastructure; production defaults cannot treat synthetic estimates or signed unsupported assertions as provider guarantees. Missing real token, account, tariff/rounding or fee evidence refuses. Preserve the exact 16,384 input / 4,096 generated token bounds, 32,768 minimum context and 4,096 reserve.

Combine ProviderResponse transport/usage validation with the real O1 W1/W2/W3 semantic validators before completing a group. Preserve original bytes, operation attribution and immutable envelopes. Contradictory model/usage/evidence/frozen context refuses. A terminal semantic failure may settle only with the complete trustworthy usage allowed by O2; missing usage remains maximum exposure. No plain `finish_reason: stop` or returned JSON object implies assessment success.

## Bounds, recovery and compatibility

Retain approved DeepSeek aliases/configuration, HTTPS destinations, non-streaming exact wire, no tools/redirects/implicit retries, 1 MiB response bound and earliest deadline. Per cognition: at most 100,000 micro-USD and 60,000 ms. Whole shared domain: at most 13 requests, 1,200,000 micro-USD and 730,000 ms, with exact policy token totals and concurrency one. Existing FC obligations remain included. No independent onboarding/holder alias resets a budget or source fence.

The actual safe-retry allowlist remains empty. Four declared attempt slots per group do not authorize retries after HTTP errors, timeout, cancellation or lost acknowledgement. Preserve all O2 one-use claim/capability/checkpoint, same-command replay, maximum exposure and evidence-only reconciliation semantics. Recovery never recreates a delivery right. No promise of remote cancellation, zero billing after timeout or power-loss durability.

Only narrow, necessary seams in onboarding, corrected DeepSeek custody and actual FRESH source owners are allowed. Preserve original tests/fixtures, original approved/signed bytes, frozen source/inventory snapshots, legacy consumers, service configuration, dependencies and locks. Do not rewrite canonical root/standing schemas in place. Any versioned state extension needs strict bounded decoding, explicit migration, original-byte preservation and refusal of unknown versions; no implicit migration on read or reset. Add inventory entries only for real newly implemented consumption boundaries; never regenerate a frozen baseline to pass.

## Proof and exit

Use the [campaign proof matrix](../docs/next-campaign-provider-onboarding-o3-augur-cognition.md). The positive integration fixture must use actual production signing/admission, base derivation, FRESH producers and holder/commission resolvers in isolated temporary roots, with clearly synthetic keys and provider evidence and an exact mock transport. Do not insert successful founding, occupancy, commission or completion records directly into the aggregate. Privileged test hooks may pause actual boundaries for negative/fault tests; they cannot supply the positive authority chain.

No real provider requests, real credentials, installed-state access, live enrollment/founding/appointment/activation, standing or existing-installation cutover, OAuth, O4 target-assignment application or O5 CLI work. Constructor-selected code stays dormant; all five public operational flags remain false. No subagents. Implement, validate, update the handoff once, and stop for source review and fresh full CI before integration. If a required seam cannot be closed, return the precise bounded blocker and completed code/evidence; do not fabricate authority or substitute another preparation-only pass.
