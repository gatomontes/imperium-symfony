# Delegate mission bounded cognition turn

Step 66 consumes exactly one scoped provider activation, credential lease, and turn authority. Before invocation, Citadel revalidates the sealed commission, model binding, access attestation, target, runtime mapping, expiry, and single-turn constraints.

The Oracle catalogue reference remains governance identity. Provider, Symfony platform service, and runtime model are separately frozen in `runtime_binding` and carried unchanged through selection, binding, attestation, authorization, activation, and result.

The Symfony AI boundary routes only an explicitly supported `platform_service`; it passes the sealed `runtime_model` to `PlatformInterface::invoke()` and has no implicit provider or model fallback. The provider returns only the exact result payload. Runtime lineage, authority consumption, and credential-use facts are established by Imperium, not by model output.

The checkpoint is `DELEGATE_MISSION_BOUNDED_COGNITION_TURN_COMPLETE_PENDING_CURIA_DISPOSITION`. Provider and cognition occurred once; the lease and turn authority are consumed. Tool, perimeter, external-action, execution, and continuing-turn authorities remain false.
