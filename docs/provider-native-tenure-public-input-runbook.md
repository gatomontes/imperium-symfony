# PPC3 future public-input runbook

**PARTIAL_NATIVE_TENURE_SYNCHRONIZATION**: direct generic tenure writes remain outside the synchronized subset because of the retained primitive source pin. Do not treat this package as a complete supported deployment set.

This runbook grants no runtime publication, integration, enrollment, appointment, activation or interview permission. The current campaign stops at local commits, public source review and fresh complete hosted CI. Countdown remains **5–7**; only receiving acceptance of R2 with no new scope may propose/apply **4–6**.

1. Verify the separate SHA-256 against `imperium-ppc3-public-review.zip`. Check exact archive membership and every payload's size/hash against `manifest.json`. Independently verify source/evidence manifests, source Git blobs/modes, all commit/tree identities, three exact diffs and the bounded bundle prerequisite. Never extract to an installed application root.
2. Reconstruct the bounded bundle only in a review repository that has the exact recorded preparation prerequisite. Inspect the code delta and all diagnostic evidence. The tested-to-final delta must contain documentation only. Compare unchanged pins, gate scripts, dependencies, service wiring, historical onboarding files and canonical countdown records.
3. Run the complete unchanged eight-partition workflow and all 11 guards on the receiving candidate. Check all native exits and exact-case/source coverage; do not replace the full run with a targeted test or PPC2's old CI. Preserve explicit platform errors/skips/warnings. Windows symlink privilege failures are not passes.
4. For additional offline reproduction, use a disposable directory and the test fixtures/worker in the source archive. Generate new ephemeral fixture keys. The retained `public-originals-final` directory is synthetic public evidence only; preserve its exact parent/actor identities when reconstructing a test. Never use installed incumbent records to establish a new parent or FRESH root. Collection scripts are task-specific evidence generators: use new empty output paths, never old `.held`/`.release` barrier files or retained result directories.
5. The supported positive input is an actual artifact-backed root package with known complete-package custody, complete scoped institutional originals and an independently signed exact appointment. Only trusted internal code receives `FormationOwnerFrame` from the held journal callback. Public requests cannot provide a frame, held-lock flag or synchronization certificate. Consume current actor/candidate/holder evidence before leaving that owner scope.
6. If a native registry, bootstrap state, unsupported successor, partial package, alternate cohort, ambiguous source or missing original is present, retain refusal. Do not remove directories, regenerate missing completion markers, copy occupancy, change a source pin or reissue an approval to obtain a positive result. Old unknown packages are not migrated by this batch.
7. Report the resulting source/validation disposition to the receiving owner. Receiving review must separately accept R2 before a countdown decrement. Current designation events, competent native-model-to-O4 binding correspondence, AssignmentEvidence, combined FRESH establishment and account/access/base/cognition evidence still require their own outcomes.

All five operational flags remain false: `deployment_approved`, `enrollment_authorized`, `live_ready`, `activation`, `execution_authority`. `DEFER_ENROLLMENT` and the empty actual retry allowlist remain. The milestone is the first separately authorized bounded live Courtyard interview, not full mission execution.
