# Recruiter/Garrison authority interfaces — A0–A3

Disposition: **AUTHORITY_INTERFACES_IMPLEMENTED_WITH_EXPLICIT_ISSUER_OR_CURRENTNESS_BLOCKER**. A0–A3 are complete through the campaign's permitted typed-preparation and refusing-verification path, with final offline gates passed. Independent implementation review is pending.

## Result and exact boundary

Implemented `imperium:citadel:authority` with six actual command/DI modes: Recruiter private-source projection, public inspection, locked comparison; Garrison unsigned request, supplied-original inspection and refusing revision verification. The dormant admission resolver never returns revised authority. The new path is exercised with synthetic private backing state and real admission/inventory consumers in disposable roots. It does not implement a positive authority revision, register a competing occupant or change a real installation. This is the campaign's explicit fallback when authentic issuer/currentness prerequisites are absent, not a claimed successful institutional act.

The source-backed A0 decision distinguishes the retired MasterMason T03/T04 producer from a current authority resolver. StateStore generation and lock provide a bounded observation among cooperating writers; they are neither a signature nor a revocation/supersession register. V0Activation writes a different operator-root format and refuses incompatible legacy state. No authentic native Recruiter provenance issuer/currentness source was established by the traced contracts.

Likewise, Constable binding defaults and vacancy construction cannot amend historical occupancy. Operator-root upgrade planning and formation/ProtectedMission/planning trust do not establish competence for the two requested native Garrison powers. The exact missing prerequisite is an authentic effect-scoped native revision issuer with its external enrolled trust path, a current occupancy/prior-revision and issuer-revocation source, and one transaction covering validation, consumption and immutable effect. No fake test issuer is wired into production to fill that gap.

## Implementation and limitations

Recruiter export reads only bounded `bootstrap-state.json` under the existing bootstrap lock. It may create `var/imperium` and `bootstrap.lock`. It never writes the backing state or temporary state files. The allowlisted projection has its own digest, source instance/manifest, observed state/T04 generations, predecessor/successor, retired predecessor, consumed commission and qualification identities. Whole private state, arbitrary errors and private extra fields are not serialized. `source_original_digest` stays null, and authenticated provenance/currentness/readiness remain false. A fresh locked equality check has the same authority limitations as export; it never converts a historical receipt into indefinite incumbency.

Garrison preparation binds supplied original occupancy digest, instance, actor/generation, an explicit prior-revision reference or unknown null, exactly two narrow powers/scopes, effective/expiry terms and replay nonce. Other powers remain true/false/null as originally observed, with original scope bound by the occupancy digest. Incoming keys and even valid signatures cannot bootstrap their own issuer competence. No signature assembly or mutation command is invented without the missing issuer contract. Requests remain untrusted, and no one-time authority is consumed.

Existing admission/custody and inventory readers retain their native representation. Proof shows actual canonical admission refuses the older fixture occupancy before any custody/disposition effect; inventory retains its original independent power. Putting a request in the occupancy slot is rejected. No new effective revision format reaches other readers. A later positive implementation must migrate all direct readers consistently and prove publication/currentness/revocation races under one boundary; those positive transition claims do not apply to this no-effect implementation.

All production formation adapters, original PHP tests, dependency locks and the public-only Python collector are unchanged. CF01 terminal refusal, CF02 authentic completed-publication recovery and IR01 understanding closure remain separate preserved contracts. No real private state, journal, key, credential or provider was used; no real lookup, enrollment, institutional act, signing, deployment, source publication or mission execution occurred. Raw earlier public evidence stays in its preserved external packets and is not recollected here.

## Verification attribution

Final tested checkpoint: `49021008edd947a3e32171abc328bdc1cf97460a`, tree `68d40a0fa3da307cd35d0c57a8cbc28a6a41e97a`. Production PHP and tests are unchanged from executable commit `2e51af29339bdf429d50e39b2a510f5bba4ab197`; the checkpoint adds the required runtime-inventory classification and its explanation. New focused development tests passed 21 cases / 149 assertions before this commit. Final synthetic command/DI proof, 16 unchanged Python compatibility tests and Symfony container lint passed on this committed checkpoint. Final focused PHP passed **37 tests / 4109 assertions**; full PHP passed **2810 tests / 53495 assertions**, both exit 0 with no errors/failures/skips. Four full-suite warnings exactly match the accepted native-lineage run after only worktree-root/newline normalization. Full wall time: 827.147 seconds. No executable changes occurred during final gates. The final closeout commit and tree are in final-identity.json, with Markdown-only post-test.diff.

The new adverse tests cover missing/failed/duplicate/contradictory/retired/superseded records, wrong source/actor/generation/digests, borrowed provenance, stale projection, self-sealed forgery, unknown issuer/wrong competence, changed predecessor, overbroad terms, stale revision, expiry, repeated preparation and no admission/revision effect. Sibling native PHP processes prove exporter exclusion behind StateStore's writer lock, committed-generation observation and recovery after writer termination. Secret sentinels test normal/error output and source/temp preservation. The source/test map records qualifications and exact covered boundaries.

Dependencies were copied offline from the accepted native-lineage vendor; Composer only regenerated local autoload without scripts/plugins/network, and no package/dependency change occurred. Harnesses clear the environment to documented OS fields and test settings, disable parent-PHP outbound functions, and retain exact arguments, UTC times, native exits and source identities. New sibling-process tests apply the same restrictions themselves. These controls are not an OS network sandbox and do not certify every existing subprocess or power-loss behavior. PHPUnit stdout/JUnit and the full historical-warning comparison are retained in test-evidence and validation-summary.json. Prior 2,789 / 53,334 results are historical evidence, not a target or a new rerun result.

## Review and later owner boundary

The packet contains complete tested/final source ZIPs and per-file Git/blob/SHA-256 manifests, bounded Git history with verified prerequisites, exact post-test diff, source/status identities, proof outputs, source/consumer and changed-test maps, the exact implemented runbook and deliberately incomplete unsigned owner templates. Synthetic prepared requests are explicitly labelled separately from those owner templates. No template assigns a real signer, expiry, nonce or institutional judgment by default.

After independent review, installation and private export need separate owner authorization. Any positive authority change requires the issuer/currentness/transaction prerequisites above. Other Seat witnesses, formation suitability/delegation, custody/trust, candidates/appointments and B1 remain unresolved. B1 needs real payload/destination/model/limits/authority and supportable timeout/cost semantics; default transport refuses, and unknown outcomes retain exposure without retry/refund.

Citadel receives; Castellan interviews; understanding closes interview authority; separate explicit drafting approval permits a proposal; separate mission approval precedes legitimate child-Curia constitution/handoff; receiving assessment grants no execution authority. Readiness, activation and execution remain false. Local commits stop for independent review; no implementation push/merge follows automatically.

The initial broad PHP focus passed 226 tests / 2,445 assertions. The first full run at 2e51af29 returned exit 1: 2,810 tests / 53,478 assertions, two exact-inventory failures and four historical warnings. Both failures detected the new GarrisonAuthorityRequest path because its output contains authority_consumed: false. One additive inventory row now identifies that path as preparation/permanent refusal with no store or effect; discovery predicates, frozen snapshots and all assertions remain unchanged. The initial checkpoint/source manifests and all initial gate outputs are preserved under initial-checkpoint. Final focused verification covers the new interfaces plus frozen tripwire and Batch12 inventory checks; the full rerun covers the complete suite.

Generated config/reference.php changes were only the known Symfony PHPDoc defaults/when@sortie entries. Each observed diff is retained and the generated file was restored before the next clean gate and final closeout. These generated documentation bytes are not a new authority/configuration change.
