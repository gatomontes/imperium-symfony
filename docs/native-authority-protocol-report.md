# Native authority issuance/currentness — P0–P3

Disposition: **NATIVE_AUTHORITY_PROTOCOL_COMPLETE_LOCAL_PENDING_INDEPENDENT_REVIEW**. P0–P3 implements and proves a positive offline protocol with genuine cryptographic/effect checks under separately enrolled synthetic trust. Final offline gates passed. This does not approve real policy, enroll real trust or commission an installation.

## Implemented result

P0 defines and implements a separate deployment-owned native trust domain and six exact effects. Public enrollment requires independent fingerprint confirmation and explicit approval of its legacy-workflow fence. It is separate from ordinary incoming commands, Formation trust and ProtectedMission competence. The owner policy is a concrete unapproved proposal with real identity/key/times null. A signed envelope cannot introduce its own trust root.

P1 implements exact signed initial adoption, current roster resolution, prospective Recruiter succession, retirement and decision/issuer revocation. Registry/roster/authority revisions do not masquerade as occupancy generations. Initial Recruiter adoption binds the exact structural projection and retains the fact that the owner attests evidence, not historical producer execution. Currentness is a locked observation. Garrison initial adoption binds the sole exact native original; no highest-generation selection, implicit migration or second ACTIVE file exists.

P2 implements unsigned payload preparation, detached public-signature assembly, verification and an immutable two-scope Garrison revision. Currentness, revocation, expected heads/predecessors, request replay and one-time decision consumption share the journal commit. Exact completed-act replay retains the original cryptographic evidence after later expiry/revocation. Actual canonical admission separately processes a complete synthetic delivery and co-publishes custody/disposition in one native frame. Inventory retains the original independent inventory power and reads native custody under the same lock. A capacity revision is not itself candidate admission or formation eligibility.

P3 supplies positive command/compiled-DI and actual consumer proof, complete valid admission controls for each missing power and substituted occupancy, signed adverse cases, independent public cryptographic verification, sibling-process races and interruption/recovery. The accepted A0 report remains history. Its old incomplete-delivery test is not relabelled as isolated predicate proof; the new controls establish actual valid admission before varying each predicate in separate roots.

## Integration and supported boundary

The two supported Garrison consumers route to the native aggregate after enrollment. The mapped 38 legacy source files acquire the native exclusion lock; unsupported T04-dependent Conscription and raw Garrison occupancy paths explicitly refuse after enrollment. StateStore follows native-lock then bootstrap-lock ordering. Existing method bodies remain behind the wrappers. The separately authorized private projection can still observe historical backing bytes and stays structural. Enrollment fences old work; it is deliberately not a transparent migration of every legacy lifecycle.

Native custody/disposition remain together in the registry. There is no split materialization into legacy custody files. Native inventory is returned as a new versioned response; legacy Guildhall transport of that format and Garrison succession are not implemented. Existing independent powers are not broadened, but unsupported old workflows are unavailable under the enrolled policy. Future deployment must explicitly approve that consequence, quiesce unsupported processes and restrict administrative writers. The registry does not control old binaries, direct file edits, external writers, path aliases or administrator rollback merely by declaring itself authoritative.

One fsynced temporary frame and rename publish the state, consumed decision and effect. Broken chains/gaps/competing filenames refuse. Pending bytes retain an unknown-outcome fence; no automatic repair, refund, forced publication or key rotation is offered. Capacity is bounded to 1 MiB/frame and 1,024 frames. The proofs establish cooperating-process and local-filesystem behavior, not a network sandbox, trusted timestamp, directory-fsync guarantee or power-loss test. The exact boundaries and future commands are in the contract, source map and runbook.

FormationInstitution, FormationPersonnel, FormationSignatures and FormationPublicationEvidence are unchanged. Native records are not admitted as formation witnesses. CF01 terminal refusal, CF02 authenticated completed-publication recognition after expiry without new authority/effect and IR01 understanding closure remain separate retained contracts. The public Python collector is unchanged. No installed private state/journal, actual key, provider, enrollment, institutional act, source installation or mission execution was used. All backing state and signing keys in this run are generated synthetic fixtures; keys remain in memory.

## Verification attribution

Entry: `031091dda39798429d2af79bca106ad2bfe983e8`, tree `986f4c12f7c7aa757ef17af58051730cf460ecb7`. Prior A0 acceptance and PR #773 integration/CI are supplied campaign facts; this run fetched and verified the pinned main/source tree and preserved earlier worktrees. It does not claim a new GitHub CI run. Final executable checkpoint: `fa4fa7dbebee26890eab3ebf827fe8a157583cb2`, tree `54c8327b98d5ddebecfdb1496f51415a217e2c08`.

Final focused PHP: **75 tests / 4304 assertions**. Final full PHP: **2848 tests / 53852 assertions**, native exit 0, no errors/failures/skips; four historical warnings exactly match after only root/newline normalization. Full wall time: 835.013 seconds. The committed synthetic PHP proof, unchanged 16 Python compatibility tests and container lint passed. The independent verifier checks the supplied synthetic public frames with the existing Python cryptography library; it does not certify real authority or prove actual historical producer execution. Exact commands, UTC times, native exits, stdout/stderr, JUnit and hashes are retained in test-evidence and validation-summary.json. Final identity and exact post-test diff are supplied separately. Only this Markdown report and four current-entry Markdown notices change after the tested commit; no executable changes occurred during final gates.

Prior supplied full PHP result is 2,810 tests / 53,495 assertions with four historical worktree warnings. It is retained for comparison, not forced as a target. Development runs are explicitly dirty-source observations with their baseline HEAD and status, not final committed evidence. The helper's first development invocation had a missing closing bracket, fixed before the executable checkpoint. The first final independent-verifier attempt could not import cryptography under the cleared environment; its exit 1/stderr is retained. The rerun supplies only the explicit existing site-packages path through PYTHONPATH and passed, with no package installation or code change. PHP protocol tests did not fail in development; the complete final suite remains separately attributed.

Dependencies were copied from the accepted authority worktree; Composer only regenerated autoload with scripts/plugins/network disabled. Final harnesses clear the environment to OS fields/test settings, restrict parent-PHP outbound functions and preserve command arguments. Sibling-process proof applies the same restrictions explicitly. This is not an OS network sandbox or a statement that every old subprocess inherits PHP flags. The Python verifier's additional dependency path is disclosed in its exact harness. Generated Symfony reference PHPDoc changes are retained and restored; they are not new runtime configuration.

## Review and later owner action

The packet contains complete tested/final source archives with every Git blob/mode/byte/hash, bounded history and verified prerequisites, post-test diff, source/status preservation records, original owner brief, contract/design, exact maps/runbook/unsigned policy, proof frames/signatures, test outputs and whole-folder manifest. Real prior public evidence remains in its preserved external packets; no recollection occurred.

Stop at local commits for independent review. Real policy approval, deployment, trust enrollment, independent fingerprint confirmation, factual attestations, native decisions and actual candidate judgments need separate authorization. Other formation Seats, formation-specific suitability/delegation, custody/trust, appointments and B1 remain unresolved. Default transport refuses; unknown provider outcomes retain exposure without retry/refund.

Citadel receives; Castellan interviews; understanding closes interview authority; explicit drafting approval is separate; mission approval precedes legitimate child-Curia constitution/handoff; receiving assessment grants no execution authority. Readiness, activation and execution remain false. No publication or merge is part of this campaign.
