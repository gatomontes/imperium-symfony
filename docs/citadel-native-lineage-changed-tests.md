# Native public tooling: changed tests

No existing assertion or production authority consumer changed. New standard-library tests live in `tools/tests/test_citadel_native_lineage.py`; all use disposable synthetic records and roots, never installed authority fixtures.

- Complete bounded structural graph with missing powers remains refusing; recording public reader confirms only allowlisted records opened, socket and process doubles confirm no provider/application/credential process activity.
- Private paths/traversal refuse before opening; unknown schemas never export raw content; changed and corrupt originals are retained without reference expansion.
- Missing commission, wrong actor/Seat/instance/generation and qualification binding remain explicit failures.
- Extra/superseded assertions cannot establish currentness or grant missing powers; inactive records remain inactive.
- Ambiguous summons names do not cause content scans; unsupplied acceptance IDs leave the entire acceptance directory unread.
- Mismatched acceptance refuses; an intact planning-only acceptance cannot provide formation competence.
- Projection/original mismatch, positive assessment tampering, duplicate paths and unreferenced records refuse offline verification.
- File/directory/JSON limits, duplicate keys, hard links/reparse paths and output into a checkout refuse; existing output is preserved.

Exact new-tool command:

```powershell
python -B -m unittest discover -s tools/tests -p test_citadel_native_lineage.py -v
```

PHP focused selection remains `Citadel|SourceReview|Constable|Guildhall|RecordReferenceValidator` over `tests`, followed by the complete `php vendor/bin/phpunit tests` suite on the committed executable candidate. The Python suite is a separate required focused check, not silently counted as PHPUnit tests. Exact results, environment, generated-reference diffs and warning comparison belong to the review report.

CF01/CF02/IR01 and SourceReview tests remain intact. Because no new production witness variant is admitted, historical alternate-witness support is explicitly blocked; existing historical proofs are validated by the retained CF02 tests, and altered public originals are rejected by the new verifier. No distributed/power-loss or adversarial administrator claim is made.
