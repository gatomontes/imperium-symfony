# Canonical native-effect reconciliation authority provenance remediation — Batch 4 proof

Status: `BATCH_4_COMPLETE_ADVERSARIAL_APPLICATION_PROCESS_PROOF`

| Class | Attack or cut | Proven disposition |
|---|---|---|
| `FORGE` | Copy labels and compute a fresh digest | Issuance-reference mismatch; no custody |
| `ROOT` | Revoke trust anchor or expire/change native principal | Existing signed Root/native loaders refuse resolution |
| `LINEAGE` | Substitute transition, admission, callback or response | Exact immutable references refuse |
| `CUSTODY` | Clone, serialize, reconstruct or copy capability fields | Exact object/process-incarnation registry refuses |
| `CONSUMPTION` | Reuse authority for another capability/claim or claim for another receipt | One winning capability ID is sealed in the claim; exact claim/receipt store conflicts on substitution |
| `CUT-ISSUE` | Exit after authority publication before issuance evidence | Orphan grants no custody; exact retry completes issuance |
| `CUT-DERIVE` | Exit after in-memory capability consumption before claim publication | No durable consumption; fresh resolution can retry |
| `CUT-RECEIPT` | Exit after durable claim consumption before receipt | Fresh recovery converges on the same consumer and receipt without callback |
| `PROCESS` | Two fresh processes resolve, then compete to derive | Resolution alone grants no durable use; precisely one capability ID wins publication |
| `CONTAINER` | Resolve the real Symfony corridor | Issuer, resolver, shared-registry typed admission and no-provider recovery construct |
| `REPLAY` | Repeat exact forward completion and reconstruct | One receipt; replay is convergence, reconstruction is read-only |
| `NO_PROVIDER` | Inspect issuance, resolution, derivation, recovery and reconstruction dependencies | No credential resolver, provider transport, command, callback input or network edge |

The process proof uses synthetic keys, disposable storage and the established
local provider double only to create the sealed response. It invokes no live
provider, credential, mission, email or external I/O.

The authority-to-claim publication embeds the winning capability identifier and
consumption evidence in one immutable claim. The claim-to-receipt consumption may
exist before a receipt at an interruption cut, but its exact source and consumer
make the operation resumable and make competing consumers impossible.

Reconstruction independently joins receipt, claim consumption, claim, authority
consumption, authority, issuance, native authority, native principal and signed
Operator Root act. Its implementation contains no write, consume, bind, credential
or provider operation.

`BATCH_5_SEPARATELY_SEQUENCED_TERMINAL_AUDIT_NEXT`
`BATCH_7_LIVE_TRIAL_AUTHORIZATION_SUSPENDED`

