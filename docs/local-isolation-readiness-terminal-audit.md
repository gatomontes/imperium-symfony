# Local isolation measurement and readiness — terminal local audit

**LOCAL_ISOLATION_MEASUREMENT_READINESS_PACKAGE_READY**

This is local implementation-agent preparation and self-audit, not independent
acceptance or deployed isolation. C0–C2 are complete. Actual owner setup, actual
Runtime/Caller evidence and authentic exact authorization remain prerequisites
for the already selected parent Batches 3–5. No real mission has executed.

Tested executable commit: `6bb2628d440b45b881d2d330c61fe3b5b4aab521`.
Tested tree: `9636c3b560a4aa3d62da9c8bfcbc46684b601db9`.
Later commits contain only public audit/status documentation. The derivative
review manifest records the exact compilation/final documentation commit/tree.
Full baseline/integration/C0/C1/C2 lineage and changed-source hashes are in
`docs/local-isolation-readiness-audit.json`.

| Verification on committed final code | Result |
|---|---|
| `php vendor/bin/phpunit tests` | 2679 tests / 52722 assertions / 0 skips; 07:22.421 |
| `php vendor/bin/phpunit --filter 'LocalIsolation\|ProtectedMissionAuthority\|MissionAmendment' tests` | 30 / 430 / 0 skips; 00:52.570 |
| Same focused command using packaged PHP with relocated minimal ini | 30 / 430 / 0 skips; 00:39.797 |
| Packaged PowerShell readiness fixtures | 722-row complete synthetic set; 42 adverse/recovery cases |
| Native Windows handle/startup fixtures | 5 handle rows; 3 exact Windows PowerShell 5.1/PHP startup checks; bytes unchanged |
| Preserved PowerShell mission route | LOCAL_ISOLATION_DISPOSABLE_REHEARSAL_PASSED |
| Preserved PowerShell amendment route | MISSION_AMENDMENT_POWERSHELL_PASSED |
| Fresh package byte verification | 6477 manifest files; 6475 runtime/dependency/target files |
| Owner installer | WhatIf only; wrong digest refused; no installation applied |
| Actual separate-account installation/measurement | NOT RUN — owner action, not a passing/skipped PHPUnit case |

The focused command uses literal `|` characters inside its quoted filter; table
escaping is Markdown presentation only. All exact commands and transcript hashes
are also machine-readable in the audit JSON. Current test identity is SID
S-1-5-21-4230689943-3750815333-1810161870-1001; its recorded token reports both
administrator flags false. It is not the designated fresh Runtime/Caller pair.
PowerShell 7.6.5 and PHP 8.4.14 were used; native checker subprocesses used Windows
PowerShell 5.1. Same-user/native fixtures are not deployed isolation.

The first two C2 full-suite attempts were deliberately interrupted for additional
reference-byte and identity/type checks. They are retained, not counted as passed.
Their focused results remain attributed to their own commits. Symfony generated
only comment changes in config/reference.php. Each generated file/diff was saved
under readiness-c2 before restoring the exact pre-test bytes; no user edits or
attempt markers were discarded. No executable or test changed after the final
committed audit candidate. All preexisting test methods/expectations remain;
two new PHP methods and four support files were added. AM01/AM02 and receipt proofs
passed. Historical assertion totals are not an arithmetic baseline: earlier audit
records explicitly qualify unchanged conditional race assertion variability.

## Corrected boundaries

LI01: current inventory enumerates code/PHP/shell/target, state/canaries/metadata,
exchange, both plan roots and every finite child, plus relevant parent replacement
rights. Policy derives read/list, write/append, delete/delete-child and ACL/owner
handle expectations. Owner/group/SDDL/attributes and exact token SIDs/groups are
retained. Owner reference files resist direct and parent replacement. Runtime
can create required outputs and legitimately owns its journal; Caller is excluded.

LI02: detached manifests bind exact final plan/evidence bytes without self-hashing
plans. Validation derives coverage independently from installed inventory/policy,
checks the reviewed package and copied references, phase/session/identity/config/
trust bindings and exact native startup outputs. Missing/extra/duplicate/reduced/
altered/stale/mismatched evidence refuses. Self-consistent rehashed reductions and
policy/result substitutions also refuse. Generic errors cannot represent the
expected identity refusal. Current live Runtime token/groups and enrolled public
trust are checked before mutation.

Prepare/Accept/Step validate before attempt markers or authority calls. Status
requires no readiness and writes neither exchange snapshots nor journal frames.
Real disposable CLI tests preserve journal and exchange hashes through refusal
and Status. Existing uncertain-attempt markers remain authoritative.

## Fresh package and retained history

Package: `E:/htdocs/imperium/var/local-isolation-evidence/package-readiness-6bb2628d`.

Manifest SHA-256:
`C0442CFCC4FD68AFF12161FC8C942AFC3E4BD4F01A7336B80A603E341CC9076D`.

All 6477 package files match the manifest. 5771 code/vendor files match current
source bytes and 4702 vendor files match the original package. The existing builder
also includes one non-executable PHPUnit result-cache file. Its captured package
bytes differ from the subsequently updated working cache and historical cache;
all three hashes are recorded. That cache is never used as test evidence. The
package's immutable manifest still binds its exact captured bytes.

All 23 original target object files remain byte-identical. Target commit
`a1fc4f27634319f2a22df2e6a1b370f70cdb98bf`, tree
`21780452f5815d4342f8f5c96923b2b276d9930a`, 15 paths and 448476 inflated bytes
remain distinct from the installed build. Budgets remain 15 files / 15 findings /
4000000 bytes / 60 seconds. The fresh package's random draft identity has expiry 0;
it is inert and grants no authority. No selected-target mission or semantic report
is claimed by preparation.

Original package-47bcc44a and manifest
`8DAB48F98D147A6ABA9E89260D25082BA5B91AD2C72638F1F10FFB9A449CF6AE`
remain unchanged historical evidence; do not install that candidate. The original
assembler, packet, all 19452 inventoried original evidence/package files and prior
branch refs were verified. One historical source ledger has equivalent LF working
text versus CRLF bytes embedded in the unchanged original packet; both hash
identities are recorded without relabeling one as the other. The original packet
retains all 55 embedded sources. Its unchanged canonical-service dependencies are
also hash-mapped to the fresh package in the audit JSON. The new packet focuses on
current instructions, changed boundaries and runtime/test sources; the preserved
original packet supplies those unchanged canonical implementation appendices.

The original implementation branch stays at 574bfc69. Main stays at planning merge
837f2731. No publication, implementation-to-main merge, branch deletion, journal
reset/migration, real account creation, installation, real secret custody, real
trust enrollment, real mission, provider or target mutation was performed.

## Owner continuation and limits

Use `docs/local-isolation-readiness-owner-runbook.md`. It supplies exact setup,
capture/import, phase verification, public-trust, current-readiness, signing,
execution and recovery commands. The fresh Markdown packet and derivative JSON
paths are in the audit JSON's `review` object.

Current evidence expires after 15 minutes and new exchange outputs require fresh
current coverage before the next mutation. Pre/post observations remain historical
installation/session-bound evidence, with exact bytes, ordering and retained
inventory checks; they never replace current evidence. No pre-enrollment replay,
replacement enrollment or journal reset is supplied as recovery.

The validator establishes consistency of trusted observations. It cannot
authenticate arbitrary producer logs, establish human transport/custody, exclude
a compromised trusted Runtime/administrator/signing environment or audit the
whole OS. Windows-only operation, local loose SHA-1, bounded journal capacity,
unavailable migration and unmeasured hardware power loss remain. Receipt bindings
do not certify analytical truth or independent acceptance. Supply only the required
public owner evidence and authentic response through the existing private ceremony;
then resume parent Batches 3–5 under their existing scope without a new campaign.
