# O3-B1 internal ownership and shapes

The v3 migration is explicit and requires a quiescent v2 predecessor (all claims settled, all steps complete, no fences). The complete prior subtree is bounded to 4 MiB and retained in the migration record; reads never migrate. Native activation can already hold its existing bootstrap-state lock before installation; the common-owner callback never acquires that lock, a credential lock, or another journal lock. Native files keep their bytes. A native partial prevents FRESH; a published FRESH holder prevents native installation and sealing. Before aggregate rename the old generation remains authoritative; after rename the holder, consumption and completion become visible together. No power-loss durability claim is added.

The assessment extension uses a closed `augur-assessment-commission` source with schema `imperium.augur-assessment-commission/v1` and fields `group_id`, `founding_step_id`, `constitution_ref`, `profile_ref`, `account_scope`, `account_ref`, `token_ref`, `tariff_ref`, `evidence_refs`, `not_before`, `expires_at`. Its governing proposed terms and exact effect slot supply competence. The founding selector must be `found-augur`; holder resolution checks that actual produced completion and its owning policy, constitution and publication generation. W2/W3 Profiles must equal their exact policy targets. All originals are reloaded, and a fixed verifier authenticates account/token/tariff projections and substantive response claims; defaults refuse. The request payload includes exact public originals, holder, workload and actual predecessor outcomes plus their retained envelopes. Its digest is the semantic input digest supplied to O1; O2 separately retains its existing hash of resolved refs and complete wire. These distinct digests avoid a self-referential wire hash and are both reconstructed at each checkpoint.

## Continuation design declared before source-owner changes

The legacy Imperator founding-model assignment and Conscription Augur occupancy writers also compete with FRESH. They must enter the same owner before validation/publication, and their existing assignment directory is part of the FRESH exclusion check. Their original schemas and serialization remain unchanged. No standing approval is translated into FRESH authority. Their per-file write locks become unnecessary under the shared aggregate fence; no second lock is acquired inside it.

Continuation entry is 8ebee96cbdcd43e5e89e8b1d222d83cd285b2e2d / 97e4c675ac75fadc0f59bb6c7c8e67c37a65420e. Existing untracked handoff copies are preserved. The attached review is continuation input, not independent PHP/CI proof.

OperatorRootOwnership gives both native install and seal the same citadel-formation inspection lock already used by onboarding. It performs no nested journal acquisition. Native files retain their old bytes and schemas; their random temporary-file writes need no additional lock. FRESH publication performs only bounded local vacancy checks while holding the aggregate lock and publishes its entire consumption/holder/completion in one aggregate generation. Native publication refuses a FRESH-owned root; FRESH refuses any existing native installation/seal/occupancy or ambiguous root artifact. This intentionally prevents mixed-route installation. Legacy records are not converted to FRESH authority. A native interruption may leave old partial files: FRESH refuses those rather than repairing them. A FRESH interruption before rename leaves no holder/consumption; after rename it leaves both. Root identity derives from the fixed canonical root, not policy/sequence/instance labels.

The explicit v2-to-v3 migration adds `augur_migration` while retaining the original v1-to-v2 migration and all existing maps/bytes. The new migration retains the exact prior v2 subtree plus its digest and predecessor head; this is bounded by the existing input/storage limits and enables preservation validation without rewriting history. Only v3 permits one `bindings` entry. That entry is the produced H `imperium.bootstrap-augur-holder/v1`, body `{policy_ref,command_ref,terms_ref,constitution_ref,root_identity,seat,generation,binding,profile_ref,persona_ref,charter_ref,base_completion_ref,base_proposal,expires_at}`. Its sources are exact admitted originals; the owning founding completion references this holder. No applications or assessment_views producer is introduced. Unknown versions and arbitrary v2 bindings continue to refuse.

The admitted constitutional input has kind `augur-constitution`, schema `imperium.augur-constitution/v1`, and `{instance_id,operator_id,root_identity,seat,charter_ref,persona_ref,profile_ref,not_before,expires_at}`. It is an original constitutional grant, never a claimed open-window boolean. A fixed ConstitutionEvidence verifier authenticates root/artifact provenance; its production default refuses. Actual vacancy comes from the shared native owner under the journal lock. Founding intents are exact `augur-founding-intent` sources `{constitution_ref,binding_ref}` inside the already-approved finite eligible_binding terms. The real O1 proposal is reconstructed at the owning base completion time, checked against its original result refs, and retained in the holder; current founding eligibility is also checked. The dynamic core derives only the exact matching permitted terms and original base completion refs.

Cognition uses a context-aware extension of PreparedOperation only where the existing state-free method cannot resolve the produced holder and prior group outcomes. The same method is called from reservation and every custody revalidation with the owning locked frame. The fixed combined adapter retains the exact same KeySource object as access and delivery. A separately admitted `augur-assessment-commission` binds the founding step, group, exact Profile, evidence/input originals, tokenizer/account/tariff evidence and expiry. A fixed factual verifier defaults to refusal. Preparation freezes canonical public inputs, predecessor outcomes, holder and group context into the exact existing Wire format. ProviderResponse validates complete transport/usage, and ParsedResponse validates the exact O1 context before group completion. No endpoint, callback, key source, readiness boolean or retry classification comes from ingress. POST remains private to Runtime custody.

Final local status: IMPLEMENTED_PENDING_SOURCE_REVIEW_AND_FRESH_FULL_CI. The owner, migration, FRESH holder and governed cognition bridge are implemented. The report and proof matrix bind the recorded local tests; all operational flags remain false.

Clean entry: 02ce09621c63822eaff9ffd23151d5d3aed593a4, tree 2fc0e9075ce199312797533d237652fa15b82865. All construction is fixed infrastructure; public ingress remains the existing O2 command schema.

The first projection uses an admitted `augur-base-input` source, schema `imperium.augur-base-input/v1`, with approval_ref, decision_ref, selection_rule_ref, profile_ref, universe_ref, account_scope, rows and evidence_refs. Each row is {binding_ref,facts_ref}; `augur-base-facts` content is {binding_ref,predicates,bounds,tariffs,contradictions}. `augur-base-observation` content is the existing O1 observation shape without its self reference; the reference is derived from the retained original. The base Profile is `augur-base-profile`, content {seat,predicate_ids}. O1 policy/workload/requirements/candidate/configuration identities and issue/current/expiry milliseconds are derived from actual admitted policy originals, not supplied projections. All other referenced originals are resolved through AuthorityStore.

BaseEvidence is a fixed infrastructure verifier of the complete factual projection and its exact retained originals. Its default refuses. It must establish actual provider/account/token/framing/tariff provenance and numeric content; an Operator signature and an OFFICIAL/PASS string are insufficient. Test-only verifiers may pin explicitly synthetic originals. BaseSelector remains the unchanged O1 implementation. SELECT_BASE's owning O2 completion retains the input-original reference and actual selected binding reference, rather than accepting a prewritten winning proposal. No eligible candidate refuses before a completion is published.

The former native/aggregate publication gap is closed by the common owner described above. No standing authority is relabelled and no native record is rewritten into a FRESH holder. Historical v2 remains unchanged and cannot publish bindings. The original projection was insufficient alone; the v3 producer now authenticates and preserves its proposal with the actual owning completion.

The projection additionally checks the existing `runtime-binding-map` source shape against each exact candidate binding: provider, adapter, model reference, dispatch ID, canonical request-configuration digest, accepted revision limitation and effective interval. Its snapshot and all row model references resolve as originals. The existing map-base effect remains the only mapping completion owner; a matching candidate-map row is not completed approval or founding. No mapping schema or O2 consumer is changed.

Final validation captures the complete tracked PHP source set. The exact-wire token evidence also bounds accepted POST usage; a provider response above that authenticated bound remains an unknown outcome, even below the global reservation ceiling. Holder snapshots are bounded to 1 MiB, and their mapping must equal the actual map-base completion.

## R1 correction: exact mapping support

The fixed per-cognition envelope is 1 call / 16,384 input / 4,096 output / 100,000 micro-USD / 60,000 ms. MappingLimits resolves the exact current original row and compares every supported meter with this envelope. Smaller mapped values exclude the configuration; no approved setting is lowered and no mapped limit inherits a global maximum. Larger supported values do not raise the operation or shared budgets.

BaseProjection first authenticates the original factual input, then demotes adapter_support to FAIL for each unsupported row before invoking the unchanged O1 selector. This preserves deterministic least-cost eligibility, existing adverse facts, exact tariffs and a reproducible snapshot. FreshProducer's publication and current-holder reconstruction consume that same result. AugurAdapter independently reloads the selected map and checks the prepared maximum at context reconstruction, including custody and dispatch/response boundaries. No new record version, source owner, locking boundary or delivery port is introduced.

The correction report/proof matrix identifies fresh corrected-source runs, including all five strict/zero meters, exact-bound success, supported-alternative selection, mapping expiry during credential callback and the unchanged reviewer and W1/W2/W3 tests. Source review and fresh complete hosted CI remain pending.
