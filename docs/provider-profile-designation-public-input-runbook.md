# PPC2 future public-input runbook

This partial packet authorizes no deployment or live operation. Review and fresh complete hosted CI are required before integration. No real credential or private installed file is needed for review.

For the implemented component, supply a disposable native institution through `OperatorRootPersonnelInstallationService`, public Formation trust through its existing explicit owner prerequisite, and incumbent-scoped personnel delegations. The versioned Profile ingress requires original signed full Profile bytes plus admitted Persona/suitability. Produce fresh four-Senate findings, Lord Speaker examination, exact competent approval and Conscription qualification for that version. Use the existing separately signed appointment only when testing appointment mechanics; it creates no current model settings or execution permission.

Empty designation initialization is an explicit offline owner call with the current `{generation,digest}` and a signature over `{schema,citadel_id,expected_head}` for `INITIALIZE_FORMATION_PROFILE_DESIGNATIONS`. It imports nothing. Existing affected sessions/custody must be resolved by their owners; initialization refuses rather than guessing their status.

Before completing PPC2, provide a reviewed native model-binding identity/correspondence source: exact authorized sealing original, original authorization/dossier line, precise O4 provider/model/version and binding/configuration references, immutable binding generation, and its producer competence/currentness rules. A candidate-binding H record or an assigned integer is insufficient. Complete the common outer-fence implementation and proofs listed in the writer inventory, then implement designation event history and the shared owner-frame application/use verifier. Keep all other provider-dependent ports independently refusing.

Retain `DEFER_ENROLLMENT`, empty actual retry allowlist and all five false operational flags. Do not route around FRESH vacancy, copy installed incumbents into a synthetic parent, change historical onboarding schemas or substitute a Profile seal for designation.
