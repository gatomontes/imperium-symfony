# Amendment correction Batch 2

Each canonical authorization now has an initialized AUTHORIZED lifecycle keyed by its authorization ID. Generation binding hashes the exact authorization ID/digest, dossier ID/version/digest, mission ID, target including repository, paths and budgets. Commissions, signed capabilities, lifecycle, inspection snapshots and receipts carry this binding. Completion verifies the inspection binding before receipt creation. Status selects only the requested authorization's lifecycle/receipt, reports its generation and current-pointer relationship, and preserves historical lifecycle separately from inactivity. Terminal mission tombstones prevent same-ID re-entry; cancellation of historical A cannot cancel current B.

Schema imperium.protected-state/v2 is explicit. Missing schema and mixed mission-ID-keyed records refuse with PMA_STATE_SCHEMA_REQUIRES_OWNER_MIGRATION. No migration or actual installation ran. Future owner-reviewed migration must archive old records as historical, never assign them a new generation or reset a real journal.

Focused: 20 tests / 305 assertions, zero skips. Command: php vendor/bin/phpunit --filter 'ProtectedMissionAuthority|MissionAmendment' tests. An initial strict comparison found binding field order changed after persistence. Production binding now returns canonical ordering; strict assertions were retained. Initial failure transcript preserved alongside passing transcript under var/mission-amendment-evidence. Full suite will run on this exact committed head before Batch3 work.

Changed-test map: Batch4's explicit disposable missing-inspection corruption now changes the initialized lifecycle by authorization ID, preserving its exact binding, instead of fabricating a mission-ID record. It still requires PMA_INSPECTION_EVIDENCE_ABSENT, unchanged journal hash and no receipt. Fixture adds a second real loose Git blob/path to prove legitimate changed-allowlist success; all prior exact-byte assertions remain. No methods removed.

Positive and negative new proofs vary paths, budget and repository independently at identical commit/tree; B complete refuses before new admit/inspect, B then completes with its own authorized findings; A's old history remains byte-equivalent and has no borrowed B receipt. Legacy and deliberately corrupted wrong-generation state/receipt/inspection records refuse.
