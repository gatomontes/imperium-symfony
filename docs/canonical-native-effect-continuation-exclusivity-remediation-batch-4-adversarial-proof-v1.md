# Canonical Native Effect Continuation and Exclusivity Remediation — Batch 4 adversarial proof v1

`BATCH_4_PROCESS_CONTENTION_SUBSTITUTION_AND_BYPASS_PROOF_COMPLETE_NO_EXTERNAL_IO`
`BATCH_5_TERMINAL_AUDIT_NOT_YET_COMPLETE`

The corrected corridor is exercised with disposable local state and provider
doubles across independent PHP processes. Distinct authorities carrying one
semantic tuple produce one admission and one explicit unconsumed loser.
Admit-and-exit followed by a fresh-process first-callback attempt refuses before
callback start. Callback loss remains terminal unknown. Response-sealed process
loss permits receipt-only forward completion without invoking the supplied
callback.

Direct and container construction share one continuation issuer between
admission and execution. A fresh issuer, copied object or reconstructed metadata
cannot authenticate. Tampered immutable admissions fail digest verification;
old-digest authority tampering fails semantic identity validation; resealed
same-authority changes cannot create another winner. Expiry, embedded
revocation/cancellation, post-winner changes and unknown outcomes remain
fail-closed.

Static and real-container checks preserve the absence of commands, credential
resolvers, AgentMail transports, HTTP clients and network operations. Workers
are local test fixtures only. No credential, external I/O or live effect occurs.

Batch 5 must independently reconcile source-labelled evidence and conduct the
terminal Blackquill audit. It is not completed by this document.
