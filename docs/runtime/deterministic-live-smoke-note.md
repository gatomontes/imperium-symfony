The `imperium:deterministic:smoke` command proves the mechanical La Cortine path without network access or cognition. It exercises exact payload authorization, one-use credential brokerage, deterministic `email.send` transport semantics, raw receipt admission through Lazaretto, and asserts `sortie=NONE`. This smoke is intentionally provider-neutral; a real email adapter can replace the fake transport without changing the boundary contract.