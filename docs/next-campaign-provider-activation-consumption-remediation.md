# Next campaign: Provider Activation-Consumption Remediation

## Status

CAMPAIGN_SELECTED_PREPARATION_BATCH_0_ONLY

The terminal audit proved that one ACTIVATED_UNCONSUMED single-operation activation can be
referenced by separate durable execution authorities because governed admission arbitrates only the
authority ID.

Preparation Batch 0 is authorized to inventory and select the smallest same-root correction. It
grants no runtime contract, activation or authority consumption, credential access, provider
invocation, external I/O, live-command adoption, Iron Gate or Lazaretto authority.

Preparation must determine the combined winner identity, schema version, lock scope, one-record
commit, replay, contention, expiry, revocation, crash recovery, reconstruction, v1 migration,
secret exclusion, and repeated terminal-audit gate. Missing atomicity evidence requires refusal.
