# PPC6 owner and caller inventory

The new designation delegate/publish/current entry points hold Formation and call actual same-root institutional verification. Native mutation remains under accepted R2 producers/storage. AtomicTransition, FormationJournal/FormationOwnerFrame and accepted storage successor bytes are unchanged. No upward acquisition or new owner-token manufacture is introduced.

| Caller | Owner path |
| --- | --- |
| CommandLedger::advance | changeAtHead callback passes owner into prepareApplication |
| ApplicationOwner::replace | same callback owner into prepareApplication before complete-pair publication |
| prepareApplication | OwnerAssignmentEvidence::verifyInOwner or unchanged generic interface |
| PersistentSettings::revalidate/resolve | inspect owner into current; both tuples verified before projection |
| PersistentSettings::verifyFormation | actual owner required; same aggregate, exact current state, then whole-pair current verification |
| FormationSettingsBinding::verify | requires owner, passes it with effective configuration |
| SettingsBoundTransport::verifyExecution/invokeForFormation | existing inspect owner through binding; external invoke remains after inspection |
| SettingsPreparedTransport | inherits protected checks and invocation boundary; no independent owner acquisition beneath a lock |
| FormationCognition | reservation/start/delivery checks carry owner; session source and lease derivation receive it for versioned Profiles |
| FormationClaimCustodyBroker::deliver/advance/validate | owner from each existing change callback passes through settings, session and lease checks |
| FormationSessionAuthority / FormationSessionLeaseService | optional owner preserves historical generic behavior; versioned native checks require live owner |
| FormationPersonnel::appoint | both model v1 and strict v2 select candidateInOwner; independent exact v2 Locksmith holder reconstruction is checked |
| Composition | unchanged MissingAssignmentEvidence and production wiring; native specialization remains explicitly constructed/dormant |

Historical schema-less fixtures retain their old generic behavior; they cannot satisfy NativeAssignmentEvidence's strict-v2 current seam. State arrays cannot replace its live same-root owner or exact AuthorityStore object. A settings check grants no session, credential, dispatch, retry or execution authority. Review inventory command: rg -n 'prepareApplication|verifyInOwner|verifyFormation|verifyExecution|verifyModelSettings|settings->verify|leases->derive|validateSession' src/Imperium/Runtime.
