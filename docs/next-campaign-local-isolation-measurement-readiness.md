# Local Isolation Measurement and Readiness — continuation

`LOCAL_ISOLATION_MEASUREMENT_READINESS_CONTINUATION_SELECTED`

Parent campaign: `docs/next-campaign-local-isolation-useful-mission.md`.
Review: `docs/local-isolation-package-independent-review.md`.
Runner: `docs/handoffs/local-isolation-measurement-readiness-ready.md`.

## Purpose and lineage

Close LI01 and LI02, then resume the parent campaign's Batches 3–5. Do not
reconstruct completed Batches 0–2 or broaden the security model.

Preserved local source head:
`574bfc69aebf12b5197f4f808b54896bf02ba142` on
`codex/local-isolation-useful-mission`. Tested code:
`47bcc44a8fc0540bef855d27ba71abaebb452c84`.
Planning base: `ad9518acb095f69bbea4fef19cf383a44286c077`.
The implementation remains local; this selection authorizes a corrective local
descendant and local integration of these planning documents. It does not admit
that implementation to main.

Preserve package `package-47bcc44a` and manifest
`8DAB48F98D147A6ABA9E89260D25082BA5B91AD2C72638F1F10FFB9A449CF6AE`
as historical evidence. Inspect and preserve any later local review-assembly work.

## Steps

| Step | Work | Exit evidence |
| --- | --- | --- |
| C0 | Verify lineage/package and reproduce LI01/LI02 in disposable fixtures. Freeze the required access matrix, phase model and readiness evidence schema. | Exact affected-source/test inventory and failing cases; no production fix. |
| C1 | Cover exchange and both probe-plan surfaces. Implement deterministic readiness validation and invoke it before mission mutation. | Complete valid set passes; missing, duplicate, altered, stale and mismatched sets refuse without authority mutation. |
| C2 | Run the supported PowerShell/CLI route, commit code before audit, run focused/full tests and build a fresh package/review packet. | Exact tested SHA/tree, counts/skips, fresh manifest, owner commands and limitations. |
| Parent 3 | Owner installs the reviewed package and records actual Runtime/caller measurements. | Scoped actual-account evidence validated; human transport facts recorded. |
| Parent 4 | Owner signs the exact dossier and Runtime executes the one selected read-only mission. | Observable lifecycle and generation-bound receipt. |
| Parent 5 | Reconstruct receipt/target bindings and write the discrepancy report. | Terminal local evidence with bounded claims. |

C0–C2 are authorized sequentially with separate local commits. Finish the
reviewable owner package before requesting human action. Owner account creation,
elevated installation, passwords and private signing remain in owner custody.
Once valid prerequisites exist, resume Parent 3–5 without redundant permission.

If an installation already exists, inventory it read-only. Never apply the fresh
installer over it or reset/migrate its journal.

## LI01 requirements

Derive the expected matrix from reviewed role policy. Cover:
- `ProtectedMissionExchange`, `ProtectedMissionProbePlans`, and
  `ProtectedMissionPostEnrollmentPlans`, their finite contents and relevant
  ancestor replacement rights.
- Runtime-required exchange operations; caller denial of exchange read/write;
  intended plan-reading and modification rights for each role.
- read/list, write/append, delete/delete-child, write-DAC and write-owner as
  applicable, plus owner/group/ACL/reparse and actual SID/token evidence.
- pre-enrollment canaries, post-enrollment journal and later exchange outputs.

File absence, wrong identity, administrator token, unknown OS error, missing row
or skipped phase is not success. Use trusted positive inventory when the caller
cannot enumerate protected paths. Probe by opening handles only; do not read,
write, delete or replace protected operational bytes.

Avoid self-referential plan hashes: separate canonical path/right enumeration
from the detached final plan/file manifest. Classify Runtime-writable outputs and
owner-controlled readiness/reference evidence explicitly. Owner-controlled files
must resist both direct and parent replacement according to the selected policy.
Do not claim exclusion of the legitimate Runtime from its own issuer state.

## LI02 requirements

Use the existing stack. The validator must:
- recompute evidence-file hashes and bind the reviewed package/installed bytes;
- bind exact Runtime/caller SIDs, roles and token groups to installation metadata;
- derive required coverage from policy and installed inventory, rejecting
  missing/extra/duplicate rows, altered masks/expectations, empty measurements,
  unknown results and producer-summary-only evidence;
- bind pre/post-enrollment phases, plan hashes, setup session, public fingerprint,
  protected configuration and exact startup evidence;
- preserve native exit codes and distinguish the required identity refusal from
  generic nonzero failure;
- return machine-readable refusal reasons and a public binding summary.

Run it before Prepare, Accept and Step. Keep Status available for read-only
recovery when readiness fails. Invalid readiness must not prepare, derive, issue
or consume authority. An editable status/boolean or unverified digest field is
insufficient.

The validator checks completeness and consistency of trusted observations. It
does not cryptographically authenticate producer-controlled logs, prove the
human transport arrangement or exclude a compromised trusted Runtime. Record
collection/custody and owner facts separately.

## Verification and evidence

Negative fixtures must include: omitted exchange/probe surface; reduced plan;
missing/duplicate row; changed expectation/result; wrong SID or admin identity;
hash mismatch; unrelated package/session/trust; missing startup evidence; generic
error presented as expected refusal; changed evidence bytes with an unchanged
claimed hash. One complete valid set must pass.

Prove invalid readiness refuses before mutation and Status remains safe. Preserve
AM01/AM02, package receipt and prior tests. Record every changed expectation.
Run focused tests after relevant changes and full `php vendor/bin/phpunit tests`
on committed final C2 code. Exercise PowerShell on Windows. Report skips honestly.

Any changed executable requires a fresh package directory and manifest. Keep the
selected inspection target
`a1fc4f27634319f2a22df2e6a1b370f70cdb98bf`, allowlist and budgets distinct
from the installed-code and evidence commits. Rebuild the owner runbook,
independent-review Markdown and derivative manifest. Inspect public artifacts for
secrets; never publish journals, issuer/private keys, passwords, pending
signatures or usable capabilities.

Successful local correction status:
`LOCAL_ISOLATION_MEASUREMENT_READINESS_PACKAGE_READY`.
Return its package path/digest, exact owner commands, tested identity and remaining
human inputs. Parent completion still requires actual measurement, signature,
mission and receipt/report review.

No implementation push/PR/main merge, branch deletion, provider action, target
mutation, journal reset, automatic migration, second mission, Iron Gate,
Lazaretto or live Batch 7. Initial fetch and local planning integration are the
authorized network exceptions. This GitHub change publishes planning only.

*Ne passum quidem regrediemur.*
