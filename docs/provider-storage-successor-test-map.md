# PPC4 changed-test and requirement map

| Contract / requirement | Executable evidence |
| --- | --- |
| A: bounded source successor, exact approval and predecessor preservation | `StorageSuccessorRecordTest`: positive chain plus wrong predecessor, unlisted AtomicTransition path, candidate hash, approval hash/reference/disposition, duplicate, proposal bytes, acceptance status, base identity and predecessor-copy refusals. |
| A: existing pin assertions retained | Only a four-line bounded recognition branch is added in `NativeInspectionSnapshotConsistencyPreparationBatch0Test::testLedgerPinsTheRequiredDocumentsAndCompleteTracingSources`; all five original tests and all original assertions remain. Both historical ledgers and AtomicTransition are byte-preserved. |
| B/C: every reserved class, all three old entries, no effects | `ReservedInstitutionalStorageTest::testOldEntriesRefuseReservedTargetsBeforeAnyEffects`: 27 data cases, empty disposable root afterward, guarded callback not called. |
| B/D: actual grammar aliases, siblings, case and ordinary compatibility | `testValidatorAliasesAndOrdinarySiblings`, `testImmutableBootstrapIdCaseAndOrdinaryTrailingSeparator`, `testInvalidGrammarIsNotBroadened`, `testSymlinkAliasesAndDanglingTargetsRefuse`; external live/dangling junction probes and `alias-matrix.php` exercise every admissible old API/alias combination with no guard/effects. |
| C/D: explicit counterparts, all classes | `testEveryClassHasExplicitOwnedImmutableAndCasBehavior`: immutable replay and both CAS counterparts under the same actual owner. |
| C: wrong and escaped owner, guard not invoked | `testWrongAndExpiredOwnersRefuseBeforeTargetEffects`; unchanged PPC3 capability serialization and root tests. |
| D: replay, conflict, corruption, CAS guard order, exception | `testOrdinaryReplayConflictCasGuardOrderAndAbsentRoot`; process ordinary competition retains exact replay and one CAS winner. |
| D: strict error-handler compatibility for absent ordinary parents | `testAbsentOrdinaryParentsAreWarningFreeWithStrictErrorHandler` covers all three old APIs; the original child-Curia concurrent-completion regression is retained and rerun. Windows dangling junctions remain refused by the parent-directory-entry check. |
| D/E: ordinary publication between absence observation and directory enumeration | `testOrdinaryPublicationBetweenAbsentObservationAndDirectoryScan`: three native-process cases pause after the real absent observation, allow real competing publication, then require immutable replay or original CAS conflict without guard invocation. The test-only filesystem observation barrier does not supply an owner or replace a writer/consumer. |
| E: authentic producers and actual current consumers, both Seats, both race orders | `StorageSuccessorProcessTest::testOwnedGenericMutationAndAuthenticCurrentHolderBothOrders`: 12 cases (two Seats × three writer entries × two orders), controlled held/release files, independent nonblocking Formation-lock observation, native exits, fresh-process positive/current-refusal reconstruction. |
| C/E: no lower-to-upper acquisition and no primitive reacquisition | `testBareReservedWriteRefusesFromLowerGuardAndHeldTargetLock`: six process cases. |
| E: process interruption and guard exception release | `testTerminatedGuardReleasesBothLocksAndLeavesCurrentOriginalIntact`, `testOwnedGuardExceptionReleasesBothLocksAndPreservesOriginal`: current reconstruction succeeds before a later writer proves locks released. |
| D/E: prior generic gap becomes expected refusal | `NativeTenureSynchronizationTest::testGenericStorageRefusesBeforeGuardAndOwnedRetirementRefusesCurrentUse`; historical PPC3 gap evidence is retained unchanged. |
| D/E: native registry changes, unsupported successors, historical completed facts, interrupted packages | Existing PPC3 tests retained in the complete gate. No schema or current-authority fallback added. |
| D: real SourceReview fixture compatibility | Fixture occupancy seed writes migrated to the held owner; complete SourceReview gate remains unchanged. |
| F: full source and exact-case gate | Unmodified eight-partition worker and aggregate verifier plus all 11 Python guards; authoritative counts and native failures in the report and packet. |

All new subprocess waits have 30-second bounds and explicit attempt/held/release observations. Polling only waits for a control event; elapsed time alone is never the serialization assertion. Synthetic model declarations are unverified; public institutionally signed fixture originals are separate evidence. Neither is current production assignment evidence.
