# Next campaign: Frozen Runtime Coverage Tripwire Restoration

## Selection

`FROZEN_RUNTIME_COVERAGE_TRIPWIRE_RESTORATION_SELECTED`

## Objective

Restore complete mechanical detection of new runtime candidates, authority
store users and perimeter files without reinstating brittle unexplained counts.
Every intentional inventory change must be explicit, reviewable and classified;
new unsnapshotted files must fail closed.

The campaign also separates and adjudicates the unrelated PR #728 runtime and
vocabulary changes without rolling back a valid correction merely because it
was committed in the wrong campaign.

## Campaign sequence

1. **Preparation Batch 0 — regression inventory and restoration boundary.** Freeze every weakened assertion, escaped file class, expanded exception and unrelated runtime change. Define exact bidirectional set invariants and the snapshot-update rule.
2. **Batch 1 — mechanical tripwire restoration.** Restore bidirectional candidate/snapshot equality, exact authority-store consumer equality and scanning of every current perimeter file. Replace unexplained raw counts only with versioned inventories whose additions require explicit classification.
3. **Batch 2 — PR #728 change adjudication.** Give the two provider-runtime changes and expanded activation-disposition vocabulary separate focused tests and dispositions; retain, correct or revert each on evidence rather than provenance guilt.
4. **Batch 3 — terminal adversarial audit.** Prove that one new unsnapshotted candidate, store user, perimeter helper or unapproved disposition producer fails the suite, while approved versioned additions remain explicit.

## Terminal acceptance

Only Batch 3 may produce
`FROZEN_RUNTIME_COVERAGE_TRIPWIRE_RESTORATION_COMPLETE`.

## Binding exclusions

This campaign may not restore the independently verified evidence closure,
rerun the disposable mission, inspect the private receipt, handle signing or
provider credentials, invoke a provider, perform external I/O, change provider
binding, implement v3 admission or authorize live execution. The controlling
evidence posture remains
`CAMPAIGN_CLOSURE_REQUALIFIED_WITH_MATERIAL_INDEPENDENT_VERIFICATION_DEFECT`.
