# Local isolation campaign — accepted closure and GitHub integration

The owner reported independent Batch 5 acceptance on 2026-09-06 and authorized
publishing the existing branch, opening/updating its PR and merging after checks
and review requirements pass. Parent Batches 0–5 and the readiness/scratch/process
corrections are closed within their documented bounds. No new mission follows.

Accepted report checkpoint: bc7ef92a318b6e08924e95b5edc7961175bad309.
The independent review checked all 57 public archive entries and upload hashes,
23 Git objects across 40 reads, 448476 object-content bytes, 239613 file-content
bytes and all 15 historical source files against the earlier receipt. Installed
file mapping and bounded analytical conclusions were reviewed. Owner-run chain/
actual-target receipt reconstruction and installed-package verification reported
pass. This acceptance retains the report's verifier and custody limitations.

## Integration scope

Fetched origin/main: a06a2dec6fc1d62aeef7260e3a9c3508c801fffb.
Campaign branch: codex/local-isolation-scratch-correction.
The checkpoint is 24 commits ahead and zero behind origin/main; no integration
conflict or executable reconciliation is required. Existing earlier mission
authority/amendment implementation is already in the base. The campaign adds
the complete inherited local-isolation implementation, not just closing reports:

- Parent packaging and public byte/receipt reconstruction, finite native access
  probes, owner installation and resumable owner orchestration.
- Readiness validation bound to setup identity, package, exact policy, native
  responses, pre/post/current measurements and a 15-minute current window.
- Separate protected scratch, cleanup before authority publication, preserved
  abandoned work and status-only recovery.
- Windows PowerShell child module-path isolation and explicit proof-process
  input-delivery/refusal handling, with no automatic retry.
- Additive package/readiness/scratch/process tests and historical proof/report
  attribution. No dependencies were added to composer.json/composer.lock.

Before this closure update the diff comprised 93 paths: 41 docs, 8 source,
18 test/support, 25 tools and one task ledger. Production references to the new
ScratchWorkspace, WindowsPowerShell and ProofProcess classes and shared
PowerShell helpers are included in the same branch. Existing autoloading covers
the added classes. The accompanying integration inventory lists all inherited
commits and paths, including this documentation closure.

No changes under src, bin, tools, tests or Composer files exist between tested
commit a5d7e63d921057b529b6e2c5e2de8d12ed4ca0f8 and the report checkpoint.
Historical installed-package evidence remains attributed to that exact build,
not automatically to future changes. The real installation is not changed by
publication or merge. No package rebuild or real mission repetition is needed.

## Publication and validation gates

All 160 distinct changed blobs across the 24 unpublished commits were scanned
for private-key blocks and common credential token formats. Structured changed
JSON was checked for populated private-key/signature/capability/consumed-nonce
fields. No matches requiring exclusion were found. Tracked path inventory is
limited to source/tests/tools/sanitized docs; raw evidence under ignored var
remains local. Key-generation test code and public fingerprints are not private
signing material. This bounded scan is not a universal secret-detection proof.

PHP lint passed for all 22 changed PHP files; PowerShell parser checks passed
for all 29 changed PowerShell files; git diff --check passed. Fresh focused
tests passed 40 tests / 470 assertions on Windows/PHP 8.4.14 against the unchanged
checkpoint code (bc7ef92a), in 62.058 seconds. Historical full Windows 2689 tests / 52762 assertions and focused 40 /
470 remain attributed to a5d7e63d, not relabeled as this integration run.

Repository workflow .github/workflows/phpunit.yml installs locked dependencies
and runs vendor/bin/phpunit tests on Ubuntu/PHP 8.4 for PRs and main pushes.
Merge must wait for that workflow's result. At initial inspection, GitHub
reported main unprotected and returned an empty applicable branch-rules list;
no external approval requirement was configured. Independent campaign acceptance
is recorded from the owner, not manufactured as a GitHub reviewer approval.
PR checks and merge outcome are recorded in GitHub and the final integration
response; this pre-publication document does not claim an unobserved merge.

Known VPS access routes were removed, but absence of persistence was not
independently established. Owner custody, transport and trusted OS/administrator/
Runtime/signer assumptions remain. Receipt verification does not independently
anchor issuer identity or certify semantic truth/elapsed deadline. No universal
isolation, exhaustive crash durability or outside-allowlist claim is made.
Historical/quarantined branches, packages and attempts must remain preserved.
