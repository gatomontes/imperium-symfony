# Canonical Native Effect Reconciliation Issuance Authority and Revocation-at-Use Remediation — Batch 4

`BATCH_4_COMPLETE_ADVERSARIAL_APPLICATION_AND_INTERRUPTION_PROOF`
`BATCH_5_REQUIRES_SEPARATE_CLEAN_MAIN_AUDIT`
`NO_PROVIDER_NO_CREDENTIAL_NO_EXTERNAL_IO`

Batch 4 closes the executable adversarial matrix for the bounded local runtime.
The focused proof distinguishes source `SUSPEND`, `SUPERSEDE`, `REVOKE`,
`EXPIRE`, `RETIRE`, generation advance and v3 lifecycle migration after a
reconciliation capability has already been resolved. Reconciliation source
resolution translates those present-tense failures into the Batch 1 refusal
vocabulary without changing the native error contract for unrelated consumers.

The combined proof also retains the earlier missing/counterfeit capability,
exact-object and process-incarnation custody, consumed/replayed authority,
changed-target conflict, Root/native revocation, expiry boundary, record
substitution, competing claimant, interruption recovery, fresh-process worker,
container construction, read-only reconstruction and frozen-perimeter cases.
The cooperative single-host filesystem lock boundary is proved; multi-host and
hostile direct-writer behavior remain explicitly outside the claim.

Focused Batch 4 proof passed `36 tests / 149 assertions`. The combined campaign
adversarial, provenance, continuation, process-custody, transactional-consumption
and frozen-runtime gate passed `287 tests / 5682 assertions`.

The inspected runtime path contains no credential broker, provider transport,
HTTP client, environment-secret or external-network edge. Batch 7 remains
suspended.
