# Canonical Native Effect Corridor Activation — Batch 4 provider-double recovery v1

`BATCH_4_PROVIDER_DOUBLE_UNKNOWN_OUTCOME_RAW_RESPONSE_AND_RECEIPT_JOIN_COMPLETE_NO_NETWORK`

The provider-double continuation commits callback start before invoking the
callable. An exception or process loss after that point leaves
`UNKNOWN_REPLAY_PROHIBITED`; a second callback is refused. A returned observation
is sealed with exact bytes digest and times, classified accepted/rejected,
validated against `agentmail.message/v1`, bound to a non-authorizing Lazaretto
admission and reconstructed read-only.

The response proves local callback lineage, not remote cryptographic authorship.
Rejected or malformed output never grants retry. Exact completed replay returns
the immutable receipt without calling the double again.

The service imports no credential broker, environment resolver, AgentMail
transport, HTTP client, Iron Gate or Lazaretto runtime. No network I/O occurs.
Batch 5 is not authorized by this document alone.
