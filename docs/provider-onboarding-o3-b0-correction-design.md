# O3-B0 custody identity correction

Entry is ef30a8fa76afe2a2fbc2983e4ffae7c6b42d3862, tree 887b138ca20e513721d3271742f17d5b683fc5ea. The review demonstrated that independent KeySource constructor arguments allowed verification of one source and delivery from another. The unchanged reviewer test reproduced dispatch and successful settlement locally before this correction.

AccessAdapter now checks strict object identity of the Runtime delivery source against its own fixed source. Runtime invokes that check before constructing transport or coordinator infrastructure. Distinct objects refuse even if they advertise identical generation strings; null versus a selected source also refuses. Both-null default construction remains dormant. This identity check grants no authority and retrieves no key. The constructor signatures remain compatible with correctly bound existing infrastructure and fixtures.

Both source properties are readonly and the adapter is final and readonly, so successful construction fixes one shared source for verification and delivery for the lifetime of the Runtime. AccessAdapter.verify still resolves the authentic admitted credential-binding original and checks its opaque_binding against that source's current generation. Runtime.current still invokes the actual O2 coordinator check against the journal original at issue, consumption, callback and dispatch; those checks now necessarily observe the delivery source. Generation-only capability checks supplement this authority check and do not replace it. KeySource generation remains a trusted, pure infrastructure currentness contract; this correction does not claim to authenticate a malicious custody implementation that lies about its own key generation.

The sole FormationJournal, O2 coordinator, schemas, checkpoint order, one-use capabilities, maxima and source fences are unchanged. A pre-admission generation mismatch refuses in original-backed verification. A constructor source mismatch refuses even earlier. Rotation after reservation prevents transport, leaves the reservation uncertain and retains its maximum and fence. Restoring the generation cannot supply missing response evidence or make reconcile dispatch. Public copied claim text, adapter names and returned booleans do not establish custody identity.

The unchanged reviewer regression uses public Runtime construction and actual synthetic B0/O2 authority. Additional construction cases cover distinct sources with matching and different generation strings and a missing delivery source. Boundary tests use privileged reflection only to pause the real coordinator at actual committed checkpoints, rotate the shared delivery source, and exercise the existing private boundary. They assert exact refusal, zero transport, no envelope, no settlement, retained maxima/fences and no recovery dispatch. Existing public positive access/replay and callback rotation tests remain required. No positive cognition bridge or production account evidence is introduced.

The only other existing file changed is the CI workflow: the supplied focused reviewer step precedes the unchanged `vendor/bin/phpunit tests` command, and the job budget rises from 15 to 30 minutes. Supplied historical logs show 3,172/3,604 tests before cancellation, with nonuniform runtime and substantial slow sections. Thirty minutes supplies headroom for the complete suite and focused step; it is a proposed budget, not a prediction or completed hosted proof. No tests, assertions, process deadlines or coverage are removed. Fresh full hosted CI on the corrected source remains required before acceptance.
