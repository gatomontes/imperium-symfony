# PPC2 changed-test map

| Test | Actual observation / limit |
| --- | --- |
| `FormationModelBoundProfileTest::testActualNewVersionApprovalQualificationAndSeparateAppointment` (both targets) | Actual disposable native installer, incumbent-scoped delegations, signed new Profile ingress, new four-Senate findings/examination, exact approval and qualification, then separately signed appointment. Lifecycle ends approved; no designation. Synthetic model specification is explicitly **not** proof of model authorization/O4 binding identity |
| `testOldIngressAndValidatorRetainTheirRejections` | New schema rejected by old ingress without journal change; historical validator rejects model_binding |
| `testCurrentVersionCannotBorrowAuthorityOrCorruptOriginals` (8 cases) | Corrupt Profile/examination/qualification/finding originals, signature corruption, older-version approval, real delegation revocation and expiry refuse. Corrupted frames are adverse in-memory inputs, never positive producer fixtures |
| `testForeignTargetAndChangedConfigurationRefuse` | Changed immutable bytes and wrong target refuse |
| `testCorruptedVersionedOriginalCannotDowngradeToHistoricalValidation` | Removing the schema/model-binding fields and recomputing only the artifact digest cannot route the retained original through historical validation; the envelope identity is checked before schema dispatch. This regression failed before the correction; both results are retained |
| `testFreshProcessReconstructsOriginalsAndObservesDelegationRevocation` | New PHP processes load originals and current native incumbent files; real delegation revocation blocks later read, historical journal remains |
| `FormationProfileDesignationInitializationTest` | Exact owner operation creates empty extension; previous bytes unchanged; stale/duplicate and wrong-effect requests refuse; two real worker processes signed at one head publish once |

New fixtures keep generated private signing keys in memory only and use the actual producer code. Worker inputs contain public signed evidence and synthetic clock values. No fake source is presented as native currentness. No historical test was weakened or reinterpreted.

Not implemented/proven: designation/revocation-purpose delegations and acts, live supersession/terminal event mechanics, authenticated O4 binding-generation correspondence, native replacement/apply/use races, designation crash recovery, complete assignment integration and combined FRESH establishment. These missing rows prohibit `ASSIGNMENT_EVIDENCE_IMPLEMENTED_OFFLINE`.
