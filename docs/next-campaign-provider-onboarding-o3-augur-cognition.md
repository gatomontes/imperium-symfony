> Current status: O3 is accepted, integrated and closed. See [O3 closure](handoffs/provider-onboarding-o3-complete.md). O4-B0 is selected. The source snapshot and instructions below are historical; original approval and evidence qualifications remain intact.

# Next local campaign — O3-B1 FRESH Augur binding and cognition

Ad Imperium.

O3-B0 is accepted and integrated in PR #792, merge `32bb8d76424d6b7447cc423cb7932af35b1f074e`, tree `36cf88119bde960250a0bb2fe5831b66c9d94d3e`. Its correction closes the actual KeySource binding defect. Fresh full CI passed 3,612 tests / 56,618 assertions / four skips in 17:16.006. [Acceptance](reviews/provider-onboarding-o3-b0-correction-acceptance.md), [identities](provider-onboarding/o3-b0-reviewed-integration.json).

This selects one direct O3-B1 implementation run under the [contract](../contracts/provider-onboarding-augur-cognition.md). [Local handoff](handoffs/provider-onboarding-o3-augur-cognition-ready.md), [prompt](handoffs/provider-onboarding-o3-b1-local-prompt.txt). Three planned implementation batches remain: O3-B1, O4-B0 and O5-B0. Live commissioning stays separate.

## Ordered work

1. Start from the published preparation in an isolated clean worktree. Record exact entry commit/tree and inspect repository instructions and the actual source owners named in the contract.
2. Declare exact internal FRESH source/holder/commission/projection shapes and ownership in a short design note. Close the base-selection projection and approved eligible-binding derivation using authentic retained originals.
3. Implement the constitutional FRESH founding/holder bridge with original source authority and atomic one-use publication. Preserve standing/existing-installation refusal and original schemas.
4. Connect W1/W2/W3 assessment preparation, exact token/tariff/account evidence, actual DeepSeek custody POST and O1 semantic validation through the real O2 coordinator. Stop before target-assignment application.
5. Prove actual temporary-root producer-to-consumer success and the negative/fault/concurrency matrix below with mock HTTP only. Resolve concrete in-scope failures; do not weaken tests or defaults.
6. Run existing regression/inventory gates, changed PHP lint, non-debug kernel smoke and all 86 static specification checks. Capture exact tested PHP bytes and complete commands/logs/exits; update documentation once after stable implementation.
7. Return one complete public all-deliverables ZIP plus report/instructions. Stop within O3-B1 for independent source review and fresh full hosted CI; do not push/merge the local implementation or start O4.

## Required proof matrix

| Area | Required proof |
| --- | --- |
| Base projection and mapping | Actual O1 selector on admitted originals; no eligible candidate, stale/missing evidence, changed configuration/Profile, false tokenizer/tariff/account bounds and selected-tuple substitution refuse |
| Dynamic founding authority | Exact approved eligible-binding rule derives only from actual completed base evidence; forged completion, different policy/instance/slot, unpermitted object, caller projection and alias substitution refuse |
| Constitutional FRESH producer | Genuine synthetic signing/admission and real root/window producer path; original-backed holder binding; no fabricated occupancy/approval or standing-route shortcut |
| One-use founding | Two processes contend for the same authority/window; stale head and changed-ID replay refuse; crash before/after publication leaves a coherent original state; a seal/occupied/existing route cannot reopen |
| Current holder and commission | Exact seat/Profile/model/configuration/credential lineage; wrong/expired/revoked holder, foreign commission, source replacement and generation changes before issue/consume/callback/dispatch prevent unauthorized transport |
| Actual custody POST | Positive path uses public operational entry, real coordinator, same verified delivery source, exact prepared bytes and mock HTTP; copied capability, alternate service/endpoint, operation swap and duplicate callback refuse |
| Assessment groups | W1 then W2 then W3 with exact frozen predecessor results; semantic invalidity, unsupported evidence refs, context/model/usage contradictions and no-fit outcomes handled by existing O1 contracts; no assignment applied |
| Bounds and unknown outcomes | Strict token/fee/deadline/response limits; FC/onboarding shared concurrency and exposure; empty retry allowlist; HTTP failure, timeout and post-dispatch revocation preserve uncertainty and fences |
| Storage and recovery | Immutable original envelope, metadata/envelope/settlement interruptions, no secret in public outputs; restart reads evidence only and cannot recreate credential delivery or founding rights |
| Preservation | Original O2/O3 reviewer tests, source pins, inventories, legacy consumers and kernel remain valid; any necessary new consumption boundary has an honest inventory row |

## Validation and delivery

Use PHP >=8.4 and locked Composer dependencies. Run all new O3-B1 tests plus the existing DeepSeek reviewer/correction/access/component/storage tests; O2 admission/ledger/custody/shared-exposure/process/reviewer selections; O1 base/selection/response validation and source-pin selections; FC/native regression and inventory/tripwire selections. Discover exact new test names from implemented source. The prior correction's `final-commands.json` supplies the existing selection lists; corresponding test classes are also discoverable in the repository. Do not claim a planned test class exists.

Record versions, argument vectors, cwd, times, exit codes and full public logs. Preserve failed/superseded runs separately. Capture all final tested project PHP bytes/hashes and distinguish later documentation-only commits. Retain the full-suite CI command and 30-minute allowance; selected local runs do not count as hosted full CI. Preparation CI validates this documentation commit only, not the future implementation.

Return `provider-onboarding-o3-b1-all-deliverables.zip` with README, report/instructions, canonical changed files, exact patch, incremental bundle with entry prerequisite, entry/tested/final commit and tree identities, final commands/logs/exits, tested-PHP snapshot/hashes, protected-original comparison and proof evidence. Include a checksummed inner review ZIP and complete payload manifest. No outer checksum or recursive historical archives. Never include real credentials, private evidence or installed state.
