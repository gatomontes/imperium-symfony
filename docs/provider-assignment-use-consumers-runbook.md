# PPC9 writer / consumer inventory and future public-input runbook

No new writer or production wiring is added. Existing NativeAssignmentEvidence now establishes the same bounded pure-parse scope for direct calls that application/settings already supply. New proof callers are the three tests, their shared original-case/public-proof helpers and fresh-process reader. This inventory distinguishes source-path equivalence from execution. The exact path/line/byte hashes and matching source lines are retained in the packet's `source-inventory.json`.

## Writers and owners

| Writer group | Authoritative path and ownership | PPC9 disposition |
| --- | --- | --- |
| Source trust, admission and revocation | AuthorityAdmission/Enrollment and Admission; AuthorityStore; Formation journal | Actual disposable trust and policy admission from existing fixture; actual refused late source admission and A/B unresolved target. No new admission method. |
| Bootstrap commands/effects and founding | CommandLedger; ApplicationOwner; FreshProducer; OperatorRootOwnership | Actual configure/access/base/map/founding owners through existing FreshEstablishmentFixture. External ports synthetic. No source-policy reset. |
| Native institutions | FormationFreshEstablishment; FreshInstitutionPackage; FreshEstablishmentRevocations; native installer and native registry boundaries | Actual R4 fixture route; prior writer inventory and receiving corrections retained. No PPC9 native writer modifications. |
| Personnel originals, approval and appointment | FormationPersonnel, FormationSignatures, FreshInstitutionalPreparation | Actual current-role delegation/signatures, candidate verification and both generation-1/2 target appointments. No direct journal seeding. |
| Native model and designation | FormationModelPreparation, FormationProfileDesignation | Actual preparation/seal/revision/findings/qualification/designation. New tests do not define a broader predicate or mutate retained signed bytes. |
| Persistent complete settings pair | CommandLedger uses ApplicationOwner::prepareApplication / publishApplication / replace; ApplicationHistory and AssignmentMigration | Source-traced; no native application publication execution. These are the only supported pair writers in this route. |

Retain [PPC6 writer inventory](provider-designation-assignment-writers.md), [native-tenure writer inventory](provider-native-tenure-writers.md), [Profile designation writers](provider-profile-designation-writers.md) and [PPC8 receiving corrections](reviews/provider-fresh-establishment-closure-review.md). In particular NativeJournal::change uses NativeBoundary; NativeTrust can publish its distinct registry under Formation after founding; that registry blocks unfinished/current FRESH validation as a successor. NativeAdmission::build is a pure constructor used by NativeProtocol. No broader closed-world claim is made from a text search or the number of inventory entries.

## Consumers and boundary propagation

| Consumer | Source path | Direct PPC9 execution versus shared-path claim |
| --- | --- | --- |
| NativeAssignmentEvidence | Fixed AuthorityStore identity → live owner → store state → exact O4 tuples/originals → current designation/independent holder/model → additional Profile IDs | **Direct** invocation reaches precisely recorded early refusals. Full positive route and final `profile.fits` branch are unexecuted. |
| ApplicationOwner / CommandLedger | prepareApplication invokes fixed OwnerAssignmentEvidence after full original assessment/selection; replacement uses same method with exact permitted set | Source equivalence only; no native application positive. Generic owner routing is not relabelled. |
| PersistentSettings | resolve and revalidate enter inspect → current reconstructs whole pair before projecting role → same OwnerAssignmentEvidence; verifyFormation retains passed live owner | Fresh-process **direct** role resolution refuses absent settings for each role. Source equivalence only for positive native use. Complete-pair current reconstruction is not proven by designation or candidate assembly. |
| FormationSettingsBinding | checks live owner, same settings aggregate, effective configuration adapter, then verifyFormation | Source-traced; does not create session, lease or dispatch rights. |
| SettingsBoundTransport / SettingsPreparedTransport | inspect/prepare use settings check; protected invokeForFormation verifies under owner, then calls underlying transport outside inspection | Source-traced native path. Protected delivery's authorization point is the current check before release; later mutation cannot cancel already authorized external I/O retroactively. |
| FormationCognition | original session authorization, reserve/deliver/start/response path; settings transport verification carries existing owner; invokeForFormation at actual invocation | Shared-path source trace. Independent session/cognition/lease/custody checks remain required and unchanged. |
| FormationClaimCustodyBroker | Locksmith custody checks current settings with passed owner when model_settings is present | Shared-path trace; no positive native credential delivery observation. |
| FormationSessionAuthority; CuriaFormationService; ChildCuriaFormationService | current candidate/appointment and authorization queries propagate owner to native personnel checks | Preserve receiving PPC6 correction. Source-traced indirect current consumers, not native settings end-to-end tests. |
| Default Composition / MissingAssignmentEvidence | Existing missing evidence remains default; request input cannot choose a native/permissive verifier | Byte-identical. No production promotion. |

New indirect consumer trace: NativeAssignmentEvidence::verifyInOwner → StrictJson::within → private verifyCurrentInOwner → existing AuthorityStore::checkSource → nested StrictJson::within / pure parser and canonical encoder. The nested scope reuses only the existing bounded synchronous context. ApplicationOwner and PersistentSettings already enter that context through their existing public owners, so their source-path equivalence is unchanged. Direct execution is separately covered by the native chronology and bounded fresh-reader test; no authority result or currentness result enters the parser cache.

## Future public-input runbook — cannot yet complete R3

1. Verify the PPC9 packet, exact prerequisite/bundle/diffs, source manifests, unchanged PPC2 proposal/approval, full validation evidence and partial disposition. Receiving source review and fresh hosted CI decide acceptance of the submitted bounded work.
2. Resolve C1 and C2 in the [reviewable chronology proposal](provider-assignment-use-chronology.md). Record any new approval separately against exact proposed bytes. Do not read this runbook as authorization to change protected O4 contracts.
3. In a new isolated disposable root, enumerate every input schema/reference, original byte digest, competent signer/delegation, generation, expiration, custody location and finite work bound before signing. Keep external account/access/base/tokenizer/tariff/cognition ports individually synthetic or unavailable. Use no installed private state or real credentials.
4. Through supported owners, create real FRESH founding, exact nine-member establishment, current actors, strict model-bound Profiles, four independent Senate findings, reconciliation/security disposition, exact approval, qualification, designation and independent appointments. Prepare exact mappings only after those acts exist.
5. Under whatever **separately approved** staged contract resolves C1, admit those complete public originals, authorize the exact finite pair/requirements/assessment scope, and retain W1/W2/W3 original five-checkpoint custody and responses. Do not substitute generic PASS rows or parsed cognition for substantive native judgments.
6. Explicitly construct one fixed NativeAssignmentEvidence shared by application and settings. Directly invoke it, then exercise both supported initial authority modes, separately signed whole-pair replacement with exact predecessor/allowed set, fresh reconstruction, each role and protected settings delivery to a counting synthetic transport. Record exact tuple/configuration/application identities.
7. Execute every open matrix row: both Seats, both real ownership orders, bounded barriers and actual lock probes; all currentness/original/index/root/bounds/nonce/generation refusals; actual rename exceptions and termination around publication, fresh reconstruction and replay. Record the earliest reached refusal and original before/after state/effect counters. Keep external invocation location and independent session/lease/custody/start/response boundaries.
8. Commit the executable candidate. Run fresh unchanged eight partitions, exact file/case aggregation and all eleven guards. Preserve every native exit, UTC interval, source digest, warning/error/skip, failed attempt and entire-worker timing. A later executable delta invalidates all prior candidate partitions. Keep the hosted 30-minute limit.
9. Publish only selected public originals and redacted synthetic observations through complete manifests; exclude private keys, credentials, fixture runtime roots, vendor/cache and unrelated environment material. Preserve exact start/tested/final identities, full-index binary diffs, sole bounded prerequisite, standalone committed report bytes and independent packet/signature verification. Stop at local commits and public review deliverables.

All five operational flags remain false. `DEFER_ENROLLMENT`; actual retry allowlist `[]`. Countdown 3–4, decrement zero; potential 2–3 after full R3 receiving acceptance is not applied.
