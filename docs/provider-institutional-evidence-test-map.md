# PPC1 changed-test map

All roots and keys are disposable synthetic test material. Provider facts remain explicit test doubles. The real `ConstitutionSource`, admission, `CurrentAuthority`, `FreshProducer`, `CommandLedger` and fixed verifier run in positive cases.

| Tests | Boundary proved |
| --- | --- |
| `NativeConstitutionEvidenceTest::testPreparedApprovalPublishesAndRevalidatesExactHolder` | Policy-effect and signed-act FRESH competence; exact artifacts; atomic generation; historical replay after revocation, current refusal, five flags false |
| `testArtifactPreparationDoesNotCreateAuthorityOrPublication` | Detached prepared originals cannot assert competence; unpublished holder cannot be current |
| `testCurrentFrameRejectsSubstitution` | Signed/admitted but unapproved exact package; changed artifact bytes; absent admission; foreign issuer; no journal mutation |
| `testEvenCompetentApprovalCannotChangeFrozenScope` | Exact authorized source still refuses wrong root, seat, instance, operator or expired interval; no publication |
| `testFreshProcessUsesCurrentAuthorityAfterConstruction` | New process reconstructs real verifier from retained originals; competing policy revocation after constructor blocks current use; read-only head unchanged; network primitives disabled |
| `testDormantCompositionUsesNativeConstitutionByDefault` | Actual founding through default composition constitution port; only base provider evidence mocked; dispatch callback throws if called |
| `AugurFreshCrashTest`, added native data sets | Real native verifier before-publication crash, after-publication crash and competing command replay; one atomic publication, no duplicate consumption |
| `InstitutionalAssignmentContractTest` | Correct Ed25519 signature cannot admit unsupported standing competence; actual admission error, unchanged journal. This is a contract incompatibility witness, not a working AssignmentEvidence implementation |
| `ProviderProductionCompositionTest` (existing, rerun) | Dormant reader behavior, coherent store/custody, safe missing sources, evidence-only recovery, current custody checks |

The synthetic fixture retains its old default verifier for historical tests. Native cases explicitly opt into actual source preparation and `NativeConstitutionEvidence`; fresh worker native mode replaces its synthetic verifier. Synthetic base/access facts remain labelled and are never default production wiring. The new fixed verifier retains no constructor-time authority snapshot and performs no journal acquisition or dispatch.

No assignment supersession success, substantive assignment-positive producer chain, or production settings resolution success is claimed: the designation contract gap remains. Historical O4/O5 tests cannot close it. Complete validation results and execution limitations are recorded in the handoff report and external packet.
