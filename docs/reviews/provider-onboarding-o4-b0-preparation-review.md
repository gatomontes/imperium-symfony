# O4-B0 preparation review

Disposition: ready for local implementation. This is the preparation author's source-grounded review, not independent runtime acceptance of O4.

Entry is merged O3-B1: `61d3f555d4bc903437687d454f05f4484ff538c4`, accepted tree `f7e10635a7d6992ea50de4a47ef2654e943e1ff7`. GitHub merge and tree were verified after marking PR #794 ready. Diagnostic PRs #795/#796 were closed without merging. The owner's instruction also selected O4 preparation.

The preparation records O3 closure and exact CI/integration provenance, updates current roadmap pointers while preserving historical evidence, and adds the O4 application contract, campaign, local prompt and isolated-worktree instructions. Scope is complete atomic target model settings, persistent resolution, explicit operator change/revalidation, migration and crash/replay proof. Personnel appointments, invocation authority, O5 CLI and live commissioning remain separate.

Source inspection confirmed the existing pure `AssignmentSelector`, original-backed `AssessmentGroups`, shared `FormationJournal` owner, and current application refusal seams in `CommandLedger`, `CurrentAuthority` and `StateValidation`. Existing formation personnel/session consumers were inspected to avoid conflating settings with appointments. The new contract requires an explicit production settings-consumer boundary; it does not claim that boundary exists already.

Validation: all 86 static specification checks passed. All 1,853 tracked PHP files match the integrated O3 baseline byte-for-byte. New O4 relative links resolve and `git diff --check` passes. Changes are documentation/contract/text/JSON only; original selection/continuation contracts, approved policy bytes, tests/fixtures, workflow, service configuration and locked dependencies are unchanged. This review file adds no executable content.

The O3 source passed the fresh full hosted run [34703567912](https://github.com/gatomontes/imperium-symfony/actions/runs/34703567912): 3,664 tests / 57,357 assertions / four skips. That is inherited implementation evidence for the unchanged PHP source, not a new O4 runtime or full-suite test. PHP is unavailable in the preparation environment, so no local PHP execution is claimed. Any automatically triggered preparation CI is separate evidence.

Exit of preparation: publish the reviewed documents and deliver the local implementation prompt. O4 remains unimplemented until the local run and subsequent source/full-CI review. All five operational flags are false; actual retries remain empty.
