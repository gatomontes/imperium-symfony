# P0–P3 test and proof map

The prior A0–A3 test/report remains historical. Its incomplete delivery did not isolate the two power predicates. `NativeAuthorityProtocolTest::testIndependentCompleteLegacyAdmissionControlsForEachPowerAndRepresentation` now uses a complete delivery with both missing IDs, establishes actual legacy admission/custody success in a fresh root, then independently removes each power and substitutes an unsigned request in separate fresh roots. The same complete delivery also succeeds through the new enrolled native command/consumer route. No old assertion or frozen discovery predicate is weakened.

The positive fixture generates Ed25519 keys in memory, separately enrolls their public fingerprint through the actual compiled command/DI, adopts exact synthetic Recruiter/Garrison evidence, signs and applies the two-scope revision, calls the real admission consumer and reads native inventory. Real private state/keys and providers are not inputs. Sentinels remain in synthetic private bootstrap bytes and never enter public proof output or native frames.

Adverse cases cover wrong domain/role/key/fingerprint/effect/instance/object digest; expired/future/self-enrolled signatures; wrong head/roster/prior revision, actor/generation/instance, overbroad scopes, modified original, expired request and revoked nonce; duplicate adoption, explicit succession/retirement, malformed history, changed replay, absent trust/currentness, and unsupported legacy paths. These use genuine signed adversarial inputs where appropriate, not a boolean verifier.

Sibling processes prove competing revision and revocation ordering, retirement blocking a waiting admission, enrollment waiting for an actual legacy StateStore writer, and permanent post-enrollment fencing. For both authority revision and admission, they interrupt before publication, after durable pending bytes, and after rename. Before publication there is no committed effect; pending bytes remain explicitly fenced; completed effects recover once after expiry with the same evidence. No test claims OS isolation, directory-fsync or power-loss durability.

The native inventory adds one explicitly classified mechanical candidate (`NativeProtocol.php`) to the existing additive inventory. Original frozen snapshots and tripwire predicates remain unchanged. Source wrappers retain old method bodies behind an explicit enrollment fence; the exact path/method map is supplied separately. Final regression gates include prior A0 tests, frozen tripwires and all PHP tests, plus the unchanged 16 Python collector tests. Exact failed/development/final runs, counts and identities belong in the packet's validation records; the prior 2,810 / 53,495 and four warnings are preserved history, not a target.
