# A0: native Recruiter and Garrison interface decision

Entry: `408597e3d4828e8cdb1f623d0eb0ec8215972830`, tree `7db7d78dc6201667e094e4e0f02a5f0ef3f3c01d`. The installed root is outside execution scope. All backing state used in proof is synthetic.

`Bootstrap/MasterMason::installOrdinaryRecruiter` records a consumed pinned commission, retired T03 predecessor, generation-2 ordinary Recruiter and qualification digest in T04. `transition` appends an event and increments the state generation. Its enclosing retired sequence uses `StateStore::locked`. The sequence is now barred by `B242`; it is evidence of the historical producer format, not an available succession ceremony. `V0ActivationService` writes a different operator-root state under the same lock and refuses incompatible existing state. The two Conscription readers accept a last successful T04 from CURIA_READY without authenticated public currentness. Do not turn that reader convention into a new authoritative resolver.

The new private-source interface will take the existing bootstrap lock, read only the bounded bootstrap state, and export an allowlisted historical T03/T04 projection with a distinct projection digest, observed state generation and local observation time. The lock may create `var/imperium/bootstrap.lock` and its directory. It serializes cooperating StateStore writers; StateStore::write itself does not enforce lock ownership, and an administrative replacement is outside that guarantee. The test-owned historical smoke driver writes synthetic state; it is not a live currentness authority. No raw private-state hash or whole-state output will be published. T04 qualification and public source bindings are checked separately. Missing, failed, duplicate or contradictory evidence refuses; later actor retirement/supersession cannot be inferred away. No currentness claim survives beyond a locked observation by convention.

Neither that private state nor supplied Git identity is an independently authenticated statement. No effect-scoped public Recruiter provenance issuer, authoritative native revocation/supersession source or trusted currentness resolver is present in the traced contract. Implement public structural inspection and an explicit refusing current-authority seam; never promote a self-sealed projection or incoming key to trust.

`ConstableSeatBindingService::bind` produces an immutable binding. Its newer admission/custody defaults cannot modify the retained old record; replay mismatch is GA55 and a second occupant is GA56. The construction/provisioning path concerns a vacant Seat, not revision of an occupied Seat. `OperatorRootUpgradePlanningService` requires an operationalization seal and emits a nonexecuting plan; it is not an issuer for a native Garrison authority extension. FormationSignatures' enrolled CITADEL_MISSION_FORMATION trust and personnel delegation do not supply an institutional authority-revision competence. No borrowed ProtectedMission or planning trust is admissible.

Implement an unsigned, deterministic request bound to the original native occupancy digest, instance, actor/generation, explicit prior revision (null means unknown), exact two powers/scopes, effective/expiry terms and caller-owned replay nonce. Preserve other observed powers without inferring missing flags. Inspection checks the supplied original and exact request. A dormant revision verifier/resolver must refuse because the legitimate issuer, current revision/revocation register and common authority-consumption/effect transaction are missing. There will be no mutation command, synthetic trusted production issuer, revision store or positive admission override. Actual admission consumers will be exercised with old native fixture occupancy to prove that preparation and forged revisions cannot grant admission/custody; inventory retains its independent existing scope. This is the campaign's expressly allowed typed-preparation/refusal fallback.

A future positive revision requires an issuer contract for these exact effects and an enrolled trust path outside incoming requests; current occupancy/prior-revision and issuer revocation checks under the same lock as one-time consumption and immutable effect; replay recovery preserving authentic completed effects; and closure across all admission/custody/inventory readers. Occupancy generation must remain unchanged without actual succession. These prerequisites are not implemented as pretend positive fixtures. FormationInstitution, FormationPersonnel and historical FormationPublicationEvidence remain unchanged, preserving CF01/CF02/IR01 and all current formation blockers.
