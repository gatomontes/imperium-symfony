# PPC4 — close the generic institutional storage bypass

Status: **PREPARED; SUCCESSOR_CLAUSES_A_TO_F_AWAIT_OWNER_APPROVAL**. Target: complete R2 native tenure synchronization after the accepted PPC3 component. Countdown **5–7 remaining**, decrement **0**. [Countdown](provider-first-interview-countdown.md), [proposal](provider-storage-successor-contract-v1.md), [approval record](handoffs/provider-storage-successor-approval.json), [local setup/prompt](handoffs/provider-storage-successor-ready.md).

Preparation baseline: main `96d05d839320909d140d50337ccd16fe68dc1604`, tree `707b94f58beb80d5180790a5aa58843f99fe55c0`. Start from the published `codex/provider-storage-successor-preparation` branch; record its actual commit/tree. Preserve existing worktrees and the installed application.

## One bounded implementation batch

The [PPC3 review](reviews/provider-native-tenure-review.md) accepted typed native ownership and proved the remaining generic bypass. Finish that outcome by enforcing a reserved-target boundary in the existing write entries, passing real live owners in legitimate typed callers, and adding the narrowly reviewed source successor mechanism. Do not rerun a broad missing-model-source discovery campaign or split preparation, documentation and test results into new countdown credits.

1. Read A–F and verify an actual owner approval naming this exact proposal/hash before changing runtime or pin enforcement. Record the original conversational reference without inventing a timestamp, signature or approval. Once that approval exists, continue all authorized implementation without repeated blanket permission requests.
2. Inventory the existing generic callers and reachable institutional destinations using the [PPC3 writer map](provider-native-tenure-writers.md) as the lower bound. Trace both path grammars, aliases, callbacks, active-set invalidation and actual target paths. Produce a writer/target/owner migration table and classify exclusions by storage semantics.
3. Implement A–D at the existing entry points, migrate legitimate reserved typed callers, preserve original ledgers and AtomicTransition, and introduce the explicit bounded predecessor-to-candidate source record. Do not calculate away a pin failure. Do not introduce a callable old-runtime alternative that bypasses the new boundary.
4. Execute E's meaningful separate-process, alias, compatibility and successor-tamper proof. Exercise both Courtthane and Locksmith current consumers and all reserved classes. Retain the authentic native producer path and label synthetic model authorization separately.
5. Commit executable changes, run the complete unchanged gate, and package exact tested/final source and public evidence. Documentation-only follow-up after testing must have its exact diff retained. Stop at local commits for receiving review and fresh hosted CI.

## Required public deliverables

- `docs/handoffs/provider-storage-successor-report.md`: disposition, approval reference/hash, exact source identities, implemented versus unresolved outcomes, validation/platform facts and countdown.
- Writer/target/alias/lock-order inventory, changed-test/requirement map and public-input reproduction runbook; the successor source record and preserved non-executable predecessor source.
- `imperium-ppc4-public-review.zip` plus separate `.zip.sha256`: committed source ZIP, public synthetic evidence ZIP, complete path/mode/byte/SHA-256 manifests, identities, exact base-to-tested/tested-to-final/complete diffs and bounded Git bundle with the exact published preparation prerequisite.
- Independent verification script and evidence for original ledger/pin preservation, approved proposal identity, no executable post-test delta, full-suite exact coverage, native exits, timing, warnings, skips and every diagnostic failure. Exclude private keys, fixture runtime roots, installed files, vendor/cache and untracked environment material. Retain only sanitized public originals needed to reproduce the claims.

Use `NATIVE_TENURE_SYNCHRONIZATION_IMPLEMENTED_OFFLINE` only if PPC3 plus PPC4 cover the entire supported set and all interfering generic paths. Otherwise `PARTIAL_NATIVE_TENURE_SYNCHRONIZATION` with exact open paths. Neither claims R1 correspondence, current assignment/use, combined FRESH establishment or live readiness. Keep canonical countdown at **5–7** on submission; only receiving acceptance of full R2 can apply **4–6**.

## Validation and unchanged boundaries

The authoritative gate remains `.github/workflows/phpunit.yml`, `tools/ci/phpunit_partitions.py` and all 11 existing Python guards. Do not edit worker selection, coverage enumeration, source checks, limits or guards. A Windows launcher adaptation may be external and documented; symlink privilege failure is retained as an error, not called a pass or skip. PPC3 CI is historical compatibility evidence, not PPC4 validation.

All five operational flags remain false. `DEFER_ENROLLMENT`, the empty actual retry allowlist and the no-live boundary remain. No extra agents are requested. Ordinary separate-process tests are required. A new independently conflicting source pin must be reported precisely; it is not permission to weaken the acceptance gate.
