# PPC10 source incompatibility: v5 shared exposure dispatch

The exact C1/C2 approval remains valid. This is a request to review one additional existing source seam, not to approve those clauses again. No change to this seam has been made.

At preparation `bc91c7d634d0c7824f8f4621e96bb96995462f0e`, `SharedExposure::formation()` returns before validation/accounting unless the onboarding schema is v2, v3 or v4. Both `FormationCognition::call()` (reservation) and `FormationClaimCustodyBroker::validate()` (protected custody) call it. They continue to session-local accounting after that call. Their model-settings verification is conditional on `model_settings` being present; routing the new settings consumer through `FormationSettingsBinding` therefore cannot cover every existing Formation source.

The approved inventory permits narrow changes to `BudgetAssociation`, `LedgerState`, `StateValidation`, settings adapters and other named onboarding seams. None is invoked before the unsupported-version return in `SharedExposure::formation()`. A change confined to an optional transport wrapper cannot restore the shared gate for an existing source that does not use that wrapper. Keeping the ledger labelled v4 would contradict the explicitly approved v5 migration. Returning projected state to the protected caller would conceal the actual state version. Disabling all legacy Formation activity would change an existing contract, and would not implement the required shared accounting route.

The exact hashes and matched lines are in `provider-staged-assignment-exposure-source-gap.json`. `StagedExposureBoundaryTest` calls the real pure gate with actual v2 ledger state (which refuses an unmapped session), then a **local, unpersisted copy with only the schema marker changed** (which returns). It checks that the actual journal remains unchanged. This is a version-dispatch diagnostic, not accepted v5 state, not a migration, not a supported-owner v5 race and not evidence of native application or protected use. Existing `ProviderOnboardingSharedExposureTest` independently exercises real reservation/custody and source-fence behavior on the accepted route.

Proposed additional permission: edit only the version-dispatch list in `src/Imperium/Runtime/Citadel/Formation/SharedExposure.php` to include v5, after a closed v5 structural validator and integrated source/claim history exist. Keep `SharedExposure::check`, all meters, membership, same-owner exclusion, reservations, settlement and limits unchanged. Prove both existing callers on supported v5 owners, with settings and without settings, each race order and maximum+1. Do not treat the diagnostic as that proof.

Dependent v5 activation, staged reservations/application/use and their race/crash campaign remain unimplemented. Independent C2 originals and additive pure C1 decoders continue. The executable candidate cannot be represented as full PPC10 or R3.

Estimate revision: the original one-batch R3 implementation/receiving estimate is split into this component/source review and at least one subsequent integrated implementation/receiving batch. Working remaining range is **4–5**, revised from **3–4**, with **zero decrement** and no new acceptance. R1/R2/R4 remain accepted; R3 open; R5 open; R6 separately authorized and deferred. All five operational flags remain false, `DEFER_ENROLLMENT`, retry allowlist `[]`.
