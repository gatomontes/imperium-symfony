# Authority interfaces: source and consumer map

| Source/consumer | Traced contract and campaign result |
| --- | --- |
| `Bootstrap/MasterMason::installOrdinaryRecruiter`, `transition`, `executeRetiredSelfConstructingPersonnelSequence` | Legacy T03/T04 format, consumed commission and qualification, event/state generation, existing StateStore lock; entry retired by B242. No replay/activation. |
| `Bootstrap/StateStore::locked/read/write`, `Bootstrap/V0ActivationService::activate` | Bootstrap lock and atomic temporary rename; V0 uses different schema and rejects incompatible state. The new exporter reads bounded bytes while holding the same cooperating-writer lock. StateStore::write does not enforce ownership. |
| `Laboratorium/ProfileElaborationSmokeService` | Retired production smoke entry; test-owned historical driver includes synthetic StateStore writes. Not a legitimate currentness issuer. |
| `Conscription/ConstableConscriptionService::ordinaryRecruiter`, `GuildhallConscriptionService::ordinaryRecruiter` | Existing backward scan for successful T04 and generation 2. Unchanged; these embedded actor assertions are not independent public currentness/provenance. Other Conscription/Curia StateStore users are readers of this same private source, not revision authorities. |
| New `Citadel/Authority/RecruiterEvidence` | Allowlisted projection, versioned structural inspection, locked source comparison. Explicit provenance/currentness refusal. No formation delegation, public authority resolver or write to bootstrap state. |
| `Garrison/ConstableProvisioningService`, `ConstableConstructionCommissionService`, `ConstableSeatBindingService` | Vacancy/provisioning/spawning/binding pipeline; not an occupied-Seat authority extension. New defaults cannot rewrite old occupancy. Existing GA55/GA56 conflicts retained. |
| `Bootstrap/OperatorRootUpgradePlanningService` and operationalization seal | Nonexecuting plan; no native revision issuer or migration authority. Not reused. |
| New `Citadel/Authority/GarrisonAuthorityRequest` | Exact unsigned two-power request; supplied-original inspection; permanently refusing revision verifier/admission resolver. No effective revision representation or authoritative issuer substitution. |
| `Garrison/SubordinatePersonaCanonicalAdmissionService::admit` | Reads named raw occupancy and requires both admission/custody powers; fixture proves old native occupancy still refuses GA87 before custody/disposition writes. No second occupancy or revision overlay introduced. |
| `Garrison/SubordinatePersonaAdmissionIntakeService::currentConstable` | Requires unique raw native occupancy and both powers; remains GA82/GA83 refusing for missing powers. |
| `Garrison/AdversarialReviewerBootstrapSeedAdmissionIntakeService::currentConstable` | Same native uniqueness/power boundary; GA74/GA75 retained. No bootstrap exception extended. |
| `Garrison/GarrisonInventoryInquiryService::currentConstable`, `GarrisonInventoryResponseService::currentConstable/custodyRecords` | Existing independent inventory scope/uniqueness checks. Fixture proves original inventory power remains usable while admission remains blocked. Original attribution stays bound to original digest. |
| Reservation, Profile-handoff and other direct Garrison occupancy readers | No new supported representation reaches them; their original reads and powers remain unchanged. A positive revision requires complete reader migration; no supported revision can now bypass stale-reader checks. |
| `FormationInstitution`, `FormationPersonnel`, `FormationSignatures`, `FormationPublicationEvidence`, `FormationPreparation` | Existing operator-root/current-actor and effect-scoped formation trust, journal and retained-publication validation. No native revision competence and no changes. Both current and CF02 historical witness formats retained. |
| `Persistence/AtomicTransition`, immutable stores and formation journal | Useful mechanical locks/digests, not an issuer or authority grant. No second unsynchronized authority store is added. |
| New `Command/CitadelAuthorityCommand` | Actual Symfony autowiring/command facade for six modes; public-only modes do not read runtime state. Two separately authorized private-source modes are explicit; no root/trust/provider override. |
| `tools/citadel_native_lineage.py` and its tests | Byte-for-byte unchanged public-only collection scope. Existing 16 Python tests rerun for compatibility. |

The bounded source trace identifies no effect-scoped native Garrison revision issuer/current register and no authenticated native Recruiter currentness/revocation producer. These are explicit production prerequisites. Synthetic backing state tests do not certify real authority or replace native lineage with an operator-root fixture.
