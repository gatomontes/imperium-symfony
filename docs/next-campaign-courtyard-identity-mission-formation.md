# Courtyard Identity and Mission Formation Boundary — CY0–CY3

Status: **COURTYARD_IDENTITY_MISSION_FORMATION_SELECTED**. Preparation only in this
merge; runtime implementation and evidence production are for the local run.
The [owner decision](courtyard-identity-decisions.md) defines the naming and mandate.
Run all four stages locally from main containing this selection, in a new isolated
worktree `E:\htdocs\imperium-courtyard-identity`, branch
`codex/courtyard-identity-mission-formation`. Do not switch or update the installed
`E:\htdocs\imperium` checkout. Record actual entry commit/tree and instructions.

## Baseline and honest status

Source baseline: PR #778 merge `88cee012d5fae0dc8e18f25eebed167912e06230`, tree
`3826740810f42c90e6d8b6e00686947003ab62c4`. FC0–FC3 is integrated source with local
disposition **FORMATION_CLAIM_CUSTODY_IMPLEMENTED_OFFLINE_LIVE_TRANSPORT_BLOCKED**.
Its report still requires independent review; Git merge is not evidence acceptance.
This campaign may proceed with offline identity work on that source, retaining
the review qualification. It neither restarts FC0–FC3 nor certifies it accepted.
CP0–CP3 and prior scoped acceptances remain as recorded. Preserve all raw evidence.

The runtime currently embeds the interview allocation in `appointCastellan`,
`currentCastellan`, `citadel.castellan`, `APPOINT_CASTELLAN`, signed Profile targets,
command operations and formation state. `FormationJournal` also uses a Citadel
storage path, schema and lock name. These are different kinds of identity: a
global text replacement would corrupt semantics or split custody.

## Required reading

Read applicable AGENTS.md, this campaign and
[the handoff](handoffs/courtyard-identity-mission-formation-ready.md), then:

- `docs/courtyard-identity-decisions.md`, `docs/officer-taxonomy.md`,
  `docs/record-taxonomy.md`, `imperium-doctrine.md`, `offices/curia/doctrine.md`.
- `docs/delegate-mission-flow.md`, `docs/next-lifecycle-delegate-mission-route.md`,
  `docs/citadel-mission-formation-decisions.md`,
  `docs/citadel-mission-formation-implementation.md`.
- `contracts/citadel-mission-intake.md`, `contracts/citadel-formation-runtime.md`,
  `contracts/citadel-formation-claim-custody.md`, `contracts/profile-artifact.md`.
- `docs/citadel-formation-claim-custody-report.md`,
  `docs/citadel-formation-correction-acceptance.md`,
  `docs/citadel-ir01-correction-report.md`,
  `docs/citadel-native-authority-protocol-acceptance.md`,
  `docs/citadel-commissioning-preparation-acceptance.md`,
  `docs/citadel-commissioning-owner-decisions.md`,
  `docs/citadel-commissioning-compatibility.md`, `docs/citadel-readiness-matrix.md`.
- `src/Command/Citadel*Command.php`, `config/services.yaml`, formation classes
  under `src/Imperium/Runtime/Citadel/Formation`, and their actual consumers in
  Clavium, Curia, MasterMason, Conscription and provider resource decisions.
  Follow the source dependencies actually used; do not rename all of Runtime/Citadel.
- Existing formation/custody/native authority tests and fixtures, the two formation
  proof scripts, `tools/package-citadel-formation.py`, and
  `docs/citadel-mission-formation-runtime-inventory-v1.tsv`. Trace the frozen
  coverage scanners before moving any inventoried source.

Repository source is sufficient input. Installed runtime, private journals,
credential stores and genuine signing material are not reading prerequisites.

## Steps

| Step | Required result |
| --- | --- |
| CY0 | Source-bound identity inventory and compatibility contract; map function, jurisdiction, Seat, signed/persisted identities and historical evidence |
| CY1 | Courtyard reception/formation interfaces and Courtthane qualification/appointment/currentness path through actual production consumers |
| CY2 | Offline end-to-end operation and compatibility proof; original lineage, budget, locks, recovery and refusal boundaries preserved |
| CY3 | Final committed focused/full gates, updated current docs, residual-name audit and independently verifiable review packet |

Proceed through CY0–CY3 without stopping after inventory if implementation is
possible. Resolve routine naming and compatibility choices within the decision.
If an indispensable authority-model decision remains, complete independent useful
work and report the exact blocked transition with source evidence; do not invent
authority or an automatic migration to finish.

### CY0 — classify before changing

Produce `docs/courtyard-identity-inventory.tsv` with path/symbol, semantic category,
producer, consumer, proposed action, compatibility rationale and affected proof.
Categories must distinguish enclosing Citadel, reception/formation function,
interview Seat, other Citadel runtime capabilities, historical/sealed evidence,
persisted/signed/wire identity, and proposed future oversight. Inventory all relevant
spellings including `Citadel`, `citadel`, `Castellan`, `castellan` and `CASTELLAN`.
Account for CLI, DI, source namespaces, Profiles, effects, frames, claims, receipts,
schema validators, error codes, fixtures, source scanners, documentation and tools.

Define `courtyard.courtthane` as the new reception Seat. Map the complete original
qualification-to-cognition chain; a display label is insufficient. Decide explicitly
which source names move, which stable protocol names remain and why. Shared native
institutional services and unrelated Citadel cognition are not automatically Courtyard.
Retain the existing enclosing Citadel identity rather than deriving a new root.

Write `contracts/courtyard-identity-compatibility.md` before changing consumers.
For each old surface state whether it is a retained format, exact read/recovery
path, CLI alias, or refused fresh mutation. Freeze representative pre-change public
synthetic fixtures with byte digests, so compatibility proof does not just regenerate
old evidence with new code. Use no real evidence or production signing material.

### CY1 — implement the function and the Legate

Make Courtyard the canonical public reception and mission-formation vocabulary.
Implement Courtthane's exact Seat, Profile target, appointment operation/effect and
currentness consumption with the existing personnel/institutional chain. Preserve
Legate classification and the Officer artifact substrate. Do not merely rename the
screen while new grants still rely on an old Castellan Seat. New Courtthane evidence
must be independently valid for that target; no string substitution in signed bytes.

Route canonical Courtyard commands through actual services and DI. If old CLI
spellings are retained, document them and route them to the same validation/storage
boundary. A command alias must not reinterpret an old signed appointment effect.
Update public preparation output and future command examples only where implemented.
Retain established error codes where compatibility requires and document any new ones.

Keep one coordination registry and aggregate custody domain. Prefer retaining legacy
storage/wire names when renaming adds no semantic value. A required schema evolution
must be versioned and strict; no permissive parser or dual writable store. Do not move
installed files, reset journals, regenerate old IDs or create a new budget/replay domain.
Source moves must update explicit successor inventories through their approved extension
path without changing frozen snapshots, disabling scanners or broadening allowlists.

Retain Seneschal's child-Curia mandate, Chamberlain and Isolde identity separation,
Locksmith custody, genuine institutional witness requirements, and MasterMason's
mechanical constitution boundary. Introduce no Castellan oversight implementation,
bootstrap officer, personnel-selection policy or operator power delegation.

### CY2 — prove identity and unchanged boundaries together

Exercise the actual command/DI and service route using generated roots, ephemeral
synthetic institutional/owner authority and recording offline transport. Cover intake,
semantic-overlap input, bounded interview, understanding closure, exact drafting
approval, numbered proposal, exact mission approval, legitimate constitution/handoff,
receiving Seneschal assessment and non-executing Step 1 validation. No Curia may be
created at reception and no proposal generated within the understanding response.

Preserve CF01 terminal refusal, CF02 retained child-publication recognition, IR01
interview closure and NA-IR01/02. Preserve FC one-use custody, exact prepared operation,
currentness, budget reservation/settlement and unknown-outcome exposure. Default
`UnavailableFormationTransport` and `UnavailableFormationWireAdapter` must still
refuse through actual DI; preparation must have zero credential/provider activity.

Prove the frozen old fixtures remain byte-identical and attributable through the
implemented read/recovery paths. Recognition is not renewed authority. Old Castellan
approvals cannot appoint Courtthane; Courtthane cannot acquire future oversight;
Seneschal cannot inherit reception grants. Wrong Seat/scope/holder/effect/version,
replaced/expired/revoked appointment and mixed old/new evidence fail at production
consumers. Any unsupported old fresh use must refuse explicitly before effects.

Where command aliases exist, use actual concurrent processes to show they share the
same lock, generation, reservation and one-use fence. Show combined exposure cannot
be doubled by alternating names. Exercise interrupted claim custody and child
publication across compatibility entrypoints without creating a second effect or
Curia. Reuse existing process/crash fixtures and assertions; add only missing cases.
Explain each injection boundary and observable effect count.

### CY3 — finish, verify and return for review

Update current doctrine/office documentation, contract map, README, handoff index,
steps, flow, current runbooks and command examples consistently. Preserve historical
reports, decisions, acceptances, sealed bootstrap and original review packets. Mark
supersession with new current pointers; do not bulk edit evidence. Return a residual
Citadel/Castellan reference audit with a reason for each retained category and no
unexplained active reception/appointment mismatch.

Commit executable source before the final focused formation/custody/native/coverage
gates, then run `php vendor/bin/phpunit tests` on the same committed source. Use
available locked offline dependencies, verify autoload resolves this worktree, and
keep the safe environment/generated-root conventions from the FC report. Do not
install/update dependencies or borrow installed private configuration. Run affected
Python checks only if those helpers change. If dependencies are unavailable offline,
report the blocker; do not fabricate results.

Keep exact commands, UTC times, native exit codes, PHP/dependency identity, warnings,
test counts, raw failures and source status before/after. Compare baseline warnings
honestly; previous FC counts are historical, not fresh validation. Any executable
change after tests requires the affected gates again; identify later Markdown-only
edits with exact post-test diff. Use public synthetic evidence only.

Return `docs/courtyard-identity-report.md`, the inventory/compatibility contract,
changed-test map, guarantee/limitation table and exact future prerequisites. The
external review packet must contain complete tested/final source ZIPs with
path/mode/blob/byte-length/SHA-256 manifests, bounded Git bundle with explicit entry
prerequisite, final/post-test diffs, public proof, raw gate logs, all source identities,
payload SHA manifest and packet verification script/instructions. Provide the outer
ZIP SHA-256 separately. Exclude vendor, installed/private evidence, private keys and
credentials. Package under `E:\htdocs\courtyard-identity-review-<UTC-date>`.

Success disposition: **COURTYARD_IDENTITY_IMPLEMENTED_OFFLINE_REVIEW_PENDING**,
only when the complete route and compatibility contract are proved. Stop at local
commits for independent review. No local push, PR or merge is part of this runner.

## Preserved limits and next gate

No installed application/private-state access, actual enrollment or migration,
live signing/appointments, provider calls, mission execution, process/service changes,
dependency updates, deployment, onboarding or Oracle/model selection. Ephemeral
fixture authority and offline recording infrastructure are authorized for proof.
`DEFER_ENROLLMENT` and B1's existing remote cost/time contract remain unchanged.
`deployment_approved`, `enrollment_authorized`, `live_ready`, `activation` and
`execution_authority` remain false.

Next gate: independent review of this exact identity implementation and preservation
proof, retaining FC0–FC3's unresolved acceptance status. Genuine formation trust,
nine institutional witnesses/competence, qualified Courtthane/Locksmith appointments,
actual provider/B1 policy, public preflight and separate commissioning authorization
remain prerequisites. Provider onboarding is separate future work. No successful
rename establishes readiness or revives the retired Castellan bootstrap role.

*Ne passum quidem regrediemur.*
