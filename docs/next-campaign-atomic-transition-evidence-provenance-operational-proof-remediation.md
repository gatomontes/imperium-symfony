# Next campaign: Atomic Transition Evidence Provenance and Operational Proof Remediation

## Selection

`ATOMIC_TRANSITION_EVIDENCE_PROVENANCE_OPERATIONAL_PROOF_REMEDIATION_SELECTED`

The prior corrected closure is requalified at
`CAMPAIGN_CLOSURE_REQUALIFIED_WITH_MATERIAL_EVIDENCE_PROVENANCE_DEFECT` after
terminal Blackquill review established that self-hashed theoretical evidence
can be constructed without authenticated execution provenance.

## Objective

Make counterfeit evidence structurally inadmissible, bind every accepted result
to a trusted executor and exact code/build/mission root, derive the real
dependency-capability graph, scan the complete evidence chain for secrets and
process-local capabilities, and prove the corridor through one explicitly
authorized disposable real mission without provider invocation or external
effect.

## Campaign sequence

1. **Preparation Batch 0 — closure requalification and counterfeitability inventory.** Inventory caller-supplied cases, results, execution claims, seals, capability declarations, secret-proof scope and legacy-audit reachability. Define the trusted proof boundary and disposable mission acceptance matrix.
2. **Batch 1 — execution provenance contracts.** Bind mission/experiment identity, source commit, build, executor, fixtures, recovery plan, case-set root, lineage, custody, freshness, limitations and sanitized evidence-package identity.
3. **Batch 2 — trusted case execution corridor.** Validate and execute cases internally; accept no caller result or execution/finding/match boolean; bind produced results to Batch 1 provenance.
4. **Batch 3 — derived capability graph.** Traverse and classify the actual executor dependency graph; bind it to the build; reject unknown, substituted, mutable or effect-capable dependencies.
5. **Batch 4 — complete-chain secret and capability exclusion.** Inspect provenance, fixtures, plans, mutations, cases, expectations, results, dependency graph, aggregate, exceptions and closure through typed structural allowlists plus adversarial values and encodings.
6. **Batch 5 — disposable real-mission execution.** Run one bounded mission through the actual internal corridor under a disposable root with interruption, replay, contention, partial-write, tamper, secret injection and executor-substitution cases. No live provider, credential or external effect is authorized.
7. **Batch 6 — independent reconstruction and legacy-path disposition.** Reconstruct without producer conclusions and prove the historical defective audit repaired, disabled or rejected by every closure consumer.
8. **Batch 7 — terminal adversarial audit and closure.** Permit corrected closure only when authenticated operational evidence survives independent reconstruction and the legacy path cannot authorize closure.

## Terminal acceptance

Only a successful Batch 7 may produce
`CAMPAIGN_CLOSURE_ACCEPTED_AFTER_AUTHENTICATED_OPERATIONAL_EVIDENCE_PROOF`.

## Binding exclusions

Provider binding remains `BOUND_INACTIVE`. Required v3 execution admission
remains `NOT_IMPLEMENTED`. `UNKNOWN_REPLAY_PROHIBITED` remains binding. No live
credential or capability, provider invocation, external I/O, live autonomy,
raw private evidence, self-authorization, production adoption or live command
migration is authorized.

A disposable real mission is not authorized by campaign selection or
Preparation Batch 0. It requires the exact later batch boundary and explicit
authorization.
