# PPC10 public-input reproduction and proof map

Use a disposable checkout of the tested commit, PHP 8.4 with sodium, locked Composer dependencies (`composer install --no-scripts --no-interaction --no-progress --prefer-dist`) and Python 3. No installed private root is an input. No provider/account credentials are required. All signing keypairs are generated in memory by disposable test actors and are not exported.

Run the three additive test files (`StagedAssignmentContractTest`, `StagedExposureBoundaryTest`, `StagedProfileFitnessTest`) and the affected existing shared-exposure suite. `PPC10_PUBLIC_EVIDENCE` optionally names an empty output directory for public observations. Native fitness creates each root through actual FRESH founding, completed R4, actual native model Profile chain, current designation and separate appointment. It never deserializes accepted fixture state. The use of historical fixture constructors calls their supported owners; populated roots are not copied.

The native test produces seven independently signed C2 judgments for each Seat and eight synthetic practice returns. It records competent separately signed wrong-Seat and UNKNOWN-exercise refusals, checks unchanged heads, then retains valid controls. Other reached checks cover wrong role, signature corruption, missing coverage, fixed-store identity, expired owner frame, exact historical replay and terminal withdrawal. Direct C2 verification is **not** assignment/use. Partial native tests do not satisfy the entire C2 adversarial matrix.

The pure act test authenticates a deliberately non-admissible probe object only to isolate the act boundary. It proves new/old domain separation, valid and corrupted signatures, and v1 admission refusal. It does not supply an accepted scope. The exposure test changes only an unpersisted local value to examine the actual pure version gate; no accepted v5 state is installed.

## Required matrix disposition

| Plan row | Evidence in this candidate | Still required |
|---|---|---|
| Original authority | Staged-domain authentication and v1 confusion refusals | Actual scoped admission, all alternate competent originals, revocation/expiry matrix |
| Chronology | Authentic founding/R4/native candidates precede C2 originals | Mapping admission, staged assessments, application/use |
| Migration | Exact source incompatibility and unchanged-journal gate diagnostic | Entire atomic v4â†’v5 producer/history/index/replay proof |
| Fitness | Both Seats, seven signed originals, eight synthetic duties, direct current verifier and selected refusals | Full corruption/expiry/tenure/saturation/history and integrated whole-pair proof |
| New competence limits | Separate exact Formation delegation and withdrawal | Full wildcard/succession/wrong-purpose matrix and races |
| Assessment | Scope commission shape only | All original W1/W2/W3 frozen input/custody/settlement/selection |
| Initial application | Terms/receipt syntax only | Actual A/B applications and all independent negatives |
| Replacement | No new replacement evidence | Both Seats, actual successor originals and whole-pair atomic publication |
| Actual use | None | Fresh-process pair reconstruction and protected custody/sink |
| Races | None for C1/C2 | Both owner orderings for every listed operation |
| Interruption | Exact C2 replay/withdrawal assertions only | All barrier/crash/restart/publication evidence |
| Bounds/corruption | Selected closed-schema/text/byte checks | All exact maxima/max+1, parser/source/history and cumulative exposure |
| Compatibility | Existing sources unchanged; complete gate attempted separately | Fresh complete hosted CI and receiving review |

## Validation and receiving

Use the unchanged `tools/ci/phpunit_partitions.py` eight-partition enumeration and `python -m unittest discover -s tools/ci -p 'test_*.py'`. On Windows only the command launcher may prepend `php` when executing the extensionless `vendor/bin/phpunit` script. Keep exact selected files/cases, source digest before/after, JUnit, native exits, UTC intervals, stderr and all guard tracebacks. Windows symlink privilege failures are missing evidence; receiving Linux must run them. No hosted acceptance is implied.

The packet verifier checks source/evidence membership, file modes/bytes/SHA-256, committed Git blobs and trees, sole preparation bundle prerequisite and three exact binary/full-index diffs. It does not claim semantic truth, current authority or R3 completion. The packet includes unsuccessful diagnostics. Do not import any exported public observation as accepted runtime state.

## Packet verification

Extract `verify-public-review.py` from the review ZIP and run:

```text
python verify-public-review.py imperium-ppc10-public-review.zip PATH_TO_REPOSITORY_WITH_PREPARATION
```

The verifier requires Git, Python 3 and Python's `cryptography` package for Ed25519 verification. The repository must contain the published preparation commit `bc91c7d634d0c7824f8f4621e96bb96995462f0e`. It fetches that prerequisite into a temporary repository and verifies the bounded bundle without changing the supplied repository. Compare the separate SHA-256 file before inspecting the packet.

In addition to source/manifests/bundle checks, it independently reconstructs exactly-once case/file coverage from the exported complete enumerations and JUnit when the local aggregate claims success. It verifies retained fitness signatures, alternate negative signatures and intentionally invalid controls, but does not rerun native owners or establish semantic truth. Its packet-integrity PASS can coexist with a guard error, local timing overruns and incomplete R3; inspect those distinct fields and the report.
