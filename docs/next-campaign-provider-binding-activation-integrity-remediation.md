# Next campaign: Provider Binding Activation Integrity Remediation

## Status

`CAMPAIGN_SELECTED_PREPARATION_BATCH_0_ONLY`

The Provider Binding Activation and Capability Custody campaign terminated truthfully at
`REFUSED_CROSS_PROCESS_CUSTODY_UNPROVABLE`. Blackquill accepted that refusal and identified five
unsettled integrity questions around the activation corridor. This campaign prepares their
remediation without selecting a credential platform or reopening execution.

Only Preparation Batch 0 is authorized.

## Preparation mandate

Inventory and classify each requirement as `EXISTS_CANONICALLY`, `EXISTS_FRAGMENTED`, `ABSENT` or
`DEFERRED_BOUNDARY`:

1. the exact producer and lifecycle for an Imperator principal bearing
   `provider_binding_activation_authority`;
2. decision and issuance interruption cuts before consumption, after consumption but before target
   commit, after target commit, on retry, at expiry and under contention;
3. terminal treatment of every issued activation authority and every `ACTIVATED_UNCONSUMED` lease
   stranded by the custody refusal;
4. the precise in-memory credential-reference access boundary, including authorized readers,
   lifetime, copying, exception, logging, dump and crash exposure;
5. the evidentiary strength of distinct-instance refusal, declared unsupported custody and a real
   second-process or restart demonstration;
6. whether remediation should preserve, quarantine, supersede or retire the activation corridor;
7. the prerequisites a future credential-platform campaign must satisfy before it may consume any
   activation artifact.

Preparation must assign exact producers, consumers, recovery owners and non-authorities. It must
propose the smallest safe sequence and may conclude that Batch 2–3 artifacts should expire unused
and never be adopted by a future platform.

## Preserved perimeter

Preparation Batch 0 may change only inventory, review, flow, handoff and documentation assertions.
It may not define or change runtime contracts; grant principal authority; consume or replace an
activation authority or lease; issue, reconstruct, persist, transfer, deliver, consume or resolve a
credential capability; persist or disclose a credential reference or secret; select or implement a
credential platform; migrate the command; invoke a provider; perform external I/O; open Iron Gate
or Lazaretto; or change inbound webhook, sortie, revocation, propagation, telemetry, reassessment,
containment or incident behavior.

The terminal custody refusal remains authoritative. Provider Execution Assurance remains paused.
