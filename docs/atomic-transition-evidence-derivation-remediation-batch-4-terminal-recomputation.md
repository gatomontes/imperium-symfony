# Atomic Transition Evidence Derivation Remediation Batch 4 terminal recomputation

## Result

`BATCH_4_TERMINAL_EVIDENCE_CHAIN_RECOMPUTATION_COMPLETE`

The pure terminal recomputer independently reconstructs the typed capability
manifest, value-aware secret-exclusion proof and evidence-bound aggregate audit
receipt. It compares the complete reconstructed records, not caller assertions,
and recomputes every seal, reference and ordered digest across the eight-kind
case chain.

Resealed capability lies, resealed secret-proof lies, reordered results,
missing cases, mismatched references and altered aggregate fields are refused.
Successful recomputation derives
`MATERIAL_EVIDENCE_DEFECT_CORRECTED_PENDING_CLOSURE`.

This batch does not remove the campaign qualification or authorize closure
replacement. That final state change remains reserved for the separately
bounded read-only Batch 5 closure.

No journal is persisted, no live lock is acquired, no state is written or
repaired, no authority is issued or consumed, no execution is admitted, no
successor is adopted and no binding state changes. No live credential,
provider invocation, external I/O or provider effect occurs.

The closure remains `CAMPAIGN_CLOSURE_ACCEPTED_WITH_MATERIAL_EVIDENCE_DEFECT`
until Batch 5. The provider binding remains `BOUND_INACTIVE`. Required v3
execution admission remains `NOT_IMPLEMENTED`. `UNKNOWN_REPLAY_PROHIBITED`
remains binding.
