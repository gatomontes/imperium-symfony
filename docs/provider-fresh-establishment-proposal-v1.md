# PPC7 — same-root FRESH institutional establishment

Status: **PROPOSED_FOR_OWNER_APPROVAL; NO IMPLEMENTATION ACCEPTANCE**. Target: R4. Baseline main `ebf2c3dfebae300c8aacd084acb152bfd9915f71`, tree `69962e5f606170b2f1965db221bd30389208df0b`. [Campaign](next-campaign-provider-fresh-establishment.md), [source inventory and implementation plan](provider-fresh-establishment-implementation-plan.md), [exact proposal hash and approval record](handoffs/provider-fresh-establishment-approval.json).

The owner's “proceed” authorizes this preparation. PPC2/PPC6 approval covers remaining R3, but explicitly excludes a new establishment contract decision: [PPC6 implementation plan, section 3](provider-designation-assignment-implementation-plan.md). The clauses below require a separate decision. R1/R2 remain accepted; PPC6 components remain accepted with R3 open. **Countdown: 3–5 → 3–5; decrement 0.**

## Selected source decision

Keep genuine FRESH founding first, including the existing vacancy and one-use founding checks. Then permit one separately authorized, exact, pre-operational institutional package through a new purpose-limited native establishment entry point. This is a successor to the present prohibition on all native installation after FRESH consumption. It does not make an occupied root FRESH or reopen the generic operator-root window.

The current source cannot execute this sequence. `FreshProducer::publish` and `NativeConstitutionEvidence` enforce vacancy. `OperatorRootOwnership::nativeInOwner` rejects onboarding v4 and any consumed FRESH binding. `FormationInstitution::actorInOwner` needs complete native installation/occupancy originals in that same root. The proposal changes the specific post-founding installation competence, rather than treating an imported holder or another root as its source.

## A. Separate competence and exact package

Approve the new native purpose **ESTABLISH_FRESH_FORMATION_INSTITUTIONS**, for one exact FRESH-founded root, instance and Formation identity. Require a separately signed native establishment authorization by the authenticated founding Operator, plus a separately signed Formation-owner decision over the same complete terms. Resolve each key against its existing independently enrolled trust and verify its scope; matching names or possession of either key cannot establish the other authority. The same human/key may occupy both roles only when both trust chains actually establish them. A request cannot enroll its own key. Use a new closed native authorization envelope; do not widen the existing O2/O4 effect allowlists or reinterpret a founding signature as this new purpose.

The terms bind the exact original FRESH policy, constitution, command, founding completion and holder; canonical root identity; instance, operator and Formation identities; both trust fingerprints; exact complete personnel package and every artifact; expected Formation head; finite validity; domain-separated nonce, correlation and reason. References carry schema, identity and digest with checked native/O4 conversions. Both authorizations sign the same terms, with no circular reference between their signatures. They grant one establishment only, never a new founding right.

The package contains exactly one artifact-backed OFFICER for each of these nine existing institutional Seats: `garrison.constable`, `guildhall.guildmaster`, `laboratorium.alchemist`, `senate.lord-speaker`, `conscription.recruiter`, `senate.committee.consistency`, `senate.committee.governance`, `senate.committee.practice`, `senate.committee.security`. No wildcard, operative, generic-v0 placeholder, extra Seat or replacement is permitted. Retain the native artifact validation and package provenance requirements; signatures over malformed artifacts do not cure them. These are the founding institutional personnel needed by Formation. Courtthane and Locksmith still require their separate actual Profile, designation and appointment chains. Augur remains the original FRESH holder and is not installed a second time.

This purpose grants no model preparation, Profile approval, designation, target appointment, provider access, settings application, operationalization or execution. Existing institutional approval jurisdiction remains unchanged. General existing-installation cutover, governed institutional succession and extra packages are outside this provision.

## B. Supported sequence and custody

Use one canonical disposable root in offline proof. Establish public trust through the actual enrollment owners, admit authentic signed synthetic inputs through their real producers, and perform base selection/mapping and FRESH founding through the actual command and constitution owners. External base/account/cognition facts may use clearly identified synthetic ports; the founding, installation, trust and institutional authority owners may not.

Only after the real founding holder and consumption have committed may the new establishment authorization be reserved. Reconstruct and currently validate all founding originals through the existing holder/constitution/base owners; a caller's H record, status or copied completion is insufficient. Verify that the original pre-operational condition remains: no operationalization seal, native-authority registry, bootstrap successor state, pre-existing native installation/occupancy, unknown partials, competing package or affected sessions/claims/reservations. A second policy, alias, trust enrollment, instance label or authorization nonce cannot reset root-wide establishment consumption.

Add an explicitly initialized optional versioned Formation state extension for the establishment protocol. Initialization itself grants no installation; it requires exact owner authorization/head and refuses ambiguous or affected in-flight state. Preserve historical outer frame and onboarding v1–v4 meanings and original bytes. Decode the new extension strictly with finite byte, record, scan, nesting, time and counter bounds. Absence means this path is unavailable; unknown or malformed state refuses. A supporting native record needing stronger semantics must have a new version and explicit adapter, never silent promotion of old records.

The selected sequence is: actual FRESH founding → explicit establishment initialization → joint exact authorization and durable reservation → exact native package publication → complete establishment receipt → ordinary current institutional resolution. No native package may be written before the reservation is durably published. The reservation does not consume the founding right again and carries no transferable owner token.

## C. Writer ownership and interruption

Keep one Formation owner fence and the accepted Formation → native → bootstrap/target lock order. Add a separate internal establishment seam at the actual operator-root writer ownership boundary. The ordinary `install`, `installInOwner`, `withOwner` and generic ownership paths retain their current post-FRESH/v4 refusal. Do not add a request-controlled bypass boolean or merely add v4 to the generic allowlist. The new seam verifies the exact live same-root frame and retained establishment reservation before it can publish the approved package.

The journal and native package files do not have a shared atomic rename. Specify a bounded protocol explicitly: first commit `PENDING` with the exact authorizations, package and one-use reservation; then, under freshly acquired Formation/native ownership, revalidate current authorization, holder and exact pending state, publish the deterministic native package, verify every installation/occupancy and the complete package fact, and commit `COMPLETE` in the journal. The second transition holds the same Formation fence across its decision reads, native writes and final journal publication. No nested reacquisition of Formation, callback-controlled currentness, provider I/O or claim of cross-file atomicity is allowed.

For this new route, every current institutional authority consumer must require the journal's complete establishment fact as well as the actual intact native package. Complete native files without the final journal receipt still refuse; a partial package never exposes an authoritative subset. Inventory indirect/legacy readers as well as `FormationInstitution::actorInOwner`, and fence any route that could consume the new placements prematurely. Historical observations must be explicitly non-current.

After an interruption, recovery may only finish the exact already reserved package, under the same owners, with fresh continuing authorization/holder checks. Deterministic intact matching files may be recognized; unknown, altered, conflicting or unauthenticated partials refuse. Specify how the new route recognizes its own pending files without changing historical `NativeInstallationPackage` recovery semantics. No deletion, rollback, reseeding, replacement package, reset or new authority nonce may erase the consumed reservation. If authority expires or is revoked before completion, preserve a refusing terminal/pending fact; this batch need not supply a repair route. Completion cannot be manufactured from files alone.

Exact replay after a completed transaction returns its original historical receipt without new effects, even when that authorization has since expired. Reused scoped nonce with changed bytes conflicts. Historical replay never reinstalls missing files or proves current authority. Revocation/expiry blocks unfinished establishment; after completion, the authorization is a consumed historical installation authority, not a renewable tenure lease. Continuing institutional authority comes from the actual native currentness owners, with their corruption/successor refusals. Do not conflate this with PPC5/PPC6 authorizations that require continuing checks by their own contracts.

## D. Actual consumer and compatibility

Preserve the existing native installation and occupancy records as the sole incumbent source. The new journal extension is transaction provenance/completion custody; it must never become an alternative seat-to-holder registry. Join the exact completed package to that provenance when resolving new-route actors under the real Formation frame. Keep old valid native fixtures and legacy record meanings intact. Unsupported native-authority/bootstrap successors continue to refuse.

Prove all nine institutional actors through the actual resolver in a fresh process after the supported combined sequence. Then use those same-root institutions to produce both permanent Seats' PPC5 strict v2 model-bound Profile chains and PPC6 designation, independent appointment and mapping evidence, using the real owners. Do not import institutional witnesses, directly seed accepted state, use `synthetic-parent-institution`, or change an earlier fixture into proof of this new route.

This establishes R4's institutional prerequisite and demonstrates its real downstream use. It does not complete R3: substantive predicate production/verification, direct native assignment evidence, initial application/replacement, actual settings delivery and their full race/refusal proof remain separately open. Do not promote `NativeAssignmentEvidence` in Composition or claim `ASSIGNMENT_EVIDENCE_IMPLEMENTED_OFFLINE` from PPC7 alone. Keep R5 account/access/base/cognition production ports distinct and unavailable.

Preserve pinned AtomicTransition, accepted R2 storage/tenure boundaries, generic Profile contract, original signed proposals, frozen baselines, service wiring, dependencies, CI/guards and the 30-minute hosted worker limit. If a required executable source is independently frozen, retain its exact predecessor and identify the necessary additive adapter or separately bounded source decision; do not rewrite a pin or broaden A–F to conceal incompatibility.

## E. Required evidence

| Proof | Required observation |
| --- | --- |
| Supported positive order | Real same-root trust/admission/base/mapping/FRESH holder, joint new authorization, exact nine-member publication, completion and fresh-process current resolution; both permanent Seat downstream chains |
| Old refusal boundaries | Institutions first still fail FRESH; generic installation after founding still fails; absent/new unknown state, onboarding v4 on the ordinary installer, sealed/occupied/foreign roots and bootstrap/native successors refuse |
| Competence and originals | Missing either signature, wrong key/trust/role/purpose/root/instance/holder/head/package/artifact, altered metadata/digest, missing completion, extra or duplicate Seat and generic placeholders refuse before placement |
| Reservation/replay | Two processes reserve at one head with at most one winner; changed nonce bytes conflict; second package/policy/alias cannot reset; exact receipt replay causes no new publication |
| Currentness and races | Authorization/trust revocation or expiry, holder invalidation, native writer/operationalization and establishment compete in both real ownership orders; detached/foreign owner rejects |
| Crash boundaries | Before/after reservation journal rename, intent write, each placement, package-completion rename and final journal rename; restart never accepts an incomplete subset or double consumption |
| Recovery | Exact intact pending package can finish only while authorized; corruption/unknown files/expired authority refuse with originals retained; completed replay cannot repair deleted files |
| Actual authority readers | All new-route current consumers require full completion; history alone cannot issue or consume authority; package completion alone is insufficient |
| Compatibility/gate | Existing proofs and source pins remain; committed candidate, unchanged complete eight partitions/exact-case aggregate and all 11 Python guards; receiving source review plus fresh hosted CI before integration |

Retain signed public originals, complete chains, process order/barriers/native exits, UTC timing, journal/source identities, exact failures and all failed attempts. Use in-memory generated fixture keys and disposable roots only. Synthetic external evidence stays labelled synthetic. No prior PPC6 run substitutes for the new executable candidate's validation.

## F. Acceptance and countdown

Claim **SAME_ROOT_FRESH_INSTITUTIONS_IMPLEMENTED_OFFLINE** only after the full supported writer/consumer/recovery proof above. Otherwise return **PARTIAL_FRESH_INSTITUTIONAL_ESTABLISHMENT** with the exact unclosed boundary. Source approval permits bounded implementation; it is not receiving acceptance, installed establishment or permission for live action.

Countdown remains **3–5** during preparation, approval, submission and partial acceptance. R4-only receiving acceptance would leave R3 (1), R5 (1–2) and R6 (1), giving an estimated **3–4**; the minimum already allowed R4 to be combined with R3, so do not mechanically subtract one from both endpoints. No reduction is applied now. Any different combined acceptance or newly discovered scope must be explained in the ledger. R1/R2 earn no second credit; R3 remains open unless separately demonstrated and accepted in full.

Stop local execution at committed source and the complete public review packet. All five operational flags remain false: `deployment_approved`, `enrollment_authorized`, `live_ready`, `activation`, `execution_authority`. Retain `DEFER_ENROLLMENT` and the empty actual retry allowlist. No real provider/account/credential access, installed private state, live enrollment/founding/appointment, commissioning, deployment, activation or execution. No subagents are requested.

*Imperium via solitaria est.*
