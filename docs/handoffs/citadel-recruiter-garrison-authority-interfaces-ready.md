# Loco handoff — Recruiter currentness and Garrison authority interfaces

Run **A0–A3** from [the campaign](../next-campaign-citadel-recruiter-garrison-authority-interfaces.md). Read it in full and every required source it names before implementation. This is local code and offline proof, not a real institutional ceremony.

## Start and preservation

Create a fresh worktree `E:\htdocs\imperium-citadel-authority`, branch `codex/citadel-recruiter-garrison-authority-interfaces`, from the campaign-selection main commit pinned in the owner's launch prompt. Record `git rev-parse HEAD`, `git rev-parse HEAD^{tree}` and tracked status before editing. Preserve `E:\htdocs\imperium`, readiness, integration and native-lineage worktrees. The installed application's last supplied source remains `8244afa5630b06973cc4a52dce6ed96f42a444f3`; a main merge is not an installation update.

Accepted native-lineage integration is PR #771, merge `fb2916b0579ce6483f47bbde2fdaf6e9dd76dfa4`, tree `9e2f30eb52fe123371b1d75128a6f935a63f4aa2`. GitHub CI passed. The exact planning acceptance and its envelope are already accepted supplied evidence. Do not recollect them or re-open prior reviews. See [acceptance and qualifications](../citadel-native-lineage-acceptance.md).

## Execute

| Step | Deliverable |
| --- | --- |
| A0 | Source-backed issuer/currentness and immutable authority-revision design; proceed into implementation |
| A1 | Minimal public Recruiter evidence producer/inspection interfaces with authentic-provenance and bounded-currentness semantics, exercised on synthetic private backing state |
| A2 | Explicit Garrison authority request, dormant authenticated revision/resolution and a real admission-consumer fixture path; retain old occupancy bytes |
| A3 | Adverse/race/recovery and secret-exclusion proof, final offline gates, exact future owner commands and complete review packet |

Implement minimum source/contract/command changes justified by these two dependencies. If an authentic issuer or currentness source is missing, implement typed unsigned preparation and refusing validation, identify the exact missing interface/owner decision and finish the rest. Do not fabricate a positive lineage, replace native evidence with operator-root fixtures or satisfy formation competence with a ProtectedMission/planning grant.

No actual installation reads of private state or journals, real keys, enrollment, authority mutation, package installation, provider activity or mission execution. A future native exporter reading private backing state must have a separately disclosed footprint; do not widen the public Python collector. Synthetic test keys/roots are permitted, clearly labelled and never used as real evidence.

Produce exact PowerShell-friendly commands after implementing their actual interfaces. Label future owner-only actions, enumerate their prerequisites and prove them only in disposable roots. Keep readiness/activation/execution false. Preserve CF01/CF02/IR01 and all historical evidence. No additional broad inventory is requested.

## Return

Follow A3's complete packet requirements. Commit the final executable checkpoint before the final full offline PHP suite; retain focused and full test attribution, four historical warnings, exact tested/final trees and all post-test changes. Raw installation evidence remains outside Git. Return local commits, source archives/manifests, bounded bundle, proof/logs, unsigned owner package, whole-folder manifest and separate ZIP hash. Stop for independent review; no automatic implementation publication or activation.

*Hoc est pretium solitudinis.*
