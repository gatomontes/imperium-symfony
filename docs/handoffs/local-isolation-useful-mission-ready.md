# Local runner: deployment isolation and useful mission

Read docs/next-campaign-local-isolation-useful-mission.md completely and follow
every required source below before edits:
- Applicable repository/ancestor AGENTS.md, if present.
- docs/protected-mission-operator-runbook.md (including its amendment supplement).
- docs/handoffs/mission-amendment-correction-local-audit-complete.md.
- docs/mission-amendment-evidence-ledger.json and docs/mission-amendment-status-examples.json.
- contracts/mission-planning.md and contracts/mission-authorization.md.
- Current selection at the top of docs/delegate-mission-flow.md and todo/blackquill-todos.md.
- bin/protected-mission.php, src/ProtectedMission/*, tools/ProtectedMission.ps1,
  tools/Install-ProtectedMission.ps1, tools/Assert-ProtectedMissionInstallation.ps1,
  tools/sign-protected-mission.php, canonical services those use, and their tests.

## Entry and branch

Start in E:/htdocs/imperium. Verify origin is gatomontes/imperium-symfony, inspect
full HEAD, branch, worktree and upstream. Read-only Git fetch is permitted.
Begin from clean main containing the merged campaign-selection documents and
accepted implementation a1fc4f27634319f2a22df2e6a1b370f70cdb98bf.
Record the exact planning merge SHA; never guess it from a screenshot.

Preserve dirty/user work; isolate it if safe instead of resetting/stashing it
without explanation. Never force pull, hard reset or delete historical branches.
Create codex/local-isolation-useful-mission from the verified selection.
If it exists, inspect ancestry and resume matching work; do not overwrite it.
Intervening changes must be assessed before installation/snapshot assumptions.

## Execute

Complete Batches 0–2 sequentially without another approval request for routine
local preparation. Implement the smallest necessary route fixes and meaningful
tests. Keep the goal finite: actual-account access evidence plus one exact-source
read-only inspection and a separately labeled useful discrepancy report.

Commit Batch 2 code before its audit; record test counts/skips, tested SHA,
installed-package candidate hashes and exact target-object evidence. Later
documentation-only commits must be labeled separately.

Do not stop at discovering missing real accounts or keys. Finish and rehearse
the executable owner setup/resume package first. Then present the exact owner
actions with reviewed paths, identities, changes and expected outputs. The owner
holds the private key and performs privileged setup/signing outside agent custody.
Never ask for the key/password in chat or use fixture keys as real trust.

After the owner supplies the required installation/isolation results and genuine
signed authorization, continue Batches 4–5 under that exact scope without redundant
confirmation. Do not execute real mission work while those conditions are absent.
No blind retry after an uncertain consume; query persisted status and preserve
the incident. Do not turn a failed setup/mission into a reported completion.

Update this campaign's steps, flow, evidence ledger and runbook as stages finish.
Return the useful report and receipt verification, or the complete owner-action
package if that is the remaining boundary. Preserve earlier branches/tests.

Stop locally before publication or merge. No providers, external services, live
credentials, key handling by Loco, target remediation, installation replacement,
journal reset, branch deletion or live Batch 7.

Fortuna eruditis favet.
