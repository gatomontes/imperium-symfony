# Iron Gate Evidence Authenticity Remediation Batch 6 complete

## Result

Batch 6 selects and defines the versioned runtime-principal caller-authority contract and states the
exact integrity threat model. Seneschal request, Imperator decision and Imperator issuance require
three distinct authority identities; no reusable Imperator token exists.

The contract is not enforcement. Actor authority remains `EXISTS_FRAGMENTED` until a native issuer
and all three consumers require canonical `AuthorityConsumptionStore` consumption. Integrity is
truthfully limited to trusted-writer canonical integrity on one authoritative filesystem root;
hostile-writer non-forgeability is not claimed.

## Next separately bounded batch

Only Batch 7 may next be considered: implement the native runtime-principal caller-authority issuer
and require exact consumption at the three transitions. Batch 7 is not authorized by this handoff
and requires explicit continuation.

No live provider, command, transport or deferred perimeter is opened.
