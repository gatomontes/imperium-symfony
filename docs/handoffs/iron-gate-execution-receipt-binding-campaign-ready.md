# Iron Gate Execution Authority and Receipt Binding campaign ready

## Completed foundation

Transactional Authority Consumption Adoption is terminal through Batch 13. Its 26 canonical
consumers, 3 locked-fragmented consumers and 202 noncanonical/issuer tripwire entries remain exact.
Internal transactional proof is evidence for preparation, not authority to cross the perimeter.

## Selected new campaign

The next campaign is Iron Gate Execution Authority and Receipt Binding.
Only Preparation Batch 0 is authorized.

Preparation must inventory exact outbound authority, deterministic and sortie execution paths,
credential capability use, effect-start and unknown-outcome boundaries, raw receipts, Lazaretto
admission, persistence, reconstruction and all concurrency/fault proof. It must propose the smallest
safe sequence without changing runtime behavior.

The deterministic lane is the first candidate to assess because its operation, destination and
payload are already decided and it uses no external cognition. The sortie lane remains separately
bounded unless the inventory proves otherwise.

## Historical launch point

This handoff authorized Preparation Batch 0, which is now complete. The active continuation handoff
is `docs/handoffs/iron-gate-execution-receipt-binding-preparation-batch-0-complete.md`. Batch 1 is not
authorized. No residual Transactional Authority Consumption Adoption batch remains.

## Preserved boundaries

Preparation opens no Iron Gate execution, external call, authority, receipt, provider journal,
credential, Lazaretto policy, sortie, tool, revocation, propagation, telemetry, reassessment,
containment or incident behavior. No terminal campaign or Delegate Mission step reopens.

## New-chat continuation

> Continue Imperium from `main` after the merged Transactional Authority Consumption Adoption
> campaign-closeout and documentation-assertion repair commits.
>
> Read `docs/handoffs/transactional-authority-consumption-campaign-complete.md`,
> `docs/handoffs/iron-gate-execution-receipt-binding-campaign-ready.md`,
> `docs/next-campaign-iron-gate-execution-receipt-binding.md`,
> `docs/transactional-authority-consumption-coverage-audit.md`,
> `docs/handoffs/credential-boundary-remediation-complete.md`,
> `docs/runtime/deterministic-execution-lane.md`, `docs/runtime/agentmail-email-transport.md`,
> `todo/defensible-decision-record.md`, `todo/continuous-agent-governance-controls.md`,
> `docs/delegate-mission-flow.md`, every PHP file under `src/Imperium/Runtime/LaCortine` and
> `src/Imperium/Runtime/Sortie`, `OracleResearchCommissionService`,
> `OracleResearchEvidenceAdmissionService`, and all associated tests.
>
> Begin Iron Gate Execution Authority and Receipt Binding Preparation Batch 0 only. Inspect every
> outbound execution-authority issuer and consumer, deterministic and sortie competing path,
> credential-capability use, effect-start point, provider idempotency/unknown-outcome rule, raw
> receipt, Lazaretto admission, persistence and reconstruction boundary. Inventory authoritative
> inputs, actor, scope, expiry, single-use identity, lock order, replay identity, partial writes,
> recovery and concurrency/fault/tamper/secret-exclusion proof.
>
> Classify every requirement as `EXISTS_CANONICALLY`, `EXISTS_FRAGMENTED`, `ABSENT`, or
> `DEFERRED_BOUNDARY`; define and assign exact consumer postures; and propose the smallest safe
> migration sequence. Do not change runtime behavior, perform external I/O, open Iron Gate,
> Lazaretto, sortie, credential-platform, revocation, propagation, telemetry, reassessment,
> containment or incident boundaries.
