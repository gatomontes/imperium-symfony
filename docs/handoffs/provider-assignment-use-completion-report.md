# PPC9 native assignment/application/use report

Disposition: **PARTIAL_DESIGNATION_ASSIGNMENT_EVIDENCE**. R3 remains open. This submission adds the existing bounded pure-parse scope to direct NativeAssignmentEvidence calls, independently reachable native original/chronology proof, and explicit unapproved C1/C2 contract proposals. It does not establish native assignment, application, replacement or protected settings delivery.

## Result and exact incompatibility

The [source/admission chronology](../provider-assignment-use-chronology.md) proves the ordering cycle: policy admission requires an exact existing target Profile source, but the authentic native mapping can be produced only after that policy's same-root founding, R4 establishment, designation and independent appointment. Fixed O4 targets and assessment scope cannot be changed during replacement. A successor appointment produces a new mapping reference even with an unchanged binding reference. No accepted state, forward-reference authority, source substitution or policy reset was introduced.

The [predicate/original specification](../provider-assignment-use-predicates.md) separates signed institutional findings and qualification from the unavailable substantive `profile.fits` definition. C1 proposes a separately approved post-establishment exact-target/assessment contract, with a distinct replacement extension; C2 requires a closed substantive fit definition. Neither is approved or implemented. Existing PPC2 clauses A–F remain approved without a repeated request. R1/R2/R4 acceptance is preserved without repeat credit.

The [requirement matrix](../provider-assignment-use-matrix.md) records every completed component and open dependent row. The [writer/consumer inventory and future public-input runbook](../provider-assignment-use-consumers-runbook.md) distinguishes direct execution from source-path equivalence. Initial application A/B, whole-pair replacement/use, native races, integrated currentness/corruption/bounds and interruption/replay proof remain open behind C1/C2.

## Source identities and protection

- Published start: `1ae4eb8a18b025c90295eafd7cc523b1e12d8fee`; tree `5d264d5db5f99d65031ae92033ae8bf87489f6da`. This is the sole bounded bundle prerequisite.
- Tested executable commit: `cb97804aa4fcd5cc09496b551f544457a5e61a7a`; tree `8d39275df1b91af993807a908674e6bf9a85a80a`.
- Exact tested source digest: `7b7695edb9914cf358ac1db34b11862e3de1f20c918f70197eaa12ad717cfdf6`. All eight before/after digests match; independent reconstruction from committed Git blobs also matches.
- Main baseline: `23d21a3bbbf4bfa33b8097b7bbacf0f78bd1b92c`; tree `1a76d25e7f5d7fb3a54fdeba81a18a97d32abedb`.
- PPC2 proposal SHA-256: `c24939fd808cd5917c8057efe518787cc2448d3da60be089511e7abd6dee2984`; separate approval SHA-256: `30f34e016b3074a38c69232cd42a0252761d46fbd074f81d8a8423882821285c`.

All 51 protected paths remain byte-identical to the published start. The only runtime change establishes existing StrictJson::within scope at direct NativeAssignmentEvidence ingress; only bounded pure parsing/canonical encoding is reused. Authority/currentness checks, O4 rules, default Composition/MissingAssignmentEvidence, dependencies, CI and accepted historical contracts are unchanged. No wall-clock performance guarantee is claimed. Final commit/tree identities are external in `identities.json` to avoid a self-referential commit identifier. The tested-to-final follow-up changes only this report and is separately validated.

## Fresh committed-candidate validation

Eight unused detached worktrees ran the unchanged reviewer prestep and unchanged eight-way PHPUnit selection, scheduled with two local workers. No partition was reused. Gate UTC: **2026-09-16T00:50:00.958917+00:00 → 2026-09-16T02:30:44.749220+00:00**.

Exact aggregate native exit **0**: **639 files, 4100 cases, 69500 assertions, 3 skips**, no JUnit failures/errors and exact file/case coverage. All partition and reviewer native exits are **0**. Diagnostic and reviewer-prestep assertions are excluded from these totals.

| Partition | Cases | Assertions | Skips | Summed JUnit seconds | Whole-worker seconds | Partition / reviewer exits |
| --- | --- | --- | --- | --- | --- | --- |
| 0 | 502 | 9511 | 3 | 1914.454 | 1957.093 | 0 / 0 |
| 1 | 415 | 7461 | 0 | 2259.637 | 2299.328 | 0 / 0 |
| 2 | 426 | 4370 | 0 | 1491.140 | 1556.610 | 0 / 0 |
| 3 | 633 | 24660 | 0 | 1278.627 | 1329.735 | 0 / 0 |
| 4 | 383 | 4393 | 0 | 355.510 | 418.719 | 0 / 0 |
| 5 | 456 | 5573 | 0 | 2082.089 | 2136.860 | 0 / 0 |
| 6 | 548 | 5391 | 0 | 780.846 | 843.781 | 0 / 0 |
| 7 | 737 | 8141 | 0 | 1169.910 | 1235.266 | 0 / 0 |

The hosted **30-minute limit is unchanged**. Local workers **0, 1 and 5** exceeded it. Native UTC launch/end, preparation/queue-inclusive times, raw logs/JUnit, exact selections, generated-reference byte digests and full source-after diffs are retained. Generated reference comment changes are distinguished by the unchanged gate's PHP-token equivalence check; no executable source delta was accepted.

All **11 guards** ran: **10 passed, 1 errored**, native exit **1**. `SourceBindingTest.test_changed_file_type_fails` could not create a symlink (`WinError 1314`, required privilege absent). The complete local acceptance gate is therefore **not green**. Fresh complete hosted CI and receiving source review remain required.

The three skipped cases are:

- `App\Tests\Imperium\Runtime\FreshEstablishmentResetClosureTest::testSymlinkRootSharesRevocationAndCannotResetConsumption with data set "pending-revocation"`
- `App\Tests\Imperium\Runtime\FreshEstablishmentResetClosureTest::testSymlinkRootSharesRevocationAndCannotResetConsumption with data set "completed-consumption"`
- `App\Tests\Imperium\Runtime\ReservedInstitutionalStorageTest::testSymlinkAliasesAndDanglingTargetsRefuse`

Their retained JUnit skip nodes contain no reason text; source tracing identifies the host's inability to create symlinks as the skip condition. These alias proofs are not claimed locally.

Four PHP warnings occurred in partitions 2 (one) and 4 (three). A separate exact-source `focused-warning-details` diagnostic retained all messages: existing TerminalRetirement, DeploymentCustody, OperationalConstruction and UnknownProviderOutcome crash-demonstration code reads `.git/HEAD` as a directory in a linked worktree. That diagnostic exited 0 (4 cases/90 assertions); it does not replace or add to gate coverage.

## Reached proof and retained attempts

The committed gate directly exercises signed A/B unresolved-target admission refusals, actual FRESH/R4 native preparation for both Seats, fixed NativeAssignmentEvidence early source/policy/owner refusals, refused late signed admission, twelve newly signed substantive-original refusal cases across both Seats, and real independent successor appointments/mappings. Later assignment predicates are not credited when an earlier source/policy check refuses.

The fresh reader reconstructed the native store and shared verifier, reached `O2_SOURCE_KIND`, then `O2_MODEL_SETTINGS_ABSENT` for both roles. It exited 0 in **106.340 seconds** within its 300-second test cap, with unchanged head and empty stderr. It did not apply or deliver settings.

`gate-public-verification` independently verified the selected complete original chains, exact digests, schema-specific Ed25519 signing authorities, event predecessor/generation/nonce/index relationships, public custody histories and recorded refusals. Native exit **0**, 39.469 seconds; exact UTC and full output are retained. These are integrity checks of public synthetic observations, not external production evidence or additional distinct grants.

All developmental failures remain available with before/after source archives, logs, native exits and timings: focused-1 was superseded/terminated after a failure; focused-2 exposed key-order-sensitive equality and was corrected to canonical equality; focused-3 and focused-4 exceeded the original 90-second compound reader cap. The final test cap is 300 seconds without changing runtime or hosted bounds. Focused-5 was terminated at the user's pause request (USER_PAUSED, exit 4294967295), not counted as a test defect. Focused-6 passed on resume (3 cases/26 assertions). Both separate Seat diagnostics passed earlier. None supplies a reused gate partition.

## Public review deliverables

`imperium-ppc9-public-review.zip` and its `.zip.sha256` contain the committed source ZIP, selected public evidence ZIP, exact membership/mode/byte/SHA-256 manifests, start/tested/final identities, three full-index binary diffs, and a Git bundle whose sole prerequisite is the exact published start. The standalone report equals committed source bytes. Selected public originals replace raw fixture-root export; private keys, credentials, installed state, vendor/cache and unrelated environment material are excluded.

The supplied independent `verify-public-review.py` checks archive membership and hashes, committed blobs, protected source, exact bundle prerequisite, diffs, documentation-only follow-up, tested source binding, unchanged aggregate and public original verification. The final outer-packet verification receipt is kept alongside the ZIP because embedding a receipt containing that same ZIP's hash would be self-referential. Review the receipt and checksum together; packaging is not receiving acceptance.

All five operational flags remain **false**: deployment_approved, enrollment_authorized, live_ready, activation and execution_authority. **DEFER_ENROLLMENT**; actual retry allowlist **[]**. Countdown **3–4**, decrement **0**; potential **2–3** after full R3 receiving acceptance is not applied. No push, integration, live provider/credential access, installed-state changes, enrollment, commissioning, deployment or activation occurred. No subagents were used.

*Imperium via solitaria est.*
