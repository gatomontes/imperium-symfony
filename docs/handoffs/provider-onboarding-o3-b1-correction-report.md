# O3-B1 R1 correction report

R1 is corrected locally. Source review and fresh complete hosted CI remain pending. No push or merge was performed. The prior review's original-candidate CI status and reviewer failure are supplied evidence, not results for this correction.

## Identities and scope

- Preparation: `02ce09621c63822eaff9ffd23151d5d3aed593a4`, tree `2fc0e9075ce199312797533d237652fa15b82865`.
- Correction entry: `18ff81299c42dc9fb60d49b419be8b7cfc38d1e3`, tree `7d08b1cff544ef8c1054afdcdaf1ddb7ccff7e20`. Tracked state was clean; both untracked previous handoff files were preserved.
- Tested correction: `17a23127fb349572cd4f8b357d679111ce312d7d`, tree `ddc10059ca0b508ab62f9ca77d6b13c5447299bd`. All 1852 tracked PHP files were captured and compared individually with Git and the checkout.
- Final documentation commit/tree: see `identities.json`. Only documentation changes follow the tested source.

The change is confined to original-backed mapping resolution and resource compatibility. It preserves the existing FRESH/common-owner producer, explicit migration, finite founding rule, independent commission, private same-KeySource custody, exact wire, semantic parsing and immutable result/envelope chain. All five operational flags remain false; retries remain disabled. No real provider, credential, installed-state, O4 or O5 operation was performed.

## Correction

`MappingLimits` resolves the exact mapping row using the existing provider/adapter/model/dispatch/configuration/revision and current-original checks. It requires all five supported meters to cover the approved per-cognition envelope: 1 call, 16,384 input tokens, 4,096 generated tokens, 100,000 micro-USD and 60,000 milliseconds. These are per-call maxima; the four declared slots and shared session budgets are unchanged. A lower mapped limit makes that configuration ineligible even if a particular response could be cheaper or shorter. No approved setting, mapped allowance or provider fact is rewritten.

`BaseProjection` authenticates the complete factual projection first, then deterministically demotes adapter_support to FAIL for each incompatible exact mapping. The unchanged O1 selector excludes those candidates and still chooses the least-cost supported alternative; prices, predicates and other exclusions are preserved. Missing factual evidence still refuses. The retained snapshot contains the derived exclusion. Existing valid snapshots keep the same bytes.

`FreshProducer` already reconstructs the projection at publication and current-holder resolution, so it now enforces the same bound before founding and whenever the holder is used. `AugurAdapter` also reloads the selected original map and checks the actual prepared maximum at reservation, each existing custody revalidation, dispatch-resource reconstruction and response validation. The original expiry check remains effective when time changes between credential consumption and callback. An uncertain claimed operation is not falsely settled.

The only correction-entry production edits are BaseProjection and AugurAdapter, plus the new MappingLimits helper. No O1 selector, owner, journal, migration, service configuration, dependency lock, full CI workflow or original fixture/test was changed. The two supplied reviewer PHP files are byte-for-byte unchanged; only reviewer-tests.patch was applied. The diagnostic workflow was not applied.

## Fresh local validation

877 selected tests / 8277 assertions pass under PHP >=8.4 with locked dependencies. All selected exits are zero, including changed PHP lint, non-debug kernel smoke and 86 static specification checks.

| Selection | Tests | Assertions | Exit |
| --- | ---: | ---: | ---: |
| correction-reviewer | 1 | 2 | 0 |
| new-o3-b1-bridge | 1 | 31 | 0 |
| correction-mapping | 16 | 130 | 0 |
| new-o3-b1-owner | 7 | 106 | 0 |
| new-o3-b1-base | 11 | 53 | 0 |
| credential-correction | 8 | 60 | 0 |
| reviewer | 4 | 10 | 0 |
| native-root-legacy | 4 | 291 | 0 |
| o3-b0 | 68 | 304 | 0 |
| o2-b1-b0 | 211 | 952 | 0 |
| new-o3-b1-refusals | 10 | 57 | 0 |
| o1-pins | 380 | 1519 | 0 |
| inventory | 16 | 4002 | 0 |
| fc | 140 | 760 | 0 |

Exact commands, versions, cwd, timings and exits are in final-commands.json and complete public output is in evidence/final. The proof matrix maps R1 and the complete existing campaign selections. New strict-meter cases prove no base completion/founding/assessment claim or POST; the unchanged reviewer test proves the original output-zero defect refuses through public Runtime. The exact-bound test and unchanged complete FRESH/W1/W2/W3 bridge prove this is not blanket refusal. Existing credential/currentness, uncertainty, concurrency/crash, O1/O2/O3/native/FC/reviewer/inventory cases are rerun on corrected PHP.

Development checks are separately labelled in evidence/development. Supplied hosted review logs remain under review-input. Neither category is final local proof or fresh complete hosted CI. The full workflow and its 30-minute allowance remain unchanged.

## Handoff boundary

Both preparation-to-final and correction-entry-to-final patches reconstruct the exact final tree; the incremental bundles name their prerequisites. Protected-original comparisons cover every tracked original from each entry. The package contains canonical changes, unchanged reviewer additions, the complete tested PHP snapshot/hashes, public logs/exits, manifests and a SHA-256-checked inner review ZIP. It excludes historical delivery archives, private evidence and runtime state. There is intentionally no outer checksum.

Stop for independent source review and fresh complete hosted CI. There is no unresolved local implementation failure; targeted/local campaign passes do not grant acceptance or integration authority.
