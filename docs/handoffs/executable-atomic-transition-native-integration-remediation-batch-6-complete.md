# Native Integration Remediation Batch 6

`NATIVE_INTEGRATION_BATCH_6_INDEPENDENT_RECONSTRUCTION_COMPLETE`.

NativeReconstructor independently verifies stored authority, creation, admission,
migration and receipt edges without invoking the admission producer or acquiring
a lock. It shares the canonical read-only principal loader and pure native
validators; it does not claim an independent cryptographic implementation.
Before/after source snapshots must agree. The authoritative binding reader now
requires this verification before returning an active operation interpretation.

Executable Root acts name the exact original native activation and production.
Scope-only acts remain nonexecutable. Reused signed act identities, constitution
predating its signed grant, renamed authority aggregates, substituted native
sources, schema relabeling, missing retirement and unexpected write-set fields
refuse. Expired or revoked native competence preserves a verified historical
receipt as COMMITTED_NOT_CURRENT; missing/replaced/revoked Root trust makes
historical verification UNKNOWN_REPLAY_PROHIBITED. Neither result permits retry.
Interrupted attempts remain unknown even when the grant later expires.
Native successor loading and independent reconstruction also validate the
original creation time against the executor activation; resealing an earlier
time cannot borrow eligibility from a later query.

`imperium:provider-transition:execute` is the Symfony application ingress. Its
only application argument is an existing authority id; project root comes from
the container and time from the native consumer. It has no provisioning, key,
clock, provider or root override option. Errors expose fixed codes only.

Focused proof: 26 tests, 263 assertions before the documentary and temporal tests.
Final full PHPUnit: **1927 tests, 44912 assertions passed**. PHP lint and diff
whitespace checks pass; the final temporal correction is included in this run.
Only Batch 7 remains, starting separately from clean locally merged Batch 6 main.
The historical terminal refusal is retained until that audit decides the claim.

Proof remains bounded to a cooperative local host, one physical root, immutable
source writers using the named locks, trusted operator-owned public trust and
an explicit complete legacy inventory. No hostile-host, distributed filesystem,
physical power-loss or hard real-time guarantee is claimed. Previously populated
legacy stores refuse migration; they are not converted or cleared.
BOUND_INACTIVE, historical NOT_IMPLEMENTED and UNKNOWN_REPLAY_PROHIBITED remain.
No live signing, provisioning, credentials/capabilities, provider invocation,
external effect, retry, Iron Gate or Lazaretto operation occurred.
