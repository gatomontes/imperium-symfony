# Next local O0 pass — selected decisions and retry-policy closure

> Current O0 provider closure: [report](provider-onboarding-o0-provider-report.md). Selected choices are retained;
> v1.3.1 and public templates await independent review and remaining evidence.
> O0 only: no runnable policy, live onboarding or O1–O5 selection.

Start from the new local owner-decision commit identified in the accompanying
review packet. Its parent is accepted delivery candidate
`5ea0a9dee3b3dea428bcf026841a6b23c0fd4a4d`. Use an isolated worktree, preserving the
existing installed checkout and historical packets. Read applicable AGENTS.md.

Read `docs/provider-onboarding-selected-decisions.md`,
`contracts/provider-onboarding-retries.md`, the v1.2 continuation/main contracts,
D2 proposal, owner sheet, accepted CY/FC addendum and all prior independent reviews.

Close O0 with the selected DeepSeek API-key/FRESH/D2-A route. Do not ask again for
those choices, three retries per assessment call or the $1.20 total ceiling.
Treat the chat decision record as provenance, not a signed policy or installed
competence. No instance creation or trust enrollment is selected by this pass.

1. Review the v1.3 retry extension against F1/F2: four predeclared attempts per
   W-group, successful group dependencies, separate one-use slots/claims, original
   command replay recognition, terminal versus unknown failure, shared budget and
   single assignment application. Reconcile current draft fields consistently.
2. Prepare the exact DeepSeek public provider appendix from official documentation
   and existing source. Retain URLs, retrieval times, source digests and precise
   support/unknown findings for model IDs, revision pinning, request configuration,
   tokenizer/token limits, usage/tariff accounting, request destinations and the
   retryable-failure evidence allowlist. Public reading only; no account API call,
   authentication, credential probe or paid evaluation. Unknown guarantees stay
   unknown; no unsupported fact or rate is adopted from a fixture.
3. Prepare deterministic public policy/workload templates and the finite attempt
   graph within the selected scope. Show exact unresolved public instance/trust/
   founding/Profile/holder/configuration refs; do not manufacture genuine acts,
   credentials, enrollment receipts or signed policy. Distinguish all owner-supplied
   choices from still-proposed technical predicates and alias limitations.
4. Identify the smallest lawful public-artifact preparation needed for the fresh
   route. Do not inspect installed/private state or reopen its founding window.
   If required evidence is unavailable, complete useful independent closure work
   and state the precise remaining dependency.
5. Update current steps/flow and report with actual source identities, decisions,
   validation attribution and remaining gates. Preserve CY/FC and prior F1/F2
   evidence. Review v1.3 separately; earlier acceptance does not cover new behavior.

O0 only. No runtime implementation, PHP/provider execution, O1–O5 selection,
appointment, enrollment, migration, activation, push or merge. B1 and
DEFER_ENROLLMENT remain unchanged. A $1.20 design cap is not live spending authority.

Commit locally and return ONE root-level `<campaign>-all-deliverables.zip`
containing every report, revised contract/policy/provider appendix, handoff/prompt,
README and evidence ZIP plus its checksum. Preserve original nested packets.
No checksum is required for the convenience ZIP. Keep it out of Git and give its
exact absolute path so the owner uploads one file.
