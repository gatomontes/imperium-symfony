# O5-B0 accepted integration and closure

O5 is accepted, integrated and closed within its offline engineering scope as of 2026-09-13. It completes the planned onboarding campaign through the three canonical commands: `imperium:provider:onboard`, `imperium:provider:status` and `imperium:provider:resume`. [Usage and limits](../provider-onboarding-o5-cli.md), [integration record](../provider-onboarding/o5-b0-reviewed-integration.json).

The operator requested **“proceed with o5”** after accepting O4. O5 used the repository's current complete parallel gate unchanged. No new live commissioning or execution authority was inferred.

## Accepted evidence

| Item | Identity or result |
| --- | --- |
| Integration | [PR #806](https://github.com/gatomontes/imperium-symfony/pull/806) |
| Accepted head | `0a4d3bb5604054eac89996286ef28a16ae1b0183` |
| Merge commit | `2fd239f7c174f896f2229c2ecdbccc977e65fdc4` |
| Verified merge and tested tree | `cea21ff41ea7b74ff08a1901b104c523c013fe5a` |
| CI checkout | `041926b9d56ac54025e019f9eef39e3f1b6ceac7` |
| Complete CI | [Run 34771816826](https://github.com/gatomontes/imperium-symfony/actions/runs/34771816826), success |
| Aggregate job | `103764780289`, success |
| Complete suite | 3,705 tests; 63,941 assertions; four explicit skips; no failures/errors |
| Exact coverage | All 608 files and every enumerated case exactly once |
| Guard checks | 11 passing source/coverage fault tests |
| Runtime | PHP 8.4.25 / PHPUnit 13.3.0 |
| Longest partition | 909.853630 seconds |
| Whole workflow | 17:30:50–17:46:33 UTC, 15 minutes 43 seconds |
| Aggregate source digest | `64ffb0cbee49c1c395115c773b029ab18503a6ca56b9f6f3d8f8a4b8342efe41` |

The merge was pinned to the accepted head. Its resulting tree was fetched independently and matched the tested tree exactly. The gate verifies committed source bytes and complete case coverage; its existing generated-reference exception permits only comments/whitespace in `config/reference.php`, with unchanged executable tokens. This is complete partitioned execution, not a claim of serial shared-process ordering.

## Reviewed behavior and corrections

The CLI preserves canonical request identity and bounded ingress. Preview/status open the existing formation lock read-only and never initialize storage, reserve an ID, migrate state, access credential material or inspect credential generation. Advances and evidence-only recovery remain under the accepted owners. The default deployment composition observes existing storage and does not install a live producer.

The seven fact dimensions use retained originals. Whole-set assignment proposals are distinct from applied persistent settings and the initial Augur base. Unknown effects retain their maximum exposure, including after expiry; current time/revocation checks do not conceal bad retained signatures. Historical application receipts remain visible when current prerequisites are stale. Actual use still requires the existing persistent-settings revalidation owner.

Review corrected post-publication error reporting, shared-budget checks, coherent failure observations, fresh-process test execution and expiry precedence. The full gate also exposed a historical pin on `AtomicTransition.php`; the shared writer was restored byte-for-byte and observation was scoped to `FormationJournal`. No successor-review ledger or provenance assertion was weakened. The pinned service file is unchanged; the new service declares its own alias. The runtime API test explicitly includes only evidence-only resume and owner-identity checking alongside its existing entries, and still excludes public delivery/capability ports.

The offline journey uses actual O1–O4 owners with explicitly synthetic provider/constitution/evidence ports. It covers registration, access, FRESH Augur, W1/W2/W3, whole-set application, persistent resolution, explicit replacement, replay, stale prerequisites and fresh-process status. Boundary tests cover pure observation, malformed ingress, redaction, exhausted budgets and interrupted dispatch/resume. Earlier candidate CI runs are diagnostic only. Review was performed by the implementing agent; no separate human or agent review is claimed.

## Campaign disposition

**Zero planned offline implementation batches remain.** O0–O5 are integrated within their accepted scopes. Live commissioning and existing-installation cutover remain separately deferred. Personnel appointment, enrollment, deployment, activation and invocation still require their own authority; all five operational flags remain false and the actual retry allowlist is empty.

These closure files are post-test documentation. They do not belong to the frozen runtime tree and introduce no executable, test, dependency, contract or workflow changes. Earlier O5 preparation holds and older stage counts are historical; this closure controls current campaign status.
