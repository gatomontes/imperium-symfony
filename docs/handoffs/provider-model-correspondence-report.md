# PPC5 local source review report

Disposition: **NATIVE_O4_MODEL_CORRESPONDENCE_IMPLEMENTED_OFFLINE** for the bounded
two-Seat preparation and strict consumer described below. This is an implementation
submission, not receiving acceptance, a fully green acceptance gate, designation,
settings use or runtime integration. Remaining validation limitations are explicit.

Countdown: **4–6 before review; potential 3–5 only after receiving acceptance of
complete R1 with no new scope**. Applied decrement: **0**. R2 is already accepted
and earns no second credit. Both canonical countdown records remain unchanged.
The milestone is the first separately authorized bounded live Courtyard interview;
full mission execution remains later.

## Exact approval and identities

The retained owner reply **“approved”** directly answers **“Do you approve PPC5
clauses A–F for bounded offline implementation?”** following PR #824. Exact UTF-8
Git blob SHA-256 of the unchanged proposal is
`8c4d9992d57d1ea98ae14e4fd4abfe6971f316dd87443388e9a31a34538998e8`, matching both
approval hash fields. The original proposed heading is frozen historical content;
the separate approval record supplies the approved disposition. No repeat blanket
approval was requested and no earlier approval was used to widen PPC5.

| Identity | Commit | Tree |
| --- | --- | --- |
| Accepted main baseline | `a0c3042d08574da3c54a0b0e2d4ffc87ea47b3a1` | `31cd602f2aa3df058b8b9f09fc13eee12ebb8b7a` |
| Preparation merge | `a3088e91624c9110f2776d7e2678e46248f250ec` | `e25745986610b2fa04d62e1c1d6dddfbf316e285` |
| Actual approved start / exact bundle prerequisite | `c93656f3be1766409f500909668caba89cda6ea2` | `0e1f81a0bca6fd0f6a2b71d920218523a9b56e1d` |
| First candidate, interrupted validation retained | `3fa7edcb6aa6af736737e2033544c9043da38c7e` | `2cecc614277ff14820e6c4b9fe514052f25f5925` |
| Second candidate, interrupted validation retained | `d2581566ae3587419d818441d976a4071261e6d2` | `183a9fcf37de0a299519c9bb3ca459a908ebdac3` |
| Final executable candidate, committed before complete revalidation | `9168eecca25bee9caa24ecbae25f41db983ab943` | `8904798344c323bf57345d547d85aa7589b35a4c` |
| Final documentary commit | Bound externally in packet `identities.json` | Exact final tree and complete source manifest |

The approved start was clean and descends from both listed baselines. Existing
branch/path names were preserved; the new worktree is
`E:/htdocs/imperium-provider-model-correspondence-1`, branch
`codex/provider-model-correspondence-1`. Earlier worktrees and installed applications
were not changed. The final report cannot name its own containing commit without
self-reference; the packet binds the exact standalone report bytes to that tree.

## Implemented scope and evidence

The [new v1 normative provision](../../contracts/formation-model-preparation-v1.md)
assigns only `AUTHORIZE_FORMATION_MODEL_PREPARATION` and the distinct
`DELEGATE_FORMATION_MODEL_SEALING` competence selected by A–F. It uses current
Formation owner trust and the actual current native Conscription Recruiter. The
domain-separated signed Conscription act seals one exact authorized source Profile
and specification. Generic Profile approval, designation, appointment and O4
application remain separate; there is no general owner model-approval operation.

Immutable specification/binding, typed source Profile evidence, authorization,
delegation and seal originals
retain full schema-qualified references and signed bytes. Complete O4 candidate
and request-configuration H originals are preserved, including wrapper metadata
and content bytes. Native and O4 digest conventions are explicitly checked.
Every H source reference resolves to a complete retained original in dependency
order. Typed source Profile evidence embeds the exact original signed Laboratorium
envelope, verifies existing journal custody and is referenced by the seal.
Provider/model/version spelling is explicit; no caller tuple or Profile/holder/
designation/application counter supplies native binding generation. The selected
provider/configuration/fee policy and O4 effect lists remain unchanged.

The dependency order is acyclic: originals and native lineage first, numbered
specification line next, separate delegation and owner authorization next, then
mechanically derived Profile and signed seal. Authorization identity exists before
output construction. Native generation comes from verified vacancy or the exact
predecessor, with deterministic lineage identity and finite bounds. No counter
reset, reused original identity or reused Profile ID/version is admitted. See the
[closed schema/reference map](../provider-model-correspondence-schema-map.md).

All current decisions, tenure checks, consumption and publication stay inside one
live same-root Formation owner frame. One existing journal publication binds
complete seal and one-use consumption; pending files are not accepted facts.
AtomicTransition, generic storage successors and the journal itself are unchanged.
No lower-lock upward acquisition, held-owner request flag or second incumbent
registry was added. The [writer/compatibility inventory](../provider-model-correspondence-writers.md)
lists every new producer and consumer boundary.

The real strict `recordAuthorizedModelBoundProfile` ingress consumes the fixed
owner-aware verifier under v2 evidence semantics. The explicit strict
`authorizedModelCandidateInOwner` lifecycle path requires that version and freshly
rechecks the native/O4 correspondence. Both targets have their own actual
Laboratorium evidence, four Senate findings, reconciliation, competent exact
Profile approval and Conscription qualification. PPC2 v1 retains its deliberately
structural meaning and refuses at the new strict seam; no synthetic authorization
fallback is available there.

Positive fixtures establish disposable institutions through the real native
installation producer, then invoke the implemented authorization/delegation/seal
and personnel methods. Their generated keys sign actual bounded institutional
acts. Independent public-only PHP and Python verification checked **38 frame
digests/links and 37 unique institutional signatures for each Seat: 76 frames
and 74 signatures total**,
including distinct owner preparation, sealing delegation and Conscription seal
purposes and each separate exact Profile approval. Python additionally verifies
native/O4 reference correspondence, source-line digests and the own lifecycle
chain. External model/version facts, access and cognition remain explicitly
**SYNTHETIC_UNVERIFIED** or unavailable. No fixture declaration stands in for a
missing model-authorization producer.

Controlled process proof covers competing seals, both revocation/tenure publication
orders, both Seats' strict current consumer races, fresh-process reconstruction,
wrong/expired owners, exception release and termination immediately before/after
the actual journal rename. Lock probes establish the held Formation fence; bounded
barriers establish order rather than elapsed sleep. Exact replay returns only a
historical fact. Changed replay, stale generation, wrong original, expired/revoked
authority or trust, retired Recruiter and incomplete native package refuse current
use. See [test/requirement map](../provider-model-correspondence-test-map.md).

## Validation

The unchanged complete eight-partition PHPUnit/source/exact-case aggregate
returned **native exit 0**, as did every worker: **619 files, 3,903 tests,
65,342 assertions, zero errors/failures, one skip and four reported warnings**.
Exact-case coverage is true. All eight before/after source digests equal
`9fb97c5f0279f92549a241e9a2e934b92419808334779337514fe7a5ee76d14c` and bind the
tested tree above. The gate ran **2026-09-14 16:16:34.227994–16:49:38.178770 UTC**;
worker start/end times and native exits are retained separately.

| Partition | Tests | Assertions | Warnings | Skips | Worker wall seconds | Native exit |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 0 | 461 | 10,280 | 1 | 0 | 912.526 | 0 |
| 1 | 369 | 7,528 | 1 | 0 | 603.406 | 0 |
| 2 | 458 | 5,576 | 0 | 0 | 1,947.306 | 0 |
| 3 | 739 | 20,747 | 0 | 0 | 590.671 | 0 |
| 4 | 378 | 3,624 | 1 | 1 | 792.858 | 0 |
| 5 | 473 | 3,702 | 0 | 0 | 978.506 | 0 |
| 6 | 494 | 4,143 | 1 | 0 | 1,379.420 | 0 |
| 7 | 531 | 9,742 | 0 | 0 | 1,696.960 | 0 |

Partition 2 took **32m27.306s wall time** (30m43.718s summed test time), exceeding
the unchanged hosted workflow's **30-minute job limit** on this Windows host.
The direct local script run is not a hosted job and does not prove that timing
budget. The workflow limit was not changed; fresh hosted timing acceptance is
outstanding. Together with the Python guard error below, this means the complete
acceptance gate is **not fully green** despite the successful local aggregate.

Platform: Windows 11 build 26200, PowerShell 7.6.5, PHP 8.4.14 ZTS x64,
PHPUnit 13.3.0, Python 3.12.10, Git 2.51.2.windows.1 and Composer 2.8.12.
Exact native tool outputs accompany `preservation-and-environment.json`.

The final committed public-proof run passed **50 tests / 497 assertions**, including the
unchanged reviewer credential regression. All new process results/native exits and
the two positive public chains were retained. Supplemental precommit diagnostics
passed 23/62, 16/308, 58/512 and 138/853 (one existing symlink skip), plus the final
two boundary cases at 2/8. These are separate diagnostics and are not mixed into
the complete gate's counts. Exact timestamps are retained where collected; the
earliest short targeted runs have tool-observed native exits but no separate UTC
envelope, and no missing timestamps are invented.

The first complete gate was deliberately interrupted after a bounds audit found
that content limits alone did not bound the whole O4 H wrapper or its references.
The second executable commit adds limits of 131,072 complete canonical bytes and
64 references without changing O4 Rules. A real-producer comparison proves two
otherwise valid H forms (65 references / 10,154 bytes and three references /
136,066 bytes) were accepted by the first candidate and refuse with
`PPC5_O4_WRAPPER_BOUND`, unchanged state, on the second candidate. That public
diagnostic retains both source identities, UTC interval and native exits. Its
references are shape-valid factual inputs, not claims of admitted external sources.

The second complete gate was deliberately interrupted after a reference audit
found that source Profile evidence still used its legacy raw hash and H source
references lacked complete retained ancestry. The final executable commit adds
the bounded typed source original and complete acyclic H resolution, without
changing historical personnel custody or O4 Rules. Meaningful producer tests
exercise missing/reversed/duplicate ancestry refusal and a valid successor's
complete strict lifecycle, plus an actual signed oversized source-envelope refusal.
The targeted correction passed 48/494 and the separate source-bound case 1/2.

The interrupted first gate is retained under `gate-20260914T154027/`, including
two completed workers and six native termination exits; the second is retained
under `gate-20260914T155204/`, with five completed workers and three native
termination exits. Each aggregate returned its actual exit 1. The authoritative
complete restart is identified by `active-validation.json`; no earlier partition
is reused. The first public proof (48/481 and state-only signature counts) and
second public proof (48/485) remain diagnostic. The final
public proof is identified by `active-public-proof.json` and includes the
separate lifecycle approval originals in its signature total.

The unchanged 11 Python guards ran: **10 passed, one error, zero skips; native
exit 1**. `test_changed_file_type_fails` could not create its symlink because this
Windows host lacks the required privilege (`WinError 1314`). This is retained as
an error, not a pass or skip. R2's receiving hosted CI remains accepted historical
evidence, but is not PPC5 validation. Fresh complete hosted CI on a capable host
and receiving source review remain required before integration. No CI, guard,
dependency, source-pin or selection weakening was used.

Platform/tool outputs and UTC/native-exit records accompany the evidence. The
external Windows launcher only adapts extensionless PHP invocation and extended
path prefixes; the actual partition worker, enumeration, exact-case/source
aggregate and guards are unchanged. Composer-generated reference documentation
was captured and restored before committing executable code; no generated service
reference change is included. All **26 protected files** checked against the
approved start remain byte-identical, including proposal/approval, countdown,
historical ledgers, storage successor, AtomicTransition, journal/owner frame,
generic model-bound contract, O4 policy/rules, Profile contract, dependencies,
workflow, guards and service/reference configuration.

## Delivery and remaining boundaries

The public packet includes committed source ZIP, selected public synthetic evidence
ZIP, complete path/mode/byte/SHA-256 manifests, tested/final identities, exact
base-to-tested/tested-to-final/complete full-index binary diffs, bounded Git bundle
with the exact approved-start prerequisite, public signature verification and an
independent packet verifier. The standalone report equals the committed source
copy. Source modes come from Git; evidence modes are normalized to 100644.
Private fixture keys, runtime roots, installed material, vendor/cache and unrelated
untracked files are excluded. The outer SHA-256 is separate.

No known supported PPC5 producer/correspondence/synchronization path is intentionally
left open. Receiving acceptance and platform-complete validation remain outstanding.
R3 designation/shared assignment/application/use, R4 combined FRESH establishment,
R5 production account/access/base/cognition and R6 separately authorized live
commissioning/interview remain open or deferred. MissingAssignmentEvidence, root
vacancy, `B225_FRESH_ROOT_OWNED`, old schemas and all production refusal ports remain.

All five operational flags are false; `DEFER_ENROLLMENT` remains and the actual
retry allowlist is empty. No real providers/accounts, credentials, appointments/
enrollment, installed private-state access, deployment, commissioning, activation
or execution occurred. Work stops at local commits for receiving source review and
fresh hosted CI. The [runbook](provider-model-correspondence-runbook.md) preserves
that boundary and the **4–6 → 4–6** submission countdown.
