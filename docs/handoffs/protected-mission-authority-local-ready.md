> Superseded local entry: use `docs/handoffs/mission-amendment-correction-local-ready.md`.
> The completed candidate is preserved for focused AM01/AM02 correction under
> `docs/next-campaign-mission-amendment-correction.md`. Do not restart this old runner.

# Protected Mission Authority and Operator Ceremony — local runner

Read `docs/next-campaign-protected-mission-authority.md` and `docs/canonical-mission-authority-post-batch-3-review.md` completely before acting. Follow repository instructions, including any applicable AGENTS.md. Read `contracts/mission-planning.md`, `contracts/mission-authorization.md`, the canonical planning/review/derivation services, custody and AtomicTransition services, relevant tests and the current top section of `docs/delegate-mission-flow.md`. Track required source reads and exact revisions.

## Start and preserve

Work in the existing local Imperium repository (normally E:/htdocs/imperium; verify, do not assume). Inspect git status, branch, remotes and full HEAD. If unrelated work is dirty, preserve it and stop before switching or changing files.

A read-only Git fetch of origin is allowed solely to obtain the merged documentation selection and preserved quarantine refs. Verify origin identifies gatomontes/imperium-symfony. Record origin/main's exact commit; confirm it contains this handoff and the campaign. Start a new branch `codex/protected-mission-authority-operator-ceremony` from that verified origin/main without changing local main. If main has moved beyond the selection, inspect the intervening diff; stop on overlapping/runtime changes or contradictory authorization rather than assuming a new baseline.

Preserve `codex/canonical-mission-authenticity-real-snapshot-remediation` at `8df34679beab0ba8699a68fdd458570bf658c4c8`. Verify its ref; stop if it differs. Do not delete, reset, force-push, merge or wholesale cherry-pick it. If the expected ref is absent locally, retain the fetched remote ref and inspect by exact SHA. Preserve the older mission-thread candidate as well.

The previous test is historical local evidence: 2661 tests / 52390 assertions, not proof of the new boundaries. Preserve transcripts and sanitized hashes; classify and selectively port useful tests. Never weaken assertions to obtain green. Do not reuse its dossier, test keys, runtime records, approval or capability material. The old real reference mission remained blocked; do not resume it.

## Execute

Conduct the Protected Mission Authority and Operator Ceremony campaign, Batches 0–5, sequentially with separate commits:

0. Baseline, previous-test disposition, exploit reproductions in disposable roots, explicit threat model and reuse inventory.
1. Protected trust/custody and canonical boundary design/implementation.
2. Sole verified lifecycle mutation plus atomic currentness, revocation and nonce consumption.
3. Complete executable Operator ceremony, integrated with existing canonical planning/review/derivation.
4. Real application/CLI, independent-process adversarial proof and one disposable test-only Git inspection mission with observable progress and receipt.
5. Separately sequenced exact-head local audit, full suite, updated steps/flow and human handoff.

Follow the campaign's detailed acceptance criteria and stop conditions. Run focused tests each batch and the full suite after Batches 2, 4 and at audit. Do not make unrelated generated-file changes; inspect incidental deltas and remove only changes demonstrably created by this run.

Do not claim protection from same-UID or same-process adversaries using private methods, container visibility, HMAC, plain digests or 0600 files. Use an explicitly enforced deployment boundary and report what the local environment cannot prove. Do not provision real accounts/ACLs, create/read/use a real Operator private key or enroll real trust. Disposable test keys are permitted only in isolated test roots. A missing real deployment setup may remain a human gate; a missing command is implementation incompleteness.

Deliver tested PowerShell-friendly commands for public trust enrollment, stable canonical payload export, separately held-key signing, authenticated submission, authorization derivation and verification. Do not invent executable commands that have not been implemented and exercised with test identities.

## Stop and return

After Batch 5, stop on the new local branch. Do not push, open a PR, merge, alter main or delete branches. No real reference mission, provider, credential, external-system access, live trial or Batch 7. Initial Git fetch is the only network allowance.

Return exact baseline and HEAD/tree, batch commit list, worktree status, test commands/results/skips, source-bound evidence ledger, previous-test map, threat-model limitations, executable human setup/approval handoff and whether deployment isolation was actually measured.

Use `PROTECTED_MISSION_AUTHORITY_LOCAL_CANDIDATE_COMPLETE_PENDING_REVIEW_AND_OPERATOR_SETUP` only if implementation and disposable proofs are complete. Otherwise report `IMPLEMENTATION_INCOMPLETE` and the exact blocker. Add `DEPLOYMENT_ISOLATION_UNPROVED_OPERATOR_SETUP_REQUIRED` whenever applicable. Never claim real mission completion or independent remote acceptance.

Imperium Maximus.
