# Credential Boundary Batch 11: Laboratorium Complete

Batch 11 migrates only the Alchemist Profile-elaboration gateway.

The resolver handles two explicit native authorities: ordinary accepted Profile derivation and accepted Delegate-mission Profile derivation. It rereads the exact acceptance or disposition and its authorization or commission context, verifies the immutable Persona/Profile-scope binding and unconsumed single-use authority, and derives the exact ordered provider-input digest. Both authority types target `laboratorium.alchemist` for one `elaborate-profile` purpose.

The gateway preserves the existing elaboration JSON contract while using the shared governance cognition invoker. The direct Symfony agent and optional transient-caller route are removed.

The executable inventory falls from 26 to 25 agents. No other cluster moved and the system-wide gate remains open.

Batch 12 migrates Senate Profile-examination cognition only, reducing the inventory from 25 to 16.
