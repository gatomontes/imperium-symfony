# PPC1 local implementation report

Disposition: **CONSTITUTION_IMPLEMENTED_OFFLINE; ASSIGNMENT_CONTRACT_AMENDMENT_REQUIRED**. Two-port engineering success is not claimed. The source changes implement the compatible FRESH producer/verifier path and leave assignment evidence refusing for the concrete owner-contract incompatibility documented below.

## Source and scope

Fresh worktree: `E:\htdocs\imperium-provider-institutional-evidence`, branch `codex/provider-institutional-evidence`, fetched from `origin/codex/provider-institutional-evidence-preparation`.

Exact starting commit: `3137d06bb2c1b0cd6d3cb2459d4e5783535a73f5`.
Exact starting tree: `d041424ed3ae69cda98cbb46ae5eeb178c317b43`.
Entry was clean. Previous worktrees, evidence and installed checkout were preserved. No dependency updates, CI changes, service configuration, pinned shared-writer provenance changes, credentials or installed private state were used.

`ConstitutionSource` prepares exact public originals using the existing source schema. `NativeConstitutionEvidence` reuses competent original authority and finite terms under the owning journal frame before publication, and requires the actual current holder afterward. `FreshProducer` supplies that current frame; the verifier neither acquires a journal lock nor retains a snapshot. The existing holder/command/slot/completion transaction remains the publication point. Dormant composition defaults to this verifier; the default deployment reader is unchanged.

See [producer / competence / lifecycle matrix](../provider-institutional-evidence-matrix.md), [changed-test map](../provider-institutional-evidence-test-map.md), and [future public-input runbook](../provider-institutional-evidence-public-input-runbook.md).

## Unresolved assignment contract

The exact approved Profile is not the same institutional fact as a current designation. Existing formation evidence ends at approved/assembled-for-appointment; the legacy model-binding producer explicitly seals a non-current revision. The frozen Profile contract requires destination-steward designation and predecessor supersession. There is no compatible synchronized producer of those exact O4 Profile/model-binding generations in the traced owner paths. An immutable source, separate incumbent-file read, operator assignment act or personnel appointment cannot silently substitute for it. The matrix documents the exact schema/owner constraints and the smallest proposed amendment. It is not implemented or authorized here.

Consequently `MissingAssignmentEvidence` remains shared by application and `PersistentSettings`. Account/access, base and cognition also remain refusing in production. The source is not production-ready; installed Ubuntu state is unobserved. DeepSeek/API key, FRESH, D2-A, P1-P9, medium target capacity, least-cost eligible base, persistent whole-set settings, empty retry allowlist and `DEFER_ENROLLMENT` are preserved. All five operational flags remain false.

## Validation and review packet

Tested executable commit: `29f8373df1edf27f703d062730f18d3eb8af4346`.
Tested tree: `80999d380d9f03247c21d84b9215415e9a2eb20f`.
Source digest before and after every partition: `80dda4c679318b4bbbb1c7bd342f1f66a7ee9a50db1e1ecedacc2bc61bf83355`.
Executable code was committed before final validation. No prior O5/PPC0 pass is credited to PPC1.

Actual host: Windows `10.0.26200`, PowerShell 7.6.5, PHP 8.4.14, PHPUnit 13.3.0, Python 3.12.10. Locked Composer installation used `--no-scripts`: 82 installs, zero updates/removals. No dependency or CI files changed.

| Fresh validation | Actual result |
| --- | --- |
| Eight complete PHPUnit partitions | All eight native exits 0; **3,733 tests / 64,089 assertions**, zero errors, failures or skips; four PHP warnings |
| Exact source / coverage verification | Exit 0; **611 files**, every enumerated test exactly once, exact tested tree and matching before/after source digests |
| Guard suite | All **11** attempted; **10 passed, one error**, native exit 1: Windows `WinError 1314` creating the changed-file-type test symlink |
| Required credential-binding regression | 1 test / 1 assertion, native exit 0 |
| Focused final internal tests | 17 tests / 64 assertions, native exit 0; subsequent composition-specific case 1 test / 3 assertions, native exit 0; all included again in complete validation |
| Warning diagnosis | Four unchanged crash-demonstration tests, 4 tests / 90 assertions, native exit 0; each treats `.git` as a directory although this checkout is a worktree |
| Ubuntu / hosted CI | **NOT RUN; REQUIRED BEFORE PUBLICATION OR INTEGRATION**. Docker Linux engine unavailable; WSL lists only stopped `docker-desktop`. Installed Ubuntu state unobserved |

Partition execution began `2026-09-13T19:41:13.518885+00:00`; the last partition finished `2026-09-13T20:13:55.711912+00:00`. Individual UTC start/end records, native exits and durations are in `partition-*-execution.json` and `validation-summary.json`. The longest partition's summed JUnit time was **1,945.274873 seconds** (about 32 minutes 25 seconds). This is a measured Windows result, not proof of the unchanged Ubuntu workflow's 30-minute job bound. No serial timeout gate was substituted.

The unchanged partition script cannot launch its Unix `vendor/bin/phpunit` entry directly through Windows CreateProcess (`WinError 2`, preserved diagnostic exit 1). An external host-only launcher prefixes that command with `php` and normalizes Windows extended-path arguments for PHP. Partition selection, test enumeration, source verification, coverage verification and repository gate bytes remain unchanged. Therefore these are complete **host-adapted Windows suite/source/coverage results**, not a fully passing Ubuntu CI pipeline. No guard was disabled or relaxed.

Preserved exploratory failures: the new composition test initially omitted its disposable response directory (`O2_ENVELOPE_ROOT`, PHPUnit exit 2), then expected internal `STEP_ADMITTED` instead of public `ASSESSMENT_AUTHORIZED` (exit 1). Only test setup/expectation changed; the final case and complete suite pass. `diagnostics.md` distinguishes exploratory runs without precise timing metadata from later captured executions. Symfony's generated reference diff is retained externally; its committed original is restored after the gate's existing token-equivalence verification. No executable change follows the tested commit.

The public packet contains `source.zip`, `evidence.zip`, complete source/evidence file manifests with Git/archive modes, byte counts and SHA-256, `identities.json` with exact starting/tested/final commits and trees, exact implementation/documentation-followup/complete diffs, and a bounded `source.bundle` requiring only the preparation commit above. `VERIFY.md` and `verify-packet.py` give checksum, archive-membership and bundle verification instructions. The outer ZIP has a separate SHA-256 file. The packet's identities record the final documentation commit without a self-referential hash inside this report.

Stop: local commits and source review only. Nothing was pushed, published, integrated, commissioned or activated. The assignment amendment and fresh complete Ubuntu CI remain required; three provider-dependent sources remain unresolved.
