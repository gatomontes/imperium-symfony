# O2-B0 preparation and O1 closeout report

The reviewed O1-B2 continuation has been followed. O1 is verified integrated and
complete within its offline scope. O2-B0 is prepared and selected for the next local
implementation; this pass adds no runtime. No O1 batch remains and six implementation
batches remain across O2–O5. Later rows and live commissioning remain unselected.

Preparation entry: `b60f16a8af003408e84b68d1ac3462fb06a9479d`, tree
`34a51ba57463419dbf1eb8165756f00d46e121c1`. Fresh origin/main fetch and ancestry
checking verified the reviewed publication `79df44bc9be8a66110830162b644c505385dcffe`.
Work is isolated on `codex/provider-onboarding-o2-b0-preparation` in
`E:/htdocs/imperium-onboarding-o2-b0-preparation`. Installed and previous worktrees
are preserved. Final local commit/tree and file hashes are supplied externally in
preparation-identities.json to avoid a self-referential report hash. No push/merge.

The supplied reviewed archive passed CRC, unique safe paths, complete manifest,
payload SHA-256, inner checksum and identical inner/outer copies. Git merge/tree/
parents and all 11 supplied B2 canonical source hashes match. The retained CI log
matches integration metadata. The [B2 review](../reviews/provider-onboarding-o1-b2-source-evidence-review.md)
and [integration identity](../provider-onboarding/o1-b2-reviewed-integration.json)
are copied byte-for-byte, retaining the reviewer's design provenance and any
original encoding artifacts. The original B2 implementation report is unchanged.

The supplied fresh B2 CI reports PHP 8.4.25 / PHPUnit 13.3.0, 3,321 tests /
55,231 assertions / four unchanged Windows-only platform skips, on the matching
reviewed tree. This preparation inspected and verified its log/source attribution;
it did not run PHP or remote CI. Earlier author and preparation test evidence is
not reclassified as newly executed or O2 runtime evidence.

The [admission contract](../../contracts/provider-onboarding-authority-admission.md),
[campaign](../next-campaign-provider-onboarding-o2-authority-admission.md) and
[launch](provider-onboarding-o2-authority-admission-ready.md) bound the next work:
separate public-trust ingress code, signed original act/object/policy admission,
trusted reference resolution, revocation and currentness in the existing Citadel
aggregate. Admission uniqueness and historical recognition are distinct from
execution rights. O2-B1 owns shared commands/steps/slots, budget and custody;
O3 owns provider adapters and genuine FRESH founding; O4 application; O5 CLI.

Source inspection confirmed FormationJournal provides mechanics under the
citadel-formation lock, FormationSignatures has formation-only competence,
NativeTrust has its separate domain/effects, and CanonicalJson does not itself
provide strict duplicate-key decoding or authentication. O1 values are pure
projections. None can substitute for separately enrolled bootstrap competence.
The brief requires refusal of missing genuine sources, legacy development acts,
unsupported institution/existing-installation paths and fabricated currentness.
Synthetic tests must use the actual new producers, not directly seed trusted maps.

Current roadmap, status, flow and README now carry the O1 closeout and point to
O2-B0. Previous status/flow bodies are explicitly historical; original reports,
reviews, source-bound approvals and workload bytes are preserved. No settled
DeepSeek/API-key/FRESH/D2-A/P1–P9 values are reopened. DEFER_ENROLLMENT, unresolved
B1 remote guarantees, empty safe-retry allowlist, persistent operator settings,
CY/FC acceptance and all five false live flags remain.

Validation for this documentation pass: all 86 existing Python specification checks,
changed-document local links, exact review-copy and protected-source equality,
documentation-only scope, Git whitespace and packet/source/bundle integrity.
Detailed results/commands are retained in the package. No new executable test was
added for a documentation change. No PHP/dependency installation, provider request,
credential access, installed-state read, enrollment, activation or later batch.
No subagents were used. This is author preparation, not independent acceptance.

Next action: review this preparation's contract/batch boundary and exact source,
then follow the included isolated implementation launch. The handoff can use the
local preparation branch without implying publication permission. The implementation
must produce its own actual code and adversarial proof; none is claimed here.
