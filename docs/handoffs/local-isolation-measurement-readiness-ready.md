# Local runner — isolation measurement and readiness

Read completely:
- `docs/next-campaign-local-isolation-measurement-readiness.md`
- `docs/local-isolation-package-independent-review.md`
- `docs/next-campaign-local-isolation-useful-mission.md`
- current local owner runbook, isolation matrix/audit/ledger/package manifest,
  target manifest, mission draft, independent-review packet and derivative manifest
- every affected installer, probe, readiness, mission helper and meaningful test
- current `docs/delegate-mission-flow.md`, `todo/blackquill-todos.md`, and
  applicable repository/ancestor `AGENTS.md`.

## Entry

In `E:/htdocs/imperium`, verify origin, branch, full HEAD/tree, worktree and
local refs. Preserve `codex/local-isolation-useful-mission` and confirm
`574bfc69aebf12b5197f4f808b54896bf02ba142` exists in its local lineage.
Inspect later local commits and dirty files; retain the review assembler and user
work. Never reset, discard or silently stash it.

Fetch read-only. Record the exact remote-main planning merge. Create
`codex/local-isolation-measurement-readiness` from the inspected local
implementation descendant. If it exists, verify and resume it. Merge the exact
documentation selection locally into that new branch. Keep the new continuation
current and prior audit/history intact. Do not merge unpublished implementation
into main. Stop on unexpected code conflicts and report them precisely.

## Work

Complete C0–C2 sequentially with separate local commits. Reproduce LI01/LI02,
close only those boundaries, run meaningful focused/full/PowerShell verification,
and rebuild into a fresh package identity. Preserve package-47bcc44a, earlier
tests, branches, evidence and uncertain-attempt markers.

Commit executable/test work before the final audit. Return:
- exact baseline/integration/batch/test/final SHAs and trees;
- focused/full counts, skips, commands and changed-test map;
- fresh package path and manifest digest;
- fresh independent-review Markdown and derivative manifest paths;
- exact owner setup/resume commands and remaining human inputs;
- residual limits and prior-package disposition.

Do all safe preparation before asking for owner action. Do not install, handle
real passwords/private keys, enroll fixture trust or execute the real mission in
the implementation-agent session. If actual owner evidence already exists,
validate it without overwriting the installation.

After authentic owner evidence and signature exist, resume the parent Batches
3–5 under their exact scope. No implementation publication, main update, branch
deletion, reset/migration, provider invocation or target mutation.

*Ne passum quidem regrediemur.*
