# Handoff: model-bound Citadel cognition-turn authorization complete

## Step 39

The exact original commissioner independently decides whether the accepted Step 38 contract may receive one bounded cognition turn. Runtime machinery revalidates the acceptance, commission, runtime activation, original issuer, exact target occupancy, unexpired model access, immutable contract, instance, Manifestations, generations, and complete digest chain.

Only `AUTHORIZED` seals `CITADEL_LEGATE_COGNITION_TURN_AUTHORIZED_PENDING_PROVIDER_INVOCATION_ACTIVATION` and creates one target-bound, single-use authority for exactly one turn. The authority expires no more than fifteen minutes after issuance.

`REFUSED`, `DEFERRED`, and `REVOKED` seal attributable `NO_AUTHORITY` branches. A conflicting second decision fails closed.

## Boundary

Authorization is not invocation. The authorized branch records bounded governed-cognition authority, but the commission remains non-exercisable until provider invocation is separately activated. Provider invocation, credential use, cognition itself, tools, external action, operational use, execution, and continuing-turn authority remain false or unperformed.

## Verification

Dedicated tests cover exact short-lived authorization and replay, refusal, maximum lifetime enforcement, expired model-access rejection, single-turn scope, and the inert pre-invocation boundary.

## Next

Step 40 must activate provider invocation for the exact authorized turn under an equally bounded credential lease. It must stop before or atomically delimit the actual model call and must not open tools, external action, execution, or continuing turns.
