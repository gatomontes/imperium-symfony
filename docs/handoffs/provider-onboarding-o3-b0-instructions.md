# O3-B0 local implementation — reviewer instructions

Ad Imperium. Stop within O3-B0 for source review and fresh full hosted CI. This handoff does not authorize push, merge, a provider request, real credential use, installed-state operations or a later batch.

1. Verify outer ZIP CRC, unique safe member paths and `payload-manifest.json`. The outer ZIP intentionally has no checksum. Verify the inner review ZIP against `provider-onboarding-o3-b0-review.zip.sha256`, its CRC and `review-manifest.json`; its base payload members must equal the corresponding outer members. There are no recursive historical delivery archives.
2. Read `identities.json`. Entry is clean preparation commit `3a9642c8e26e0fc19d87267cb664995a2860a47e`, tree `0d12c3c74d657494198ea1f6a6b3792ae1af74bd`, including integrated O2 `9b50d53362e44af1d628034ad6153985b9a24d1c`. Verify the incremental bundle with that prerequisite and inspect it in a separate clean worktree. Do not reset or reuse another dirty worktree.
3. Read the report, original O3 contract/campaign and `docs/provider-onboarding-o3-b0-design.md`. Compare all paths in `changed-files.json`, canonical `source/` bytes and `implementation.patch` against the final Git tree. Verify every protected-original row and all 1,816 tested PHP streams against the tested commit and hashes. Post-tested changes must be documentation only. The individual root report/instructions are convenience copies of their canonical handoff documents.
4. Inspect the real four O2 interfaces and the new package. Check exact admitted grant/terms/executor/configuration/account-original linkage; fixed key generation; private one-use issue/consume/dispatch; journal rechecks; strict response/usage and local observation attribution; total deadlines, redirect/retry refusal and native direct-proxy configuration; immutable claim-bound storage; recovery from original metadata and envelope only. Recheck R1–R3 and all four unchanged reviewer cases. No O2 seam edit or legacy adapter/configuration/lock change is included.
5. Distinguish the evidence levels. The access integration fixture uses purpose-built public synthetic sources signed and retained by actual B0 producers and invoked through actual O2. Its account verifier is explicitly synthetic and pins its original. Public snapshots and signatures alone do not establish provider account facts. Default production evidence refuses. POST component tests use privileged access to a private transport primitive and do not establish an Augur, founding, holder, assessment completion or a positive O2 cognition journey. Missing selection/founding producers remain refusing.
6. Read `final-commands.json` and complete `evidence/final/` logs/exits. These are fresh local selections, not full hosted CI. Use PHP >=8.4 and the existing dependency locks; disable network functions as recorded. Replay the selections, changed PHP lint, non-debug kernel smoke and the 86 static specification checks in the review checkout. Author runner paths are recorded Windows paths; relocate a copy or use the argument vectors with the review checkout as cwd. Do not overwrite the supplied author evidence. Development failures and two intentionally interrupted runs are retained separately and do not count toward the final results.
7. Decide source acceptance separately from fresh full hosted CI and integration. No fresh hosted CI is claimed here. Keep all five operational flags false, the actual retry allowlist empty and DEFER_ENROLLMENT intact. Do not start O3-B1, O4, O5, OAuth, CLI, enrollment, appointment, assignment, activation or live commissioning from this packet.

The report states remaining boundaries and any concrete blocker. Final commit/tree identities live outside the self-referential report in `identities.json`.
