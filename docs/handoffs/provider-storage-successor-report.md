# PPC4 local source review report

Disposition: **PARTIAL_NATIVE_TENURE_SYNCHRONIZATION** pending the outstanding platform proof and receiving validation below. The generic reserved-target bypass is implemented closed in all three old entries; no known supported typed or generic writer path is deliberately left outside Formation. This report does not claim accepted complete R2, a fully green acceptance gate or runtime integration.

Countdown: **5–7 before review; potential 4–6 after accepted complete R2 closure with no new scope**. Applied decrement: **0**. Both canonical countdown records remain byte-identical to preparation. Preparation, submission, a partial component and local green tests earn no decrement. The milestone is the first separately authorized bounded live Courtyard interview; full mission execution is later.

## Approval and identities

The retained owner reply is **“approved”**, directly answering **“Do you approve clauses A–F for bounded offline implementation?”** after PR #820. Publication commit `65e8e27570a3899dec73affc70a72b2936223dde`, merge `20d27ad3b216b1e3c6588279fc20d1d5444bc77f`. SHA-256 of the exact committed proposal blob is `9c8a8d8e4fde15ef6aabddfab83fa4a6deb569837ca3ead457d6b9f297bbf464`, matching both approval hash fields. The frozen proposal's pending-state heading remains historical; its separate approval record supplies the approved disposition. No new blanket approval was requested.

| Identity | Commit | Tree |
| --- | --- | --- |
| Accepted main baseline | `96d05d839320909d140d50337ccd16fe68dc1604` | `707b94f58beb80d5180790a5aa58843f99fe55c0` |
| Actual ready start / bundle prerequisite | `816f0ffd14d6cc4d2c5d2c4605ec306aecffbf44` | `0cbf06a20928ce6be664b33925a220be1e87ab50` |
| First executable candidate, retained failed full run | `341fdef6583d5bf8e41ffd54fbc270eeedda3b4b` | `14dd6131b970fa3d4c8c3cb7431b319697738d33` |
| Second candidate, interrupted validation retained | `f605e816f101617a9889e2dae161fe8036eba627` | `6081a3d735ce71bc19bda6fa61d0957f3d7e670a` |
| Final executable candidate, committed before complete revalidation | `88e4c481dbf43a329565eebe1cb898b46790dbab` | `06cdd13fe5c615ef9aaf1a9b2b815563a45895b0` |
| Final documentary commit | Recorded after this report in packet `identities.json` | Exact final tree in that record and source manifest |

The ready checkout was clean. Existing unsuffixed branch/path were preserved; work proceeded in `E:/htdocs/imperium-provider-storage-successor-1`, branch `codex/provider-storage-successor-1`. No earlier checkout or installed application was changed. The final report cannot embed its own containing commit identity without self-reference; the external packet binds it to the final Git tree and includes its byte-identical standalone copy.

## Implementation and proof

The three original generic APIs enforce reserved-target refusal before their primitive lock, current-state read, guard, directory creation or publication. Explicit owner-aware counterparts require the actual live same-root Formation frame. They retain the original conflict/sealing/guard/commit code beneath the existing target lock. No generic callback automatically acquires Formation, no boolean substitutes for ownership and AtomicTransition stays non-reentrant and byte-identical.

Reserved classes cover bootstrap presence, all operator-root/native-authority/Formation storage and every Office occupancy subtree, including unknown Offices. Actual validator aliases and ambiguous descendant topology are handled before effects. Ordinary provider, mission, custody and terminal storage retains its original behavior. Deliberate alias refusal and platform distinctions are detailed in the [writer/compatibility map](../provider-storage-successor-writers.md).

Legitimate generic reserved fixture/regression writers now pass the held owner. Production institutional writers use the existing PPC3 bespoke owned paths; no additional production generic reserved caller was found. The old APIs themselves close arbitrary reserved calls from variable-path wrappers. The full PPC3 writer set, native registry changes without occupancy changes, independent appointments and real model-bound current consumers remain under **Formation → native → bootstrap/target storage**. Direct root installation already held Formation indirectly before PPC3; the old preparation error is not repeated.

Controlled separate-process proof covers both Seats, all three owned generic writers and both observation/publication orders. Actual native root installation and independently signed fixture appointments precede successful fresh-process current observations. Held/release barriers and independent nonblocking lock probes establish serialization; subsequent processes refuse unsupported successors or retired originals. Lower-lock guards and held primitive locks cannot induce upward or non-reentrant acquisition. Wrong/expired owners, guard exceptions and process interruption refuse or release correctly. Ordinary immutable replay and competing CAS behavior remain supported.

The unchanged PPC3 interrupted-package and historical completed-fact tests remain part of validation. No collection of individual renames is claimed to be package atomicity; the supported current reader requires the completed exact package. Unknown native/bootstrap successor forms continue refusing without falling back to old occupancy files.

Public fixture originals contain real institutional signatures from generated ephemeral keys. The public-only verifier checked 34 frame digests/links and 34 unique institutional signatures for each Seat (68 signatures total). Model/source-line declarations remain **synthetic unverified model authorization**, distinct from those signatures. No model approval, current assignment, designation or production authority is inferred from a fixture. See [changed-test map](../provider-storage-successor-test-map.md) and [runbook](provider-storage-successor-runbook.md).

## Narrow source successor

Both original snapshot reading ledgers remain byte-identical, including their predecessor history. Only the named pin test's successor-recognition branch changes, with all original assertions and all five cases retained. The new record permits exactly the two approved storage source paths, exact predecessor copies and proposal/approval reference. Negative cases reject wrong predecessors, unlisted paths, candidate hashes, approval defects, duplicates and other tampering. This is an authorized candidate chain, not receiving acceptance.

| Source | Exact predecessor SHA-256 | Candidate normalized SHA-256 |
| --- | --- | --- |
| ImmutableRecordStore.php | `51acaf101d0c14301cd2d29f19581473ff93fdc82179fa34225566282f97ae7a` | `a7ecbf47e350e0a673a2aaea6a1bea6425651145f69a425723847160c013ebe9` |
| MutableStateStore.php | `ca5feb40101e18b0577c84b7df265f4067d13c67213a222cf9b39e4e0520d99e` | `5e6d30f542df7c25bb9e26465f0986e0b7eb7b1bec7288d531e00ec495c1c1f5` |

Non-executable predecessor `.php.txt` copies are exact Git blob bytes. The tested record initially leaves its own tested identity null; the final documentary record fills the now-known committed/tested identity. Candidate source hashes are unchanged across that documentary update. No historical hash is rewritten to obtain a passing test.

## Validation and remaining proof

The authoritative resumed eight-partition run completed from the committed final executable candidate: **617 test files, 3,854 tests, 64,847 assertions, zero errors, zero failures, one skip and four reported PHPUnit warnings**. Every partition returned native exit **0**; the unchanged source/exact-case aggregate returned **0**, with `exact_case_coverage: true`. The separate unchanged 11-guard run returned native exit **1**: **10 passed, one error, zero skips**. The complete acceptance gate is therefore **not green**.

UTC run envelope, including disposable-worktree preparation: **2026-09-14T13:18:47.361700Z through 2026-09-14T13:54:26.365896Z**. Partitions began at 13:19:20Z; their individual end times and native exits are retained. The longest JUnit partition took **1,993.570498 seconds** (33m 13.57s); its full worker took about 35m 05s. This Windows result does not demonstrate completion within the unchanged hosted 30-minute job budget. Summed JUnit time is **7,069.443862 seconds**. The guards ran from **13:19:21.8860936Z through 13:19:23.4163482Z** on the same date.

All eight before/after source digests are identical: `8a808aceb99a0c557a557ae1965b7cc5cadb9e55105ccad8070df13982ef2ab9`, bound to tested tree `06cdd13fe5c615ef9aaf1a9b2b815563a45895b0`. Partitions 0, 1, 2 and 6 generated documentation-only `config/reference.php` deltas accepted by the unchanged reference guard; their exact diffs and before/after hashes are retained. The other four worktrees remained clean. The final committed reference file remains byte-identical to preparation; no generated runtime configuration is delivered as a source change.

Per-partition JUnit/selection/enumeration files, native logs, UTC/exit envelopes, aggregate output and `final-gate-facts.json` are under `resumed-20260914T131846/` in the public evidence ZIP. The final-source focused storage/process check passed **75 tests / 503 assertions** with one symlink skip; the documentary-pin/credential check passed **18 tests / 466 assertions**. These supplemental checks are separate from the full-run counts above.

Platform: Windows, PowerShell 7.6.5, PHP 8.4.14, PHPUnit 13.3.0, Python 3.12.10 and locked Composer dependencies. Exact tool outputs, UTC times, native exits and source inventories are retained in evidence. An external Windows launcher adapts PHP invocation only; worker enumeration, source/exact-case verification, CI and all guards remain unchanged.

The exact open proof paths are descendant **symlink aliases into reserved storage and dangling symlinks on this Windows host**, whose creation requires unavailable privilege. Live and dangling **junction** aliases separately refused all three old APIs. The PHPUnit symlink test has an explicit skip; the unchanged Python changed-file-type guard retains its actual `WinError 1314` error, not a pass or skip. Suitable-host alias evidence and fresh complete hosted CI are required. No supported generic bare-write bypass is knowingly retained in the executable candidate; the conservative partial disposition avoids claiming whole-set proof beyond the available platform evidence.

The first complete run exposed a compatibility error in `CitadelFormationCorrectionTest::testCF02ConcurrentCompletionBetweenSnapshotAndRecognitionIsIdempotent`: a strict error handler promoted the preflight's suppressed `lstat()` warning for an absent ordinary child-root path into refusal. The correction uses warning-free existence and parent-directory-entry checks. An intermediate file-existence-only edit failed the dangling-junction probe and was never committed; the final check explicitly distinguishes an absent entry from an unresolved Windows junction. A new strict-handler ordinary-storage regression preserves this behavior. The targeted original failing case passed after correction, and the complete eight-partition gate was restarted from the corrected commit without reusing first-run partitions.

Further review identified an ordinary publication race between absent-path observation and parent enumeration. A controlled test-only pause after the real filesystem observation let another process publish through the real old immutable API; all three first-writer variants then incorrectly refused as ambiguous on the second candidate. The final correction clears cached state and reobserves a newly present entry, preserving immutable replay and CAS conflict/guard order while still refusing unresolved junctions. The three controlled cases pass. This test pause supplies no owner or authority and replaces neither the writer nor current consumer. The second full run was explicitly interrupted, with native termination exits retained, before restarting all eight partitions from the final executable commit.

All diagnostic logs, including failures, warnings and skips, accompany the authoritative resumed run under **`resumed-20260914T131846/`** in the evidence ZIP. The first failed complete run remains at the archive root; the interrupted second attempt is under `final/`; the third attempt under `complete/` was stopped at the owner's pause. The owner subsequently requested continuation, and all eight partitions were restarted in unused worktrees at the same final executable commit. No earlier partitions are mixed into final counts. `active-validation.json` identifies the authoritative run. PPC3 hosted CI remains historical and supplies no PPC4 acceptance. No dependency, guard, CI, source pin or test-selection weakening is used.

## Delivery and boundaries

The public packet contains committed source ZIP, public synthetic evidence ZIP, complete path/mode/byte/SHA-256 manifests, exact identities, base-to-tested/tested-to-final/complete full-index binary diffs, bounded Git bundle with exactly the ready prerequisite, standalone report and independent verification script. Source modes come from Git; evidence modes normalize to `100644`. No vendor/cache, fixture runtime roots, private keys, installed state or unrelated untracked material is included. The outer checksum is separate.

Stop is at local commits. Receiving source review and fresh complete hosted CI precede runtime publication/integration. MissingAssignmentEvidence and account/access/base/cognition production ports remain refusing. FRESH vacancy, `B225_FRESH_ROOT_OWNED`, old onboarding schemas, independent authority semantics, source pins and accepted O0–O5/PPC0–PPC3 components remain intact. R1/R3/R4/R5/R6 remain open.

All five flags remain false: deployment approval, enrollment authorization, live readiness, activation and execution authority. `DEFER_ENROLLMENT` remains; the actual retry allowlist is empty. No real credentials/providers, appointments/enrollment, installed private-state access, deployment, activation or mission execution occurred.
