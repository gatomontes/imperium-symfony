# PPC4 public reproduction runbook

1. Verify the separate outer SHA-256 and run the packet's independent `verify-public-review.py` against the ZIP and an existing repository containing the exact prerequisite `816f0ffd14d6cc4d2c5d2c4605ec306aecffbf44`. The script checks nested membership, modes, bytes/hashes, source Git blobs, identities, exact diffs and the bundle prerequisite. Import into a disposable repository/worktree; preserve earlier checkouts and installed applications.
2. Read the report, A–F proposal, retained approval and candidate successor record. Verify the proposal SHA-256 from Git blob bytes: `9c8a8d8e4fde15ef6aabddfab83fa4a6deb569837ca3ead457d6b9f297bbf464`. The frozen proposed-state heading is historical; the separate approval supplies the approved disposition. The source candidate ledger is not receiving acceptance.
3. Check out the **tested commit** recorded in `identities.json`, install locked dependencies with the unchanged Composer command from `.github/workflows/phpunit.yml`, and record platform/tool versions and clean source identity. Use only disposable roots and freshly generated fixture keys. Do not copy installed state.
4. Run the changed tests listed in the test map. For public process evidence, set `PPC4_PUBLIC_EVIDENCE` to a new evidence directory while running `StorageSuccessorProcessTest`. It exports only sanitized process observations and selected public fixture JSON originals. Model authorization declarations remain explicitly synthetic and unverified. No ephemeral private keys are exported.
5. Run the unchanged workflow's credential-binding regression, all eight partitions via `tools/ci/phpunit_partitions.py run <0..7> 8 <result-directory>`, all 11 guards with `python -m unittest discover -s tools/ci -p 'test_*.py' -v`, then `phpunit_partitions.py verify <evidence> 8 <tested-tree>`. Retain native exits, UTC intervals, before/after source inventories, JUnit counts, warnings, failures and skips. The external Windows launcher only supplies PHP for the extensionless entry and strips Windows extended-path prefixes for PHP; worker selection and guards are unchanged.
6. Run on a host capable of symlink creation. This producer's Windows host proves live/dangling junction refusal but cannot create the symlink fixtures: one PHPUnit skip and the Python file-type guard's actual privilege error must not be represented as a pass. Fresh complete hosted CI and receiving source review are required before integration.
7. Compare tested-to-final: only documentary report/maps/runbook and the candidate record's tested identity may change. Original ledgers, proposal/approval, countdown, AtomicTransition, CI, dependencies and unrelated pins must remain byte-identical to preparation.

No runtime publication is authorized by this packet. All five operational flags stay false; `DEFER_ENROLLMENT` and the empty retry allowlist remain. MissingAssignmentEvidence, account/access/base/cognition ports, FRESH vacancy and `B225_FRESH_ROOT_OWNED` remain refusing. R1/R3/R4/R5/R6 remain open. Countdown is **5–7 before review**, potential **4–6 only after accepted complete R2 closure with no new scope**; this submission applies no decrement.
