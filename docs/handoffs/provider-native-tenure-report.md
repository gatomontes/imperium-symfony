# Receiving acceptance of the PPC3 component — countdown unchanged

The typed native synchronization component passed source review and complete Linux CI and is integrated through [PR #818](https://github.com/gatomontes/imperium-symfony/pull/818), merge `fe65bd91efa3eb5cc5bc07a1ac48658f12e46edf`. Linux: 3,767 tests / 64,310 assertions / four skips, 614 files with exact case coverage and all 11 guards passed. [Receiving review](../reviews/provider-native-tenure-review.md), [integration record](../provider-native-tenure-reviewed-integration.json).

The original report below remains producer evidence. Its **PARTIAL_NATIVE_TENURE_SYNCHRONIZATION** disposition remains: generic storage is still outside Formation and R2 is open. Countdown **5–7 → 5–7, decrement 0**. Original Windows failures and limitations are retained.

---

# PPC3 local implementation report — R2 remains open

Disposition: **PARTIAL_NATIVE_TENURE_SYNCHRONIZATION**. The typed native producer set and a real model-bound Formation current consumer now share the Formation owner. Direct generic storage remains an interfering path because the attempted generic primitive change conflicts with a retained source pin. This submission does not claim a whole supported synchronized deployment set.

**Countdown before receiving review: 5–7 batches to the first separately authorized live Courtyard interview.** Selected outcome: R2. Accepted outcome evidence from this submission: none. Revised range: **5–7**, no decrement. A potential **4–6** is conditional on later receiving acceptance of complete R2 with no new scope; neither partial acceptance nor a green local compatibility run qualifies. Canonical countdown JSON/Markdown are byte-identical to the preparation branch. Full mission execution is outside this milestone.

## Source and authorization

| Identity | Commit | Tree |
| --- | --- | --- |
| Accepted main baseline | `cd3a46ba45d0b89208c68e39225a0f8e08ca6de6` | `451140919d570d196384b5e0802ac076c67ba579` |
| Actual clean start, matching `origin/codex/provider-native-tenure-preparation` | `eccd068322e06dd0449586d77703215faf18b12b` | `047aa833c54da20ca17be139e57e2de823903dc6` |
| Final committed executable candidate, before final validation | `6ecf1e4417453a35f9781b21fbc16632965febec` | `7cef4598673365b70e41a83af72fc4ba36851de8` |
| Final documentation commit | Exact identity in packet `identities.json` | Verified source ZIP/manifest; tested-to-final is documentation only |

Branch: `codex/provider-native-tenure`. The working tree had no tracked or untracked changes at entry. No applicable AGENTS.md was found in this worktree or its filesystem ancestors. Earlier worktrees and installed applications were preserved. Validation uses task-owned isolated worktrees; the final run reuses only this task's stopped disposable validation checkouts.

Read the full selected campaign, countdown and JSON, PPC2 receiving review/writer inventory, approved A–F and retained approval record. The owner’s “proceed. remember countdown” plus approved A–F supplied the bounded offline scope; no additional blanket permission was requested. The historical proposal hash/approval bytes were not rewritten. No new institutional/model jurisdiction was selected.

## Implemented component

`FormationOwnerFrame` issues a private live capability only inside the unchanged Formation AtomicTransition. The journal passes it through explicit internal seams. Native acquisition now orders Formation before native and bootstrap; nested native owners retain the actual live capability. Cross-root nesting refuses before acquiring a second owner, and V0 passes the held owner into required installation and operationalization. The direct installer **already** held Formation indirectly before PPC3; this work resolves the surrounding inversion, not the corrected preparation finding.

Direct/required root installation, V0/StateStore/MasterMason, operationalization, Constable and Guildhall binding, native enrollment/protocol/journal changes and independent Formation appointments use the typed boundary. Registry changes are visible to the new current consumer even when raw occupancy is untouched. Any native registry, bootstrap state, unsupported same-Seat successor/cohort, incomplete lineage or ambiguous actor refuses instead of falling back to an old occupant.

The actual model-bound ingress, candidate validation and current Courtthane/Locksmith owner methods consume the new verifier. Positive fixtures use actual native package production, scoped institutional Ed25519 signatures, complete findings/approval/qualification and independent exact appointments. Both target consumers reconstruct in new processes. Model authorization/source-line fields remain **synthetic unverified declarations**, separate from those real institutional signatures.

New package completion custody records every expected installation. Current verification requires the completion record and every exact installation/placement in that package. Four process-interruption points refuse, including after all placements but before completion. This is fail-closed package verification, not multi-file atomicity. Pending packages do not automatically recover, and old unknown packages gain no completion marker through replay. Existing original schemas/bytes and completed appointment facts retain their historical meaning without renewing current tenure.

Detailed artifacts: [writer/caller/lock-order map](../provider-native-tenure-writers.md), [implementation and compatibility matrix](../provider-native-tenure-implementation.md), [test map](../provider-native-tenure-test-map.md), [future public-input runbook](../provider-native-tenure-public-input-runbook.md).

## Exact open paths and pinned conflict

1. **`ImmutableRecordStore::put` directly targeting institutional storage remains outside Formation.** The [original snapshot-consistency reading ledger](../native-inspection-snapshot-consistency-reading-ledger-v1.json) pins its normalized SHA-256 to `51acaf101d0c14301cd2d29f19581473ff93fdc82179fa34225566282f97ae7a`. The [terminal ledger](../native-inspection-snapshot-consistency-terminal-reading-ledger-v1.json) states “future unlisted changes still fail closed” and lists no successor for this primitive. The unchanged `NativeInspectionSnapshotConsistencyPreparationBatch0Test::testLedgerPinsTheRequiredDocumentsAndCompleteTracingSources` rejected the attempted edit. Both generic primitives were restored; no pin, ledger or existing test was weakened.
2. **Direct `MutableStateStore::compareAndSwap` / `compareAndSwapGuarded` calls remain outside Formation too.** The final test successfully acquires Formation independently from inside the generic guard, proving that open path. A completed generic retirement makes the current consumer refuse, but this does not prove serialization while a generic write races the consumer. Targets include installation/package custody, Office occupancy, registry, bootstrap and Formation storage where accepted by the primitive's path validator. Already-owned typed callers retain their outer owner; bare generic calls do not.
3. **The complete local acceptance gate is not green on this Windows host:** the unchanged file-type guard cannot create its symlink (`WinError 1314`). That is an error, not a pass or skip. Receiving source review and fresh complete hosted CI remain required, and cannot by themselves erase the generic-writer source gap.

The smallest proposed further boundary is a reviewed versioned generic-storage target/owner contract that routes or refuses all reserved institutional targets before the primitive's own lock while preserving other old targets. It must cover the old generic entry paths and use the already-held owner seam; an unused wrapper is insufficient. Changing the pinned primitive requires its actual successor review, not recalculating a ledger hash here. No such contract change or additional jurisdiction is invented in this submission.

## Validation and diagnostics

Final committed-candidate targeted verification: **22 tests / 623 assertions, exit 0**, including all 17 new tenure cases and the unchanged five-test pin regression. UTC interval: `2026-09-14T00:37:30.6320374Z` → `2026-09-14T00:37:53.8291624Z`; JUnit time 22.394 seconds. Earlier targeted and raw process-proof runs remain separately retained.

Final unchanged eight-partition aggregate: **3,767 tests / 64,326 assertions / zero errors / zero failures / 0 skips**, **4 reported warnings**, **614 files**, every enumerated case exactly once. All eight native exits and the unchanged aggregate verification exit are **0**. Every partition has identical before/after source digest `ebbfeae4d1aef76f085077aa9c7bec74b04d5c9e1ac6f9c3bb282d0961b38230`.

Overall retained gate interval: `2026-09-14T00:34:15.802968+00:00` → `2026-09-14T01:04:23.910414+00:00`. Longest JUnit partition: **1799.737655 seconds**; its native launcher interval was `2026-09-14T00:34:16.733522+00:00` → `2026-09-14T01:04:23.515647+00:00`. That launcher interval exceeds 30 minutes including enumeration/source checks; this Windows run does not establish the hosted Ubuntu 30-minute job budget. The workflow limit is unchanged and fresh complete hosted CI remains required.

All **11 unchanged Python guards** were attempted against the final executable identity: **10 passed, one error, zero skips**; native exit **1**. The retained traceback is `WinError 1314` from `SourceBindingTest::test_changed_file_type_fails`. Therefore the overall local acceptance gate is not fully green even though complete PHPUnit/source/exact-case verification passed. Neither that platform limitation nor the retained generic-writer source gap is waived.

Windows 11 build 10.0.26200; PHP 8.4.14; PHPUnit 13.3.0; Python 3.12.10; PowerShell 7.6.5; Composer 2.8.12. Locked dependency installation used 82 installs, zero updates/removals; normal Composer scripts ran only in this isolated worktree. Validation checkouts use copies of those locked dependencies. No dependency/CI/pin change was made.

The external Windows launcher only prefixes the extensionless PHPUnit command with PHP and normalizes Windows extended path prefixes passed to PHP. Partition enumeration/selection, source verification and exact-case aggregate checks are the unchanged repository scripts. All native logs, JUnit files, selections, UTC launch/end records and source identities are retained. Exact process barrier transcripts and native child exits are in `process-proof-final/events.json`; authentic public originals for both targets are in `public-originals-final/` in the evidence ZIP.

Five earlier gate attempts are retained as diagnostics, not complete successful runs. Four were interrupted for source-review corrections: unknown bootstrap fallback; stale model-bound ingress; unknown successor status; and cross-root native nesting. The fifth produced a real partition-4 failure at `c1c0e72b`: **397 tests / 5,380 assertions / one failure / one warning / native exit 1**, exposing the pinned ImmutableRecordStore conflict. Its completed partition and the remaining interrupted logs are retained. Interrupted native exits are not inferred as success. Initial read/search command errors and the first unsuccessful coordinator lookup were diagnostic invocation errors, not successful validation or product failures.

## Public packet and stop boundary

`imperium-ppc3-public-review.zip` and its separate `.zip.sha256` contain the final committed source ZIP, complete public synthetic evidence ZIP, path/mode/byte/SHA-256 manifests, source Git blobs, tested/final identities, full-index binary base-to-tested/tested-to-final/complete diffs, bounded Git bundle with exact preparation prerequisite, native bundle verification, validation summary and independent archive membership/hash checks. Windows Git omits ZIP Unix mode attributes, so the builder preserves modes from the exact Git tree and verifies every source blob before independent archive verification. Exact final commit/tree and outer checksum are recorded outside this document to avoid a self-referential Git identity.

Current designation events, independent native-to-O4 model-binding correspondence, AssignmentEvidence and combined FRESH establishment are outside completion claims. `MissingAssignmentEvidence` and account/access/base/cognition production ports still refuse. Root vacancy, `B225_FRESH_ROOT_OWNED`, old onboarding schemas, source pins, service wiring, accepted O0–O5/PPC0/PPC1/PPC2 behavior and approved provider/model/fee policy are preserved.

All five operational flags remain false: deployment approval, enrollment authorization, live readiness, activation and execution authority. `DEFER_ENROLLMENT` remains and the actual retry allowlist is empty. No real credentials/provider access, installed private state, deployment, enrollment, actual appointments, activation or mission execution was used. Stop at local commits for receiving source review and fresh complete hosted CI; no runtime publication or integration is performed.
