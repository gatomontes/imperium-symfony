# PPC10 local component and source-boundary handoff

PPC10 is **incomplete**. This candidate implements additive C1 syntax/authentication and a native C2 fitness owner with selected component proofs. It does not implement staged admission/migration, assessments, assignment application, replacement or protected use. It is not a full R3 submission or acceptance. The exact C1/C2 approval remains in force; no repeat approval is requested.

## Disposition and source boundary

The approved edit inventory excludes `src/Imperium/Runtime/Citadel/Formation/SharedExposure.php`. Its current schema dispatch returns before shared accounting for v5. Existing reservation and custody callers invoke this gate, including paths without model settings. Activating the required v5 ledger without covering that gate would leave the required cumulative exposure invariant unestablished. The extension is held at that boundary, under the user's express instruction to stop an extension needing a broader protected edit and continue independent work.

`../provider-staged-assignment-source-incompatibility.md` gives the concrete proposed narrow dispatch change and rejected alternatives. `../provider-staged-assignment-exposure-source-gap.json` retains exact blob, byte and SHA-256 evidence. The diagnostic changes only an unpersisted value passed to the actual pure gate; it does not establish an accepted v5 runtime, owner race or exploit. No unauthorized existing source edit was made.

The new source requirement splits the original implementation/receiving batch. Remaining estimate is explicitly revised **3-4 to 4-5**, decrement **zero**. R1/R2/R4 remain accepted and receive no repeat credit. R3 remains open, R5 production ports remain open, and R6 remains separately authorized and deferred. All five flags (`deployment_approved`, `enrollment_authorized`, `live_ready`, `activation`, `execution_authority`) are **false**. Disposition: **DEFER_ENROLLMENT**. Actual retry allowlist: `[]`.

## Implemented components

Five additive runtime classes provide the closed staged Operator domain, pure scope and application terms/receipt decoding, exact C2 contract vocabulary and a native fitness owner. C2 preparation verifies the complete original candidate chain. Current institutional actors receive separate exact purpose-limited delegations; seven signed judgments per Seat supplement existing qualification and model correspondence. The native owner retains original envelopes, bounded evidence and terminal withdrawals using the unchanged FormationJournal owner/publication mechanism. Current verification requires the complete PASS set. Exact completed replay recognizes history without republishing or implying present usability.

Both Seats' component tests build disposable roots through actual FRESH founding, completed R4, native Profile/model chains, designation and independent appointment. Private signing keys remain in memory. Eight practice results per Seat are explicitly **synthetic**. Signatures and structural checks establish mechanics and attribution, not external semantic truth. The tests call the fitness verifier directly; they do not claim assignment or protected delivery.

Selected independently signed alternate negatives cover wrong Seat, wrong role and UNKNOWN exercise, with unchanged heads and valid controls. Deliberate signature corruption is identified separately. Additional assertions cover missing coverage, fixed-store identity, expired owner frame, historical replay and terminal withdrawal. Pure contract tests cover separate-domain authentication/rejection, exact approved vocabulary and selected size/shape limits. These are not the full requested adversarial matrix.

## Open implementation and proof

No first scope admission or v4-to-v5 atomic migration exists. No new W1/W2/W3 frozen input, custody, settlement or selection producer exists. Neither initial application mode, either Seat replacement, complete settings-pair publication nor fresh-process protected transport use is implemented. Full integrated history limits, original corruption/expiry/tenure matrices, same-root aliases, reset resistance, cumulative-budget maximum+1, concurrent writers, withdrawal/designation/appointment orderings and crash boundaries remain open. Pure decoders must not be wired as admission or usability verifiers.

`../provider-staged-assignment-contracts.md` documents schemas and writer/authority boundaries. `../provider-staged-assignment-public-input-runbook.md` maps every required proof category to reached evidence and remaining work. All pre-existing tracked files, original approval/proposal/source pins, founding/R4 owners, fixtures, persistence writers, Composition, dependencies, CI and guards remain byte-identical.

## Source identities

- Clean published preparation: `bc91c7d634d0c7824f8f4621e96bb96995462f0e`, tree `5a2f475b981cd58c4467a862b396d57981b35c29`; sole bounded bundle prerequisite.
- Accepted main ancestor: `6cadc3125db6d9ccdbec9d40264033cd73a94de1`, tree `9999b032b54123c80b53397a472eb63a6b6713a4`.
- Runtime component commit: `8ba0d4f500ff3a95c7ec536b9616245d69dc1f53`, tree `e6ad62ae35e9e335362f9c1b7a1df846403396d8`.
- Full-suite tested candidate: `045b6f1142349193144fb68b244378bb54875a16`, tree `1d2e17936558ae476079523816f959a10c33226f`. The intervening changes add public alternate-envelope retention in the new test and correct its syntax; runtime bytes do not change. The intermediate c398e6ef candidate failed discovery and is retained as failed evidence.

All 82 immutable inventory observations were compared to actual Git blob bytes, sizes, SHA-256 and object IDs without normalization. The approved proposal remains SHA-256 `bd817ea572c259874887375389ed3ecea0972ecf6ff0bb22b1cf72e75d06844d`. Final identity is in the packet's `identities.json`, outside this committed document to avoid a self-referential commit hash. `tested-to-final.diff` is documentation only.

## Validation

Locked dependencies were installed with no scripts or updates. PHP 8.4.14 / PHPUnit 13.3.0 ran on Windows; Python guards ran with Python 3.12. The unchanged partition runner was used with only an external Windows launcher prefixing `php` for the extensionless executable. No test repartitioning, internal limit change or hosted-limit enlargement was made.

Targeted components: native exit 0, **15 tests / 128 assertions**, UTC start `2026-09-16T11:28:50.1853249Z`, end `2026-09-16T11:56:57.6821611Z`. That run loaded the earlier component/test version. The final runtime bytes are identical; the later test delta exports alternate envelopes and corrects a syntax error. A separate quick post-fix run passed **13 tests / 48 assertions**, native exit 0. Targeted before/after source digests were not captured; no exact-source-bound targeted gate is claimed. The fresh full suite binds the final executable candidate below.

The intermediate c398e6ef attempt failed during discovery (PHPUnit native 255, launcher native 1) due to an extra parenthesis in the new observation-export test. All eight tracebacks and prestep results are retained under `full-discovery-failure/`. The error was fixed in the final tested candidate before this fresh complete run. Earlier invalid-digest/contract diagnostics and the deliberately interrupted superseded native run are retained separately and do not count as passing gates.

Full-suite aggregate native exit: **0**. **642 files, 4107 exactly-once cases, 69616 assertions, 3 skips**, with zero failures/errors. Exact case/file coverage passed. All eight partitions retained matching before/after source digest `89fb1373728f9a0d6437e42d5fdddebd2ef3c95bc36c33a2623a8e14813afc49` against tree `1d2e17936558ae476079523816f959a10c33226f`.

| Partition | PHPUnit exit | Cases | Assertions | Skipped | Worker wall seconds | Within 1800 seconds |
|---|---:|---:|---:|---:|---:|---|
| 0 | 0 | 543 | 11133 | 3 | 3194.891 | False |
| 1 | 0 | 422 | 9202 | 0 | 3661.172 | False |
| 2 | 0 | 438 | 4417 | 0 | 2608.985 | False |
| 3 | 0 | 634 | 24828 | 0 | 2320.250 | False |
| 4 | 0 | 371 | 4088 | 0 | 581.047 | True |
| 5 | 0 | 410 | 3789 | 0 | 3726.844 | False |
| 6 | 0 | 539 | 3729 | 0 | 3860.469 | False |
| 7 | 0 | 750 | 8430 | 0 | 2172.328 | False |

Exact worker launch/end UTC intervals, native exits, selected files, complete case enumerations, JUnit and source-before/source-after digests are retained per partition in the evidence ZIP and summarized in `../provider-staged-assignment-validation.json`. The credential-binding prestep is recorded separately and excluded from aggregate counts. `config/reference.php` receives only the unchanged runner's existing generated-PHPDoc/token exception; its raw before/after hashes are retained.

Three existing symlink cases were skipped: the pending-revocation and completed-consumption datasets of `FreshEstablishmentResetClosureTest::testSymlinkRootSharesRevocationAndCannotResetConsumption`, and `ReservedInstitutionalStorageTest::testSymlinkAliasesAndDanglingTargetsRefuse`. JUnit marks these skipped without reason text; `skip-source-reasons.json` retains the exact committed source conditions/messages for hosts unable to create symlinks. These proofs remain unavailable on this host.

Partitions 4 and 5 reported three PHP warnings from unchanged crash-demonstration helpers reading `.git/HEAD` as a directory inside a Git worktree. Separate diagnostics reproduced them with native exits 0: two tests / 61 assertions in `warning-controls.log`, and one test / 18 assertions in `warning-terminal-control.log`. Exact file/line details are retained. Those diagnostic tests are excluded from full-suite counts. No protected helper or warning policy was changed.

All eleven Python guards ran: **10 passed, one error**, native exit **1**. `test_changed_file_type_fails` could not create its symlink: `[WinError 1314] A required privilege is not held by the client`. The full traceback and exact interval are retained. This is missing local evidence, not a passed or skipped guard; receiving Linux must run it.

Workers 0, 1, 2, 3, 5, 6, 7 exceeded the unchanged hosted 30-minute bound on this local Windows host; their completed local results are not hosted time-bound passes. Fresh complete hosted CI was not run and hosted acceptance is **false**. Local component and suite results do not close R3 or supply the missing integrated proofs.


## Public delivery and receiving

The public review ZIP contains committed source, synthetic observations and public original histories, full file/mode/byte/hash manifests, three exact binary/full-index diffs, a bounded Git bundle, identities, this report and an independent verifier. The separate outer checksum binds the delivered ZIP. Verification checks bundle prerequisites, exact source Git blobs/trees, unchanged baseline pins, manifests, diffs, exported journal links, positive fitness signatures, retained alternate-negative signatures and deliberately invalid signature controls. It does not authorize an import of public observations as accepted state, certify external semantics or close R3.

The evidence includes earlier unsuccessful diagnostics and the explicitly interrupted superseded run; they are not passed gates. No signing keys, installed/private runtime roots, account credentials, vendor/cache or unrelated untracked data are delivery inputs. No push, merge, integration, deployment or live operation was performed. Receiving Linux must run the missing guard proof and fresh complete hosted CI on the eventual complete candidate; historical PPC9 results and local component tests cannot substitute for that gate.

Imperium via solitaria est.
