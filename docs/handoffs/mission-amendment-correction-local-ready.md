# Mission Amendment Authority and Evidence Binding — local runner

Read completely:
- `docs/next-campaign-mission-amendment-correction.md`
- `docs/protected-mission-amendment-review.md`
- `contracts/mission-planning.md` and `contracts/mission-authorization.md`
- Applicable repository/ancestor AGENTS.md instructions.
- At the preserved candidate: AuthorityOwner, Ceremony, PublicTrust, Cli, inspection code,
  canonical assembly/review/derivation services, protected mission tests, prior audit/evidence
  ledger and Operator runbook. Read current flow/steps and record exact source revisions.

## Entry: preserve implementation, bring in current planning

Verify the local repository and origin gatomontes/imperium-symfony. Inspect full HEAD, branch,
status and remotes. Preserve unrelated dirty work; stop before switching if it cannot be isolated.

An initial read-only Git fetch is allowed. Verify the preserved branch
`codex/protected-mission-authority-operator-ceremony` points exactly to
`7deb84b9f687ecb09c93f568464a606f0a602a89`. Do not move/delete it.

Create a new local branch `codex/mission-amendment-authority-evidence-correction` from that
exact candidate commit. This is explicitly authorized corrective reuse, not acceptance into main.
If the new name already exists, inspect it and resume only if its lineage and work match;
never reset it.

Record the exact merged main commit containing this selection. Confirm the diff from
`0099c5f7e4008fe06cafc9c5dfa3458dc68f4db9` to that selection changes documentation only.
Merge that exact selection commit locally into the NEW corrective branch to acquire these
instructions and current steps/flow. Do not merge the candidate into main or change local main.
Resolve documentation overlap by making the amendment selection current and preserving candidate
audit history below it. Stop on unexpected code conflicts or intervening runtime changes.
Record both parents and the integration commit. Do not run the superseded old campaign entrypoint.

## Work

Complete Batches 0–3 from the campaign with separate commits and focused tests.
First reproduce unsigned amendment interference and cross-version inspection reuse in disposable
roots through the public protocol. Then implement inert proposals, signed predecessor-bound
activation, exact authorization-generation state/evidence binding and historical status.
Prove legitimate fresh amendment execution as well as refusal; do not disable amendments wholesale.

Keep all prior branch/source/evidence history. Preserve the old 2665 / 52515 result as historical.
Retain useful tests and explain each changed expectation. Do not carry old runtime state or keys
into new tests or erase a real journal. Run full PHPUnit after Batch 2 and at the terminal audit.
Commit Batch 3 test/code work before auditing its exact head; label later documentation commits.

Update this campaign's checklist, current flow, tested PowerShell runbook and sanitized evidence.
A missing real deployment is a retained limitation, not permission to manufacture isolation proof.

## Stop and handoff

Stop locally with
`MISSION_AMENDMENT_CORRECTION_LOCAL_COMPLETE_PENDING_INDEPENDENT_REVIEW`
only when both findings and required regressions are satisfied.
Retain `DEPLOYMENT_ISOLATION_UNPROVED_OPERATOR_SETUP_REQUIRED`.

Return implementation/planning/integration bases, batch commits, tested SHA and final HEAD/tree,
worktree status, test counts/skips/commands, reproduction and correction evidence, changed-test
map, per-version status/receipt examples and remaining limitations.

No push, PR, main merge/update, branch deletion, installer execution, real trust/key handling,
real mission, provider, credential or external-system action. Initial Git fetch and the local
documentation integration above are the only publication/network-related exceptions.
If blocked, report the concrete unresolved condition without claiming completion.

In imperium fidimus.
