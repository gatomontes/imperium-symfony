# Credential boundary Batch 14 complete — Batch 15 ready

## Proved boundary

Guildhall cognition is migrated across four separately typed native authority families:

1. disciplinary fit;
2. composition;
3. boundary challenge; and
4. Guildmaster synthesis.

The exact accepted commission identity now reaches the cognition gateway. The Guildhall resolver rereads the intact acceptance, planning commission, mission-plan turn, atomic four-Seat binding cohort, exact occupant generation, and stage-specific predecessor checkpoint before normalizing any authority.

Each stage binds its own Seat, purpose, immutable input digest, single-use consumption state, and predecessor chain. Composition cannot open without disciplinary fit; boundary challenge cannot open without disciplinary fit and composition; Guildmaster synthesis cannot open without all three committee records.

The gateway uses the shared governance claim-bound Clavium invoker. It no longer receives any Symfony agent. Provider journal reservation, credential resolution, stable idempotency, sealed response handling, replay refusal, and unknown-outcome refusal remain owned by the common substrate.

## Removed surface

The following direct definitions and injections are removed:

- `guildhall_disciplinary_fit`;
- `guildhall_composition`;
- `guildhall_boundary_challenge`; and
- `guildmaster`.

The executable inventory is now **2**:

- Curia `seneschal`; and
- La Cortine `sortie`.

Both credential-bearing platforms remain. The system-wide credential gate remains open.

## Preserved boundaries

Batch 14 does not reopen or change Senate work. It does not migrate Curia, La Cortine, or either global credential-bearing platform. Guildhall's final profession determination still opens only Garrison inventory inquiry authority and grants no final personnel disposition, spawning, Seat-binding, or execution authority.

## Focused verification

The operator runs PHP locally:

```bash
php vendor/bin/phpunit \
  tests/Imperium/Runtime/GuildhallDeliberationServiceTest.php \
  tests/Imperium/Runtime/GuildhallGovernanceCognitionBoundaryTest.php \
  tests/Imperium/Runtime/CredentialBoundaryAgentInventoryTest.php
```

## Next bounded lifecycle

Batch 15 migrates Curia `seneschal` only. It must bind cognition to the exact audience/deliberation authority and current Seneschal occupancy, preserve the distinction between assessment and Imperator authorization, remove only the direct `seneschal` definition, and reduce inventory from **2** to **1**.

Do not migrate La Cortine or remove either credential-bearing platform in Batch 15.
