# Native Integration Remediation Batch 1 dependency handoff

`NATIVE_INTEGRATION_BATCH_1_BLOCKED_ROOT_TRUST_POLICY_REQUIRED`

The operator authorized completing the campaign and PHPUnit after every batch.
Batch 1 began from Preparation Batch 0 commit `81e892b`; it is not complete.
Seven planned stages remain unfinished. Batches 2 through 7 were not started.

The existing constitution contract covers initial or missing principals. Its
evidence-store Root references do not authenticate an act widening an existing
principal. Ordinary lifecycle forbids scope change, and operationalization closes
the founding personnel window. Choosing a new signing identity as Operator Root
would introduce a trust policy that the existing source corridor does not define.

The concrete proposal and reading ledger are in
`docs/executable-atomic-transition-native-integration-remediation-implementation-v1.md`.
Approve that narrowly scoped post-operationalization public-signature trust route
for implementation, or identify the authoritative existing Root ingress. This
decision does not authorize live key provisioning, signing, principals or grants.

The `senior-symfony-backend-engineer` skill requires escalation for a missing
product/policy decision. This is the reason for pausing, not an obsolete
Preparation Batch 0 authorization limit. Speculative runtime drafts were removed.
No runtime or service configuration changes remain.

Validation: the full `php vendor/bin/phpunit tests` suite passed **1854 tests,
44250 assertions** on PHP 8.4.14 / PHPUnit 13.3.0 (2 minutes). The focused
campaign/preparation/dependency checks also passed; lint and diff whitespace
checks passed. Existing pure validators reject resealed scope widening and route
relabeling. These results do not establish the absent native trust route or native
executable completion. No failures required runtime fixes.

`EXECUTABLE_ATOMIC_TRANSITION_TERMINAL_AUDIT_REFUSED_NATIVE_INTEGRATION_ABSENT`
remains controlling. `BOUND_INACTIVE`, native v3 `NOT_IMPLEMENTED` and
`UNKNOWN_REPLAY_PROHIBITED` remain. No live authority, transition, credential,
capability, provider, external I/O, effect, retry, Iron Gate or Lazaretto action
was performed. Resume Batch 1 after resolving the exact Root trust route; finish
and test each batch before advancing. Batch 7 remains a separate audit from
clean merged Batch 6 main.
