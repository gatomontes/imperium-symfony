# Provider Binding Activation Principal Provenance Remediation Batch 4 complete

## Result

The future-instance producer now requires one intact, unexpired operator-root
`FUTURE_INSTANCE_ROOT_ESTABLISHMENT` authority before operationalization seals. Under one locked
transition it consumes that exact authority and commits one immutable generation-one
`PENDING_ACTIVATION` Imperator principal version. Exact replay converges; route, generation, expiry,
source integrity and sealed-operationalization violations fail closed.

The producer neither issues the source authority nor uses the founding-personnel installer. It
creates no active principal, caller authority, credential material, provider invocation or external
effect. Provider Execution Assurance remains paused.

## Authorized continuation

Only Batch 5 is authorized: implement the existing-instance remediation producer against one intact
operationalization seal and one exact operator-originated `EXISTING_INSTANCE_REMEDIATION` authority.
It may constitute only a provably absent principal and must not reopen or alter founding personnel.

Batch 5 may not activate a principal, issue caller authority, implement lifecycle transitions or a
current-state index, reconsider corridor disposition, activate a binding, handle a credential,
invoke a provider, perform external I/O, or open Iron Gate or Lazaretto.
