Implement PPC4, the remaining generic institutional storage part of countdown outcome R2, after verifying owner approval of the exact docs/provider-storage-successor-contract-v1.md clauses A–F. Countdown is 5–7 batches to the first separately authorized bounded live Courtyard interview; full mission execution is later.

Read applicable AGENTS.md, docs/next-campaign-provider-storage-successor.md, the entire successor contract, docs/handoffs/provider-storage-successor-approval.json, docs/reviews/provider-native-tenure-review.md, docs/provider-native-tenure-writers.md and both countdown records. The owner has approved the exact A–F proposal: the record retains “approved” in direct response to “Do you approve clauses A–F for bounded offline implementation?” after PR #820. Verify approved_proposal_sha256 against the exact committed proposal bytes (Git blob bytes if checkout newline conversion applies), preserve the reference and implement without asking for approval again. The proposal's original pending-state text is frozen historical content; the separate approval record supplies its current approved disposition.

Start in the isolated worktree from origin/codex/provider-storage-successor-ready. Baseline accepted main is 96d05d839320909d140d50337ccd16fe68dc1604, tree 707b94f58beb80d5180790a5aa58843f99fe55c0. PR #820 published the proposal; the ready branch additionally records approval. Record the actual ready commit/tree and clean status. Preserve earlier worktrees and installed applications.

Close the bypass in ImmutableRecordStore::put, MutableStateStore::compareAndSwap and compareAndSwapGuarded themselves: reserved destinations refuse before the primitive lock, guard or any filesystem effect through the old APIs. Add explicit counterparts consuming the actual live same-root FormationOwnerFrame and migrate legitimate typed callers at their owner boundary. Keep Formation → native → bootstrap/target storage; no automatic upward acquisition from generic callbacks, fake held-lock booleans or reentrant Formation. Follow A–F for target classes, actual validator aliases, ordinary-target compatibility and fixed-topology threat limits.

Preserve both historical snapshot reading ledgers and AtomicTransition exactly. Use the specifically approved new successor record and minimal recognition branch in the named pin test, retaining its five cases and adding meaningful wrong-predecessor/unlisted-path/hash/approval/duplicate refusal proof. Record exact predecessor and candidate hashes and the proposal approval; a candidate ledger is not receiving acceptance. Stop and retain evidence for an independently conflicting pin beyond the approved successor scope. Never rewrite historical hashes to green the suite.

Inventory all reachable reserved generic callers and migrate them; an unused wrapper cannot close R2. Prove all three old entries refuse with no effects, each reserved class and relevant aliases, actual positive native producer/current consumers for both Seats, both process race orderings, lower-lock guard refusal, wrong/expired owner refusal, interruption/exception lock release, new-process reconstruction and ordinary immutable/CAS compatibility. Preserve the old PPC3 open-gap evidence while updating its executable expected-open regression to the now-required refusal. Use controlled process barriers, bounded timeouts and native exits, not elapsed sleeping alone.

Keep current assignment evidence unavailable, FRESH/root/schema fences intact and R1/R3/R4/R5/R6 explicitly open. Do not add model approval, designation authority, actual appointment/enrollment, providers/accounts, installed private state, deployment, activation or execution. Use disposable roots and generated ephemeral keys; public institutional originals are distinct from synthetic unverified model authorization. All five flags stay false, DEFER_ENROLLMENT remains and the actual retry allowlist stays empty. Do not spawn additional agents unless the owner separately requests them.

Commit executable code before final validation. Run the unchanged full eight-partition PHPUnit/source/exact-case coverage gate and all 11 Python guards; retain counts, platform/tools, UTC timing, exits, skips, warnings, failures and before/after source identities. No CI/guard/dependency weakening. PPC3 CI is historical. Return docs/handoffs/provider-storage-successor-report.md and imperium-ppc4-public-review.zip plus its separate SHA-256 file with all campaign deliverables. Stop at local commits for source review and fresh hosted CI before integration. Show 5–7 before review; propose 4–6 only for accepted complete R2 with no new scope. Never decrement on preparation, submission, a partial component or local green tests alone.
