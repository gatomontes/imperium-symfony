# O3-B0 DeepSeek adapter and API-key custody — local implementation report

Status: LOCAL_IMPLEMENTED_AND_VALIDATED; SOURCE_REVIEW_AND_FRESH_FULL_HOSTED_CI_PENDING.

Ad Imperium. The bounded dormant O3-B0 implementation is committed in `E:/htdocs/imperium-onboarding-o3-b0`, branch `codex/provider-onboarding-o3-b0`. This is implementation and local validation, not independent acceptance, integration, account access or live eligibility. No push or merge was performed. No concrete in-scope implementation blocker remains; the requested next gates are source review and fresh full hosted CI.

## Identity and scope

Clean entry was `3a9642c8e26e0fc19d87267cb664995a2860a47e`, tree `0d12c3c74d657494198ea1f6a6b3792ae1af74bd`, including O3-B0 preparation and O2 integration `9b50d53362e44af1d628034ad6153985b9a24d1c`. The original main worktree was dirty and was not reused or reset; the already isolated O3 worktree was clean. Other worktrees were preserved.

The exact final tested PHP commit is `8ba64fd1da6de5123de50939de4dbb3195da8b98`, tree `a3dfb7e8c4080adbdf0726e2543f0603cb2954fb`. Initial implementation is `80ad772b`; `b7c41952` corrects native proxy handling and root dot segments; the tested commit closes escaped credential echoes and strict integer configuration. Final commit/tree and documentation-only post-test changes are recorded in the packet's `identities.json`, avoiding a self-referential report hash.

All runtime additions are in `src/Imperium/Runtime/Onboarding/DeepSeek`, with Symfony Exclude attributes. Existing O2 ports, R1–R3, original reviewer cases, tests/fixtures, approved originals, source pins, FormationJournal, SessionExposure, legacy adapters, service configuration and dependency locks are unchanged. No narrow O2 seam edit was necessary. The inventory selection passes unchanged: this facade delegates authority consumption to the existing ledger and adds no mechanical authority-consumption candidate.

## Implemented behavior

- `AccessAdapter` derives the exact GET operation from a constructor-fixed typed grant. It re-resolves the real retained proposed terms, grant, executor, configuration, credential binding and account original under O2's owning transaction. Missing production account/zero-fee evidence refuses. The access test verifier pins a purpose-built synthetic account original signed and admitted through B0; generic fixture grant text is not treated as a production schema.
- `Runtime` keeps credential and response ports private and exposes only advance/reconcile. Actual reserved claim, immutable operation, current source and credential generation are rechecked through the original coordinator. Process-local identity capabilities are one-use, non-cloneable/non-serializable and cannot be regenerated by replay. Key retrieval and HTTP occur outside the journal lock; duplicate or absent callbacks cannot fabricate settlement.
- `Wire`, `TokenEvidence` and `Tariff` implement the exact approved Flash/Pro JSON serialization and typed preflight components. No character estimator, public pricing snapshot or absent meter becomes evidence. Micro-USD arithmetic checks overflow and uses explicit per-meter ceiling rounding. The private GET/POST exchange uses exact fixed destinations, no redirects or retry decorators, a total monotonic deadline and incremental response-byte limits. Native proxy bypass is verified without making a network request.
- `ProviderResponse` rejects duplicate keys, malformed/truncated bytes, unsupported extensions, model/fingerprint/finish conflicts and incomplete or inconsistent usage. It preserves raw bytes. Listing uses an explicitly local observation identity because GET supplies no provider-issued response ID. Trusted dispatch binds the exact operation digest; provider echoes do not establish attribution. Cognition reasoning is included within completion, not charged twice.
- `EnvelopeStore` retains the unchanged closed O2 envelope by complete claim identity. Atomic publication, replay/conflict handling, separate-process contention and interruption are tested. Dot segments, symlinks and Windows junctions refuse. Recovery uses original journal metadata and the retained envelope only, with no credential issue or redispatch. Failed or uncertain dispatch retains O2's maximum exposure and source fence.

The initial internal-shape note was written before runtime implementation. Its final refinement section documents the concrete implementation and the inspected legacy broker/storage mismatch. Packet paths below are relative to the extracted review packet.

## Fresh local validation

PHP **8.4.14**, PHPUnit **13.3.0**, Composer **2.8.12**, locked dependencies. The final dependency dry run reports nothing to install/update/remove. Final PHPUnit processes disable network functions and URL file access as recorded in `final-commands.json`. All effects use mock HTTP, public synthetic authority and temporary roots.

| Final selection | Tests | Assertions | Result |
| --- | ---: | ---: | --- |
| Original four O2 reviewer regressions | 4 | 10 | PASS |
| New O3 components, actual access integration and storage | 68 | 304 | PASS |
| Existing O2-B1/corrections and four B0 suites | 211 | 952 | PASS |
| FC custody/process, mission/correction and native protocol | 140 | 760 | PASS |
| O1 selection/response/base and three source-pin suites | 380 | 1,519 | PASS |
| Transactional coverage and frozen inventory tripwires | 16 | 3,996 | PASS |
| **Total selected local runtime checks** | **819** | **7,541** | **PASS** |

All 16 changed/new PHP files linted, non-debug kernel boot passed, and all **86** Python specification checks passed. Those Python checks remain static examples, not PHP, concurrency or crash evidence. Final selected logs report no skips or warnings. These are selected local runs; no new full hosted CI or full-suite result is claimed.

Every one of the **1,816 tracked project PHP files** was captured before the final run, compared byte-for-byte with its tested Git blob and checked again afterward. `tested-php-source.zip` and `tested-php-hashes.json` retain the exact streams and identities. Third-party PHP is not duplicated in this snapshot; locked dependency identities are recorded separately. The package compares every entry-tracked original against final Git and checkout bytes without exclusions.

## Campaign proof mapping

| Campaign area | Concrete executed evidence |
| --- | --- |
| Exact requests and evidence refusal | DeepSeekComponentsTest checks canonical GET/POST options and POST bytes, named aliases, integer configuration, unsupported fields, missing/mismatched token evidence, tariff expiry/fees and overflow. A private POST mock component test creates no authority state. The native configuration test verifies the effective direct stream context without network I/O. |
| Real access authority | DeepSeekFixture uses actual B0 signing/admission and O2 registration/configuration/evidence/access progression, with a typed grant and pinned synthetic account source. DeepSeekAccessIntegrationTest reaches settlement through the new facade and checks exact retained operation/response attribution. |
| Custody and currentness | New tests cover policy and grant-admitting-act revocation, expiry, key rotation before and during custody, foreign/used capabilities, clone/serialization refusal, operation swap, duplicate/absent callbacks and zero unauthorized transport entries. Fresh unchanged B0/O2 suites retain their original signature, foreign-record, source-link and checkpoint requirements. |
| Response and resource failures | New tests cover invalid UTF-8, duplicate/escaped keys, truncation, byte overflow, unknown fields, model/fingerprint/finish/type/meter conflicts, arithmetic overflow, 301/401/402/429/500/503, exceptions and a keepalive/read crossing the total deadline. Original and JSON-escaped sentinel echoes refuse without retaining rewritten evidence. |
| Storage and recovery | New process tests race identical and contradictory retention and terminate before/after atomic publication. O2 integration tests interrupt after metadata and before/after envelope publication, then reconcile in a new facade without transport. Existing FC/O2 process selections retain their broader reservation/checkpoint/settlement crash tests. |
| Budget and replay | Fresh ProviderOnboardingSharedExposureTest and FC selections exercise the unchanged shared domain. New access tests check same-ID replay, changed-ID/expiry attempts after unknown outcome, retained maxima/fences and idempotent restart settlement. All four original R1–R3 requirements pass unchanged. |
| Scope | Public facade methods are checked; default missing evidence refuses; service wiring remains unchanged; all five flags remain false. Successful listing still cannot proceed through missing selection/founding/cognition producers. Sentinel exclusion is checked in retained state, envelope and captured public transport options. |

## Development evidence, separately retained

`evidence/development/` and `development-commands.json` preserve failed and superseded runs. Early failures included incorrect new-test exception expectations, duplicate synthetic terms IDs, Symfony's normalized default port, Windows junction detection/cleanup, an invalid attempted source-level revocation target and anonymous-object serialization behavior. They were resolved without changing original tests or assertions.

Two broader runs were intentionally interrupted and are explicitly marked incomplete: one after source inspection found that the locked Native client rejects an empty proxy string, and one to add JSON-escaped credential echo and strict integer-configuration guards. Neither contributes to the final totals. Their partial logs and tested identities are retained. The final complete run is on the exact tested commit above.

Composer's installation script regenerated `config/reference.php`; its original entry bytes were restored before the tested commit. Final non-debug kernel smoke leaves that original intact. No configuration or dependency-lock delta is delivered.

## Remaining boundaries and handoff

Production account/zero-fee evidence, an actual deployment-selected key source and genuine tokenizer/framing/tariff evidence are not supplied by this batch. Defaults refuse; public snapshots and structural TokenEvidence/Tariff objects are not authenticated authority. The real Augur/founding/holder bridge and positive O2 cognition journey remain O3-B1. POST is exercised only as a private transport/serializer/parser component. Source review and fresh full hosted CI remain required before integration.

The known-value secret guard covers literal and JSON-escaped echoes, not every possible transformation of sensitive data. Storage tests prove the tested process-interruption and contention boundaries, not power-loss durability or protection against arbitrary hostile code running as the storage owner. No remote cancellation, exactly-once provider execution or billing-after-timeout guarantee is claimed.

No real provider request, real credential use, installed-state operation, real enrollment, appointment, assignment, activation, OAuth or CLI work occurred. O3-B1/O4/O5 were not started. All five operational flags remain false; the actual retry allowlist is empty. DeepSeek/API key, FRESH, D2-A, P1–P9 and DEFER_ENROLLMENT remain unchanged.

The public delivery contains this report/instructions, canonical changed files, exact patch, incremental Git bundle, entry/tested/final identities, exact tested PHP archive/hashes, protected-original comparison, commands and complete final/development logs with exits. The inner review ZIP has a SHA-256; the outer ZIP intentionally does not. No raw private evidence, runtime state or recursive historical delivery archives are included.
