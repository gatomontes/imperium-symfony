# PPC7 — same-root FRESH institutional establishment report

Disposition: **PARTIAL_FRESH_INSTITUTIONAL_ESTABLISHMENT**. The local implementation demonstrates the real combined sequence and bounded native publication/recovery. The entire R4 acceptance gate is not met: a required Python guard errored on this host, receiving hosted validation remains outstanding, and the explicit evidence gaps below remain. No SAME_ROOT_FRESH_INSTITUTIONS_IMPLEMENTED_OFFLINE or R3 implementation claim is made.

## Approved scope and exact source

The owner replied exactly **approved** to **Do you approve PPC7 clauses A–F for bounded offline implementation?**, following preparation PR #831. The retained separate approval record is `docs/handoffs/provider-fresh-establishment-approval.json`. The unchanged proposal Git blob SHA-256 is `e32bfd849a96f380505c0486702124c4bb60a9d5b26a15e7a39e7ef30eed3d4e`. Its historical PROPOSED heading was not rewritten; the later separate approval governs. Approval was verified and was not requested again.

Baseline main: `ebf2c3dfebae300c8aacd084acb152bfd9915f71`, tree `69962e5f606170b2f1965db221bd30389208df0b`. Actual published approved start: `636990b919afe23c224e0802c6c54129bab9b3c9`, tree `d3817d27beeb7f8188aff144900a5b953b4e370c`. Work proceeded in unused isolated `E:/htdocs/imperium-ppc7`, branch `codex/provider-fresh-establishment-ppc7`. Prior worktrees, the original workspace and installed application were preserved.

Executable candidate: `839ff91687773b53fe147997d614d76dc2e793fd`, tree `8550dc73ad05d25fc706d52d5f5792c88faa9e35`. It was committed before this fresh full gate. The final commit adds only this report and the source review ledger. Its exact commit/tree are supplied in packet `identities.json` and checked against the bounded bundle, all three diffs and source manifest. A report cannot recursively embed the hash of its own containing commit. The standalone report is byte-identical to its committed copy. No executable candidate change followed validation.

## Implemented behavior

The selected route keeps actual native trust/admission/constitution/base/mapping/FRESH founding first. An explicitly initialized optional Formation extension then requires separate authenticated founding Operator and Formation-owner signatures over identical complete original terms. The new native domain and effect do not widen O2/O4 allowlists. Both independently enrolled trust chains are checked; neither role implies the other.

One durable reservation binds the exact nine OFFICER members, all artifacts, founding policy/constitution/command/completion/holder, canonical root and identities, trust fingerprints, expected head, validity, nonce, correlation and reason. Under the existing Formation then native owner order, the concrete writer revalidates current authority and holder, publishes exact deterministic placements, verifies the complete native package and then commits journal completion. These are separate publication points, not a cross-file atomic rename.

Closed versioned wrappers retain the complete original native installation and occupancy records. They expose no legacy seat/status/authority fields. The current Formation adapter reads the actual native originals only after the journal completion and every package file agree. A partial package or complete native package without journal completion cannot expose an authoritative subset. No second incumbent registry is introduced. Generic installation, v4 installation, operationalization, original FRESH vacancy/consumption, historical fixtures, NativeInstallationPackage recovery and pinned AtomicTransition/R2 sources retain their existing boundaries.

Recovery only completes the exact reserved package while current authorization and holder checks pass. Unknown/corrupt/conflicting native bytes or expired/revoked authority refuse with originals retained. Exact completed replay returns its historical receipt without publishing again or repairing deleted files. Unsupported native/bootstrap successors refuse current resolution. Detached/foreign owner frames cannot use the new writer seam.

The real founding history retains a completed access claim. Original PPC5/PPC6 initializers refuse any retained claim and remain byte-identical. The additive FreshInstitutionalPreparation adapter invokes the unchanged complete LedgerState validator, requires settled five-checkpoint custody and empty affected fences/sessions/reservations, preserves all claim bytes, and requires distinct exact existing PPC5/PPC6 initialization decisions. Establishment signatures grant none of those later competences. Its positive and affected/corrupt refusal tests are retained.

The normative provision, exact bounds/reference/state map and future public-input runbook are in `docs/provider-fresh-establishment-provision-v1.md`. Actual owner/reader treatment and the limits of the 234-file conservative predecessor inventory are in `docs/handoffs/provider-fresh-establishment-source-review.md`; exact predecessor and candidate SHA-256 values and selector lines accompany the evidence archive. Structural refusal evidence is not mislabeled as 234 end-to-end passing invocations.

## Real same-root evidence

The committed candidate's combined fixture uses generated in-memory keys and actual enrollment, signed admission, native ConstitutionSource, CommandLedger founding, separate establishment owners and current institutional resolver. External account/base/cognition material and the controlled test clock are explicitly synthetic. No synthetic parent or imported institutional witness supplies authority.

All nine institutions resolve in a fresh process. The same root produces both `courtyard.courtthane` and `clavium.locksmith` strict PPC5 model-bound Profile chains, PPC6 designations, independent appointments and factual mapping H sources. Mapping admission is false and assignment application is false. This demonstrates the R4 prerequisite's downstream use, not native assignment/application/settings delivery.

The independent public verifier checks 94 linked Formation frames, 77 distinct signatures within the exported history, 18 native placements, nine fresh-process actors and both permanent Seat chains. Process evidence contains 178 child records with UTC events/native exits; 178 have independently checked joint signatures against captured public trust. 0 final-candidate child records lack public trust. Every corrected child input retains its actual pre-process public Formation frame; that exported observation is never consumed as live authority. Public integrity verification grants no current or external authority.

The candidate includes eighty process-death points: before/after reservation and final journal renames, intent write, each of eighteen native temporary writes, each of eighteen placements, and package-completion rename. Restart uses fresh actual owners; exact authorized recovery and absence of partial authority are asserted. Public evidence covers 80 distinct operation/crash points, of which 80 have an independently signature-verified observation. An earlier eight-case supplemental repeat belongs to the superseded first candidate and is retained separately. It contributes no final-gate counts or authority claim.

Two-process reservation competition, Formation decision revocation, native public trust enrollment, generic installation and operationalization compete in both real lock orders, with an actual nonblocking flock probe proving the fence is held. The report's full-gate totals below, not this case inventory alone, determine which executions passed.

## Full committed-candidate validation

Eight fresh detached worktrees ran the unchanged partition script and exact-case/source aggregate. Gate interval: 2026-09-15T10:57:25.126923+00:00 through 2026-09-15T11:46:21.780465+00:00. Aggregate native exit: **0**. Counts: **4062 tests, 68834 assertions, 0 errors, 0 failures, 1 explicitly skipped test**, and 4 reported warnings. Exact-case comparison: expected 4062, observed 4062, missing 0, unexpected 0, duplicate identities 0.

| Partition | Native exit | Tests | Assertions | Errors/failures | Skips | Wall seconds |
| --- | --- | --- | --- | --- | --- | --- |
| 0 | 0 | 488 | 12632 | 0/0 | 0 | 1986.292 |
| 1 | 0 | 476 | 10384 | 0/0 | 0 | 863.836 |
| 2 | 0 | 472 | 6221 | 0/0 | 0 | 2901.440 |
| 3 | 0 | 473 | 20756 | 0/0 | 0 | 1120.025 |
| 4 | 0 | 428 | 5134 | 0/0 | 0 | 459.157 |
| 5 | 0 | 762 | 5736 | 0/0 | 0 | 1301.061 |
| 6 | 0 | 522 | 3844 | 0/0 | 1 | 2410.943 |
| 7 | 0 | 441 | 4127 | 0/0 | 0 | 1529.562 |

All partition source-before/source-after values, UTC launch/end, native logs/exits, exact enumeration/selection and JUnit originals are in `gate-final/`. Initial source digest: `130a2025a725d5adb89f19c86fb1ad642be4910bcb9af8b0ae0cbf3bd6d43a15`. Generated-reference comment changes are retained separately; the unchanged guard validates executable-token identity. No previous campaign or partial run substitutes for this gate.

All 11 unchanged Python guards ran: **10 passed, one errored, none skipped**. `test_changed_file_type_fails` receives Windows **WinError 1314** while creating its test symlink. This is an error, not a pass or skip. The hosted workflow's 30-minute worker limit remains unchanged. Longest local worker wall time: 2901.440 seconds; workers exceeding the 30-minute hosted limit: [0, 2, 6]. Local execution beyond the hosted limit does not prove hosted success. Fresh full hosted CI and receiving source review remain required.

The separate warning-detail diagnostic passed four tests/90 assertions with four warnings: the unchanged TerminalRetirement, DeploymentCustody, OperationalConstruction and UnknownProviderOutcome crash demonstrations read `.git/HEAD` as if `.git` were a directory. Exact source/line/native warning output is retained. The single gate skip is ReservedInstitutionalStorageTest::testSymlinkAliasesAndDanglingTargetsRefuse. Its JUnit skipped element has no reason text; the separate display-skipped diagnostic records: Host cannot create symlinks; suitable-host alias proof required. That skip remains distinct from the Python guard error. No warnings or skipped tests are silently counted as additional passing coverage.

Tool versions: PHP 8.4.14, PHPUnit 13.3.0 and Python 3.12.10, with exact version outputs in tool-versions.json. Vendor files were copied only after byte-identical composer.lock verification against the prior disposable PPC6 workspace. Dependency manifests, service wiring, production Composition, CI scripts/guards and workflow are unchanged.

## Failed attempts and remaining obligations

All development logs/JUnit/native exits remain in evidence.zip. The first sequence diagnostic failed on a missing multi-class fixture clock autoload; a dedicated additive clock fixed it. The first downstream run exposed PPC5_IN_FLIGHT from retained completed founding custody; the separate adapter above addressed it without editing original initializers. A mapping H ID exceeded the eighty-character contract and was shortened. Canonical JSON reordered object keys, exposing an overly strict test array-order assertion; canonical digests now compare the same artifact. A first crash diagnostic observed a transient wrong Operator fingerprint format; its native sha256-prefixed format was restored before the candidate. Public verifier development initially assumed command custody was an H record and used the wrong Locksmith literal; those independent verifier defects were corrected and failed outputs retained.

Development runs were uncommitted and some overlapped source edits. They do not have sealed source-before/source-after identities at every launch; that limitation is explicit in early-attempts.json. Exact candidate/source/UTC/native-exit evidence is supplied for the fresh full gate. Passing development observations (19/309 refusal/publication cases, 9/63 ordering cases, 5/19 preparation cases and 1/122 complete downstream case) are not added to its totals.

The first committed candidate `2ab05a69ff8c979c5a711b8c96aaefb5d951226b` completed a separate gate under `gate-bounded/` (aggregate native exit 0); none of it substitutes for this corrected candidate. A public completion-expiry probe showed a returned completion and changed journal head followed by PPC7_COMPLETION_ORIGINAL after the clock crossed expiry during native publication. Completion now revalidates both authorities and the founding holder after that phase, preserving valid PENDING custody on expiry. Two focused regressions passed 2/16 before the fresh full gate. Review also found that refusal helpers could catch PHPUnit's own AssertionFailedError through RuntimeException. Those new helpers were corrected, and a harness regression explicitly requires successful operations to fail the refusal assertion. The corrected source retains per-process public trust and exact malformed-input/refusal observations. These changes required a wholly fresh eight-partition validation.

Individual native Operator-act revocation has no separate admission owner in this candidate. The native envelope is outside the O2 admitted-acts map, so the existing O2 act-revocation target check cannot admit its nonce as such. Pending work can be stopped through the actual Formation decision or Operator issuer revocation owners. This is an implementation boundary in addition to the missing ordering evidence.

R4 proof remains incomplete for individual end-to-end invocation of all indirect reader/writer candidates; dedicated two-process Operator issuer/act/trust-expiry and founding-holder policy invalidation orderings; and all alias/second-policy reset variants. Serial issuer/policy revocation, authorization expiry and the named Formation/native successor races are implemented and tested, but do not substitute for those missing combinations. The source review ledger records these boundaries without promoting structural evidence to full acceptance. Earlier first-candidate records with missing public trust remain disclosed in their collector coverage file; corrected final-candidate records include public trust directly.

R3 remains open: broader substantive Profile predicate production/verification, direct native assignment verification, initial application/replacement, actual settings delivery and the full assignment race/refusal matrix. NativeAssignmentEvidence is not promoted in Composition. R5 production account/access/base/cognition ports remain missing; R6 live commissioning/interview remains deferred.

## Receiving packet and disposition

`imperium-ppc7-public-review.zip` and separate `.zip.sha256` contain committed source.zip, public evidence.zip, complete path/mode/byte/SHA-256 manifests, Git blob identities, start/tested/final identities, exact full-index binary start-to-tested.diff/tested-to-final.diff/complete.diff, bounded review.bundle with the exact published start prerequisite, standalone committed report and independent verifier. Run `python verify-public-review.py PACKET.zip PREREQUISITE_REPOSITORY` with Python cryptography and Git available. The verifier checks integrity and recorded partial results; it does not manufacture acceptance from a platform error or incomplete proof.

**Countdown: 3–5 → 3–5; decrement 0.** R4-only complete receiving acceptance would conditionally leave an estimated **3–4**, because R3 (1), R5 (1–2) and R6 (1) remain. That reduction is not applied; R1/R2 earn no second credit.

All five operational flags remain false: deployment_approved, enrollment_authorized, live_ready, activation, execution_authority. Native enrollment disposition remains **DEFER_ENROLLMENT** and the actual retry allowlist is **[]**. No real provider/account/credential access, installed private state, live enrollment/founding/appointment, commissioning, deployment, activation or execution occurred. Work stops at local commits and the public review packet; no push or integration. No subagents were used.
