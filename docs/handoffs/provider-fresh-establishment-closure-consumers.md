# PPC8 owner and consumer reconciliation

This is additive to the unchanged PPC7 ledger. The packet preserves the original 234-file inventory, the exact PPC8 pre-edit source bytes, and `consumer-call-paths.json` (234 files, 1,627 methods). `consumer-reconciliation.json` links every original entry to public entry paths, local method calls, concrete storage/schema selectors, rejection branches and returns. It separately identifies historical observations, supporting/indirect routes, unsupported schemas and actual current entry points. A lexical match is not credited as an executed authority test.

The raw Office consumers have several distinct contracts: single top-level occupants, Guildhall cohorts, Garrison bootstrap/native-registry branches, mission/operational occupancy, and Curia/Imperator/principal lineages. Each source entry retains its own reachable selector and refusal body; these contracts are not treated as one generic wrapper predicate. The new wrappers remain exact nested installation custody and do not acquire the old top-level authority fields, IDs, schemas or missing Office lineages. Historical observations and fixture demonstrations remain identified separately. No consumer is made competent by unwrapping a new original into an old input.

`new-route-source-snapshot.json` and `new-route-call-paths.json` inventory 20 current source files referencing the new boundary or its direct consumers, including command, session, child and assignment callers absent from the original inventory. The preserved raw discovery text contains 184 unique paths and four paths outside the original 234-source snapshot: Personnel, ModelPreparation, ProfileDesignation and RequiredV0PersonnelInstallationService. All four are explicitly included in the supplemental inventory; the original 234 entries are not silently replaced. The final packet pins their exact bytes. Source call-path proofs establish the following delegation of responsibility:

| Route | Actual boundary and meaning | Executed evidence |
| --- | --- | --- |
| `FormationFreshEstablishment` | Actual Formation owner; exact initialization, reservation, recovery, completion and native revocation. Historical reserve/complete replay does not publish or renew current authority. | Native revocation, durability, ordering, original-validation and reset tests; retained earlier crash cases |
| `FreshEstablishmentRevocations` | Pure closed signed-history grammar and exact target joins; no writer, trust import or filesystem repair | Corrupt retained originals, authentic native/revocation signatures, bounds and ordering tests |
| `OperatorRootOwnership` / ordinary installer / `RequiredV0PersonnelInstallationService` | Root-wide consumed binding fence; the V0 wrapper calls the same installer `installInOwner`. Explicit separate `establishInOwner` routes only to the FRESH package producer | Original two-order writer tests, reset tests, generic/v4 refusals |
| `FreshInstitutionPackage` | Formation → native → bootstrap/target; exact 18 placements and completion, bounded scan, no subset authority | Original per-placement/crash tests; pending native-complete observation; missing/corrupt/successor consumers |
| `FormationInstitution::actor/actorInOwner` | The actual live owner must resolve complete journal/native agreement, for every one of nine roles | All nine standalone/owner-held/pending actors, fresh processes, damage and successor cases |
| `FormationPersonnel::authoritySource/delegate` | Exact signed delegation resolves the actual institution before retaining evidence competence | Every role in pending and damaged states; real intact downstream producers |
| Three personnel record ingresses | `recordOwned` resolves the retained signed delegation and actual institution before versioned envelope validation; old/PPC2/strict PPC5 remain distinct formats | Valid retained generic and strict envelopes, plus a separately signed valid PPC2 envelope; missing-placement refusal |
| Candidate, strict candidate, current Courtthane/Locksmith | `candidateInOwner` requires the exact live frame and all actual source actors; detached FRESH candidate APIs refuse | Both Seats' current/candidate entry points and explicit detached refusals |
| Appointment and exact appointment replay | Both branches now resolve actual current native candidates before returning an authority-bearing holder | Both authentic appointments and exact replay after missing/corrupt/successor placement; intact replay preserves the journal |
| `FormationModelPreparation` | New binding/delegation/authorization/sealing operations resolve current Conscription/Laboratorium through the same live owner; `verifyInOwner` rechecks the complete correspondence | Each distinct new operation and verifier with valid retained prerequisites against damaged placement; positive strict chains retained |
| Model preparation history | Exact initialization/binding/delegation/authorization/seal replay and `sourceOriginal` are historical facts; the current verifier still fences any new use | Source branches explicitly return original records before current checks; no renewed competence is claimed |
| `FormationProfileDesignation` | New delegate/publish/current operations resolve current Laboratorium; designated candidate resolves strict current sources. Exact event replay is historical. | Distinct delegate/publish/current entry points plus both original positive designation chains |
| `FreshInstitutionalPreparation` | Both initializers call `completedInOwner` before initialization and preserve separate PPC5/PPC6 signatures | Both actual initializers, damage refusal, all four backdated receiving regressions |
| `FreshInstitutionalProfileMapping` | Full native completion, current designation, independently appointed matching holder and strict model correspondence | Both intact and damaged mappings; mapping remains unadmitted |
| `CitadelFormationCommand` | Closed dispatch table forwards the same exact arguments to the personnel public methods above; no second authority resolver | Concrete dispatch entries in source snapshot; underlying distinct method tests |
| `CitadelPublicInstitutionsCommand` | Calls legacy `witness`, reports each unsupported new wrapper as UNVERIFIED with false operational flags | Actual command execution returns 2 and nine UNVERIFIED observations |
| `FormationSessionAuthority`, `FormationSessionLeaseService`, `FormationCognition` | Owner-held strict branches call the same current Courtthane/Locksmith methods; detached branches refuse. They add no new institution adapter. | Concrete calls/branches retained; current-holder fence tests. No provider/session execution is claimed |
| `CuriaFormationService` / `ChildCuriaFormationService` | Current Courtthane branch uses the fenced owner; mission candidates use detached `candidate`, which explicitly refuses FRESH. Legacy publication recognition is historical. | Source proof of both branches and tested detached refusal; no child/mission competence added |
| `NativeAssignmentEvidence` | Calls current designation, current independent appointment, strict candidate and model verification under its existing owner | Call-path proof only. R3 assignment/application/use remains outside this batch; no assignment is admitted or applied |

The damage matrix invokes every distinct direct current ingress with retained valid prerequisites against a missing placement. Corrupt placement and successor state additionally exercise all nine actors and the shared downstream candidate, appointment and mapping joins. Their source paths converge on the same `completedInOwner`/`FreshInstitutionPackage::verify` fence; there is no claim that every redundant Cartesian combination was executed. Pending/partial/native-complete-without-journal cases exercise all nine actor, owner-held actor, authority-source and delegation paths. Deeper retained prerequisites cannot be created through those producers before completion, so accepted downstream state is never seeded to force those cases.

The preserved raw call/branch ledger is receiving-review evidence, not a formal control-flow dominance proof. The final report distinguishes executed cases and source-equivalence arguments, and receiving review must validate that distinction. No R3/R5/R6 acceptance, model approval extension, succession, repair or live competence follows from this record.
