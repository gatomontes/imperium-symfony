# Local runner: Citadel formation correction

Run the bounded continuation in
[the correction campaign](../next-campaign-citadel-formation-correction.md).

## Loco prompt

Work in E:/htdocs/imperium-citadel-stages. Complete the authorized Citadel
session-refusal and handoff-recovery correction, C0–C3.

Read applicable AGENTS.md, the correction campaign, accepted formation decisions,
candidate implementation guide/runtime contract, and previous review evidence.
The campaign controls this correction; older preparation-only or unstarted-stage
instructions are historical. Do not ask again for settled implementation choices.

Verify repository/origin, clean worktree and the exact candidate
e0386e75ce7619fbbeaac450af078d2d606df012 (tree
54b32f189130d421ce7cd74a54b8e11e200ace60). Preserve its branch.
Use codex/citadel-session-handoff-correction based on that candidate.
If the branch already exists, inspect and resume safely; do not reset it.
Fetch the published campaign. Integrate its two new instruction files without
replacing candidate implementation reports. Compare current remote steps/flow
with local records and reconcile their status while preserving history.
Do not start from main alone: the reviewed executable candidate is local.

First reproduce CF01: authentic REFUSED -> DEFERRED -> OPEN revives the original
grant. Then fix refusal as a terminal state, including stale control replay;
preserve valid never-refused defer/resume and cumulative budget evidence.

Reproduce CF02 using real child publication followed by interruption before parent
publication and clock advance beyond approval expiry. Implement narrow historical
receipt reconciliation under the original durable identity fence. Prove exact
identity/bytes/provenance and idempotence. Never let expired approval create a new
effect. Absent/mismatched/corrupt or unverifiable evidence stays fenced. Distinguish
revocation before effect from later changes; recovery grants no new authority.
Cover concurrent/repeated reconciliation and no duplicate child or cognition.

Commit coherent corrections locally. Use focused tests during development and
the real command/DI offline demonstration with recording fake transport. Run the
full repository suite on committed executable code and record exact SHA/tree,
commands, results and warnings. Preserve the old 2,709/52,902 result as historical;
do not claim it proves either correction or rerun old owner ceremonies.

Update current steps/flow and implementation documentation with actual results.
Package an allowlisted source/test/proof ZIP and SHA-256 manifest, correction
report, changed-test map and precise handoff. Preserve original packet and
transcripts. Finish pending independent review, with an explicit evidence-backed
disposition for each finding and retained live/custody limitations.

This is local implementation and offline proof. No real keys, live calls,
commissioning, installation changes, mission execution, push/merge, branch
deletion or alteration of completed campaign evidence in this runner.
GitHub instruction publication is separate from corrected-code acceptance.

Nulla requies impiis.
