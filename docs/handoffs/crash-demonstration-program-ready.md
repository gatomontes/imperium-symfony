# Crash demonstration program ready

## Current state

Repository: `gatomontes/imperium-symfony`  
Required base: `main` at or after `20208f177df576b863340ee397730b455b2965df`

- Delegate Mission Steps 1–69 are complete and terminal.
- Runtime Integrity Hardening Steps 1–35 are complete.
- The severe-source cleanup gate is closed.
- Final source rescan: 376 runtime PHP files, zero severe-compression targets.
- Cleanup Batches A and B passed explicit local PHP lint and the complete PHPUnit suite.
- Secondary formatting debt remains non-blocking and recorded.
- Crash Demonstration 1 now has an implemented six-boundary recovery harness, private retained-evidence output, sanitized summary output, and focused PHPUnit coverage. Operator-local PHP execution remains required to capture the retained evidence.

Do not invent Delegate Mission Step 70 or Hardening Step 36. The next lifecycle is an operational-evidence program, not a new authority-bearing mission leg.

## Read first

1. `docs/delegate-mission-flow.md`
2. `docs/delegate-mission-authority-consumption-matrix.md`
3. `docs/delegate-mission-record-schema-catalogue.md`
4. `docs/delegate-mission-terminal-operational-evidence-audit.md`
5. `docs/handoffs/runtime-integrity-hardening-leg-complete.md`
6. `docs/handoffs/runtime-severe-source-cleanup-closed.md`
7. `todo/blackquill-todos.md`

## Demonstration sequence

### Demonstration 1 — operational construction recovery

Exercise the recoverable Steps 44–46 transition with deterministic fault injection immediately before and after each Codex checkpoint. Prove ordered immutable Folia, generation monotonicity, exact replay, single-winner conflict behavior, and absence of deployment/cognition/provider authority.

### Demonstration 2 — deployment custody recovery

Exercise the recoverable deployment-custody transaction around authorization, custody compare-and-swap, immutable transition persistence, and activation boundary. Prove one completed deployed-and-bound inactive state, exact replay, and no leaked operational authority.

### Demonstration 3 — unknown provider-outcome recovery

Exercise the claimed/in-flight provider boundary. Prove unknown outcomes prohibit automatic replay; recovery requires the exact sealed response envelope and one exact consumed recovery authorization; the provider is not reinvoked.

### Demonstration 4 — terminal retirement recovery

Exercise every terminal retirement checkpoint. Prove deterministic forward resume to one terminal Folium, restored available Persona custody, retired binding, unbound Seat, and zero surviving authority.

## Evidence contract for every demonstration

Each demonstration must deliver:

- one repeatable PowerShell-friendly local command;
- deterministic fixtures isolated from production state;
- explicit pre-crash, crash, recovery, replay, and invariant assertions;
- machine-readable JSON evidence retained privately;
- a digest and source commit binding the evidence to the tested runtime;
- a sanitized external summary that states what was proved but omits internal paths, schemas, authority topology, credentials, model identity, and proprietary implementation detail; and
- PHPUnit coverage for the harness itself where practical.

Never commit private raw evidence or credential-adjacent material to GitHub. Repository documentation may contain only the demonstration contract and sanitized example shape.

## First action in the new chat

Inspect the existing operational-construction coordinator, `CodexImperiiStore`, its fault injector, relevant Steps 44–46 services, and their tests. Design Demonstration 1 around the existing recovery checkpoints rather than creating a parallel toy mechanism. State the exact crash matrix and retained-evidence schema before implementation, then implement, push, merge, and provide the local command.

## Ready-to-paste continuation prompt

```text
Continue Imperium from `main` at or after merged commit `20208f177df576b863340ee397730b455b2965df`.

Read `docs/handoffs/crash-demonstration-program-ready.md`, `docs/delegate-mission-flow.md`, `docs/handoffs/runtime-integrity-hardening-leg-complete.md`, `docs/handoffs/runtime-severe-source-cleanup-closed.md`, and `todo/blackquill-todos.md` before changing code.

Delegate Mission Steps 1–69 are terminal, Runtime Integrity Hardening Steps 1–35 are complete, and the severe-source cleanup gate is closed. Do not invent Step 70 or Hardening Step 36.

Begin Crash Demonstration 1: operational construction recovery. Inspect and reuse the existing Steps 44–46 recovery coordinator, Codex Imperii store, fault injector, services, and tests. First state the exact crash matrix and private retained-evidence schema; then implement the repeatable PowerShell-friendly demonstration, tests, sanitized external summary shape, and documentation. Never commit raw private evidence or credential-adjacent material. Push and merge when complete. I will run the local PHP commands.

Ad Imperium. Not one step back.
```
