# Iron Gate Execution Authority and Receipt Binding campaign complete

## Result

`TERMINAL_THROUGH_BATCH_11`

Preparation Batch 0 and Batches 1–11 are complete. The deterministic proof corridor now has native
Curia request, Imperator decision and separate issuance, one durable execution winner, a pre-effect
unknown-outcome journal, one journal-gated credential callback, truthful raw provider evidence,
exact accepted-receipt admission and read-only reconstruction. Batch 11 closes the campaign with
end-to-end, crash-stop, replay, tamper and secret-exclusion proof.

The canonical terminal evidence is
`docs/iron-gate-execution-receipt-binding-terminal-evidence-audit.md`. No batches remain.

## Preserved limits

This is not a live-provider rollout. `IronGate`, `AgentMailEmailSendCommand`,
`DeterministicBoundaryExecutor`, `AgentMailEmailTransport` and the existing credential platform are
unchanged and do not consume the new corridor. Tests use an in-memory callback and perform no
network I/O. A future live-consumer adoption requires its own explicit campaign and operational
evidence.

Sortie cognition/tools, generalized Lazaretto policy, credential-platform work, revocation,
propagation, telemetry, reassessment, containment and incidents remain closed deferred boundaries.
No Delegate Mission step or terminal campaign is reopened.

## Continuation

There is no Iron Gate Execution Authority and Receipt Binding Batch 12. Any continuation must name
a new, separately bounded campaign. The two honest candidates are live deterministic consumer
adoption or sortie execution-authority assessment; neither is selected or authorized here.
