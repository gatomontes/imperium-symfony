# Handoff: Delegate mission Step 62 complete

Step 62 consumes the exact Curia selection authority and has the ordinary Recruiter seal the selected provider/model version and configuration to the exact Delegate mission target and turn 1.

Terminal checkpoint: `DELEGATE_MISSION_MODEL_BINDING_SEALED_PENDING_ACCESS_ATTESTATION`

Only one exact single-use Clavium access-attestation authority is open. The Profile is unchanged; no model is assigned, no access is asserted, no credential is released, and no provider, resource, external action, or execution is authorized.

The last confirmed operator-local baseline is Step 50 on PHP 8.4.14 with 367 tests and 5,180 assertions. Steps 51–62 await the next local PHPUnit run because this environment has no PHP binary.

Step 63 has Clavium attest that access to the exact bound model is available under explicit restrictions and expiry, without releasing credentials or invoking it.
