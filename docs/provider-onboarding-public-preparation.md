# O0 public preparation and smallest fresh-route dependency

Status: NON_RUNNABLE_PUBLIC_TEMPLATES_PREPARED; evidence and review remain open.
Read the [current report](handoffs/provider-onboarding-o0-provider-report.md),
[provider appendix](provider-onboarding-deepseek-appendix.md) and
[v1.3.1 draft](../contracts/provider-onboarding-retries.md).

The [policy preparation wrapper](provider-onboarding/o0-public-preparation.json),
[workloads](provider-onboarding/o0-workloads.json),
[cost arithmetic](provider-onboarding/o0-cost-comparison.json) and
[source index](provider-onboarding/deepseek-source-index.json) are deterministic
public artifacts. JSON uses explicit preparation_unresolved_ref descriptors,
never zero digests, fabricated acts or synthetic assignment generations. These
are NOT examples to submit to onboard. The wrapper has runnable=false and no
runtime policy schema. Exact closed runtime fields cannot be claimed while refs
and approved predicates are absent.

The finite selected-route skeleton has 19 steps: configuration, evidence admission,
access, base selection, mapping, fresh founding, 12 assessment attempts, and one
whole-set application. Seventeen distinct one-use slots cover all effectful steps;
configuration/selection confer no authority. W1→W2→W3 resolves group success to the
first valid outcome and skips unused retries. Application consumes all three group
results locally and does not add a cognition call. Signed approval/designation/
qualification/cutover cannot be replaced by these policy-effect slots. This is
only the fresh founding branch conditional on genuine open-window/root evidence;
if that branch is unavailable it refuses, never falls through to standing cutover.

Every preparation_unresolved_ref must resolve to its inventory entry and actual
retained public evidence before a future runtime policy is constructed. Terms
objects must be complete pre-enumerated objects; lists of independent Profile or
model refs cannot substitute for one complete founding tuple. Candidate config
proposal values are not owner-approved configuration refs. Canonical request bytes
cannot be frozen before real Profile/input/tokenizer evidence exists. Each W-group
freezes its resolved semantic input before attempt 0, not at retry time.

The smallest lawful next public preparation is:

1. Review v1.3.1's group selectors, slot ownership, assignment result view and empty
   retry allowlist independently; keep earlier F1/F2 acceptance attributed only to
   its reviewed v1.2 source.
2. Record approval/amendment of the exact proposed technical rows: two candidate
   aliases/configurations and their pinning limitation, required Profiles/fit
   predicates, token/context/output ceilings, freshness/lifetime, data scope,
   request/time limits and two assignment targets. Do not ask again for the
   settled D2-A/provider/FRESH/retry-count/dollar choices.
3. Supply public-only intended future instance identity, competent issuer/trust
   description, actual fresh founding/root evidence, genuine approved Augur,
   Courtthane and formation Locksmith Profiles, and permitted tuple/mapping
   definitions. The executor can derive hashes from authentic public bytes; the
   owner need not invent digests. No private key or installed state is needed.
4. Establish provider/account-scope public evidence for access fee/invoke rights,
   exact tokenizer/chat framing/usage and B1 remote bounds. Where unavailable,
   retain UNKNOWN; this pass cannot replace it with a paid probe. Public account
   attestation later must have genuine provenance, scope and validity, not an
   example fixture. An account API call would require its own future authorization.
5. Only after those inputs are available, separately prepare real policy/terms and
   admission materials at actual heads/times for review. No signature, enrollment,
   instance creation, appointment, founding act or trust acceptance occurs now.

Current source lacks the genuine fresh founding/model-assignment bridge, full
standing approval/activation corridor, pre-Augur access evidence path, runtime
mapping/structured usage transport and intended assignment consumers identified
in the prerequisite map. No amount of template completion implements them. O0
review/technical approval, O1–O5 selection, software deployment, trust enrollment
and live commissioning stay separate. B1 and DEFER_ENROLLMENT remain unchanged.
Citadel encloses Courtyard; Courtthane's exact Seat and Seneschal's Curia role
remain intact. Understanding, drafting approval, mission approval and execution
are distinct; model-fit reports/assignments are none of those approvals.
