# PPC10 implementation and proof plan

**Pending exact C1/C2 approval.** [Contract](provider-staged-assignment-proposal-v1.md), [decision](handoffs/provider-staged-assignment-decision.json), [source inventory](provider-staged-assignment-source-record.json). This page authorizes no runtime implementation before that decision. Prior accepted R1/R2/R4 and PPC9 partial evidence remain intact.

## Implementation sequence after approval

1. Record the exact approved proposal hash and conversational reply. Create an isolated checkout from the published preparation revision; retain original start commit/tree and the unchanged source inventory. No installed state, live keys or provider calls.
2. Add closed successor schemas and pure decoders for C1 scope/act/commission/input/view/application and C2 contract/delegation/judgment/revocation. Implement strict version dispatch, cross-domain refusal, complete raw-original retention and a compiled C2 obligation/exercise map. Old v1 contracts must continue to refuse this new meaning.
3. Add C2 purpose-limited native producer/verification owners, using current installed actors and existing live Formation owner/signatures. Produce separately signed originals for both Seats. Use existing Profile ingress, model seal, complete examination, separate approval, qualification, designation and appointment. Do not weaken a historical fixture to manufacture a positive result.
4. Add C1 one-commit first scope admission/v4→v5 migration and branch activation. Preserve v4 originals, first admission, root consumption and accounting. Add finite scope successor history, shared nonce/revocation lookup, no-reset rules and branch exclusivity. Explicit migration/retention owners do mutations; observation does not.
5. Add scope-aware W1/W2/W3 preparation, expected context, actual reservation/custody/settlement and assessment selection. Retain both founding-policy and scope identity end to end. Enforce unchanged cumulative limits and expiry. An assessment failure cannot be avoided with another scope.
6. Join actual initial applications A and B, separately signed complete-pair replacement and current persistent settings. Route both application and protected settings delivery through one current native/C2 verifier. Preserve legacy route, missing-port defaults and existing lock order. Exercise actual protected delivery with an explicit offline transport double and retain its reached call/custody evidence; no real provider I/O.
7. Complete adversarial, process race and interruption proof, then local complete locked-dependency validation and a public packet. Receiving review must inspect the exact candidate, run the complete unchanged hosted gate and accept full R3 before any countdown credit.

## Required evidence matrix

| Boundary | Positive evidence | Independent negative / temporal evidence |
| --- | --- | --- |
| Original authority | Exact new-domain Operator scope, current enrollment and original founding lineage | Old-domain act, wrong purpose/object/issuer/root/head, missing competence, future/expired/revoked original; no v1 signed-terms bypass |
| Chronology | Real FRESH → original base/founding → completed R4 → actual Profile chains → mapping → scope admission → actual W1/W2/W3 → application → protected use | Missing/unadmitted/forward native source; second-policy refounding; uncompleted R4; pending designation/appointment; attempted native source substitution under old policy |
| Migration | One atomic initial admission+migration, all original maps/digests preserved, scope generation one | Active claims/source fences, legacy assessments/application, partial migration, corrupt prior state, concurrent first scopes, replay after revocation/expiry, unchanged state on rejection |
| Fitness | Both Seats, seven independently signed obligation judgments and eight exercise cases; current exact model/qualification originals | Missing/duplicate/unknown obligation, wrong competent role, another Seat/Profile/binding/contract, shortened exercises, PASS backed by UNKNOWN/FAIL exercise, empty/foreign evidence, stale/revoked actor/delegation; all original Senate/approval/qualification failures still refuse |
| New competence limits | Exactly scoped Formation delegation and terminal withdrawal | Old personnel delegation used as fitness power, wildcard scope, successor reuse, arbitrary model approval, designation or appointment by fitness signer |
| Assessment | New scope context in all W1/W2/W3 originals, five custody stages, settlement, exact selection | Replayed old v1 or prior-stage response, mixed target/generation, W2 before W1, W3 wrong W2, invalid ranking/comparative support, changed candidate set, failed stage followed by another scope |
| Initial application | Both A derived-from-scope and B separately signed, actual persistent pair and current shared verifier | Missing mode-B act; scope admission treated as application; partial pair; stale expected head; chosen pair outside exact permitted sets; forged profile PASS |
| Replacement | Each Seat separately succeeds through actual successor candidate chain and independent appointment, new exact scope/assessment, separately signed replacement and whole-pair settings | New mapping before separate appointment; implicit settings update; unchanged-pair budget renewal; changed binding under old immutable Profile; wrong predecessor/view/old nonce; cross-policy/cross-root replacement |
| Actual use | Fresh process reconstructs originals and resolves both roles; protected transport reaches explicit offline sink with exact pair and custody checks | Other Seat revoked while requested Seat unchanged; settings snapshot mistaken for dispatch authority; detached/expired/wrong-store owner; stale application; requested configuration mismatch |
| Races | Barrier-controlled independent processes, both ordering directions, both Seats | Application versus revocation; replacement versus appointment/designation; delivery versus withdrawal/expiry; competing first stages and competing replacements. Verify loser refuses and history is unambiguous. |
| Interruption | Before/after each stage and application atomic publication; assessment reservation/custody interruption | No orphan current index, no partially replaced pair, exact historical replay without new effect, no reset/refund or automatic resend; preserve unknown exposure and settle through existing rules |
| Bounds/corruption | Exact accepted maxima and maximum+1 refusal for new/combined budgets | Scope count, shared cognition/request/time/cost, original expiry, parser/bundle/depth/visits/source count, fitness bytes/count/references/rationale/exercise/history; missing middle/terminal history, forged index and changed original bytes |
| Compatibility | Old tests/fixtures and protected bytes unchanged, old route still explicit | v1 accepts v2 evidence, mixed input/schema, silently widened generic Profile or selection semantics, unsafe shared state validation, cached current authority |

Each negative must name its exact expected reached refusal, retain before/after head and relevant counters, and then show a valid control still passes. Do not credit a later branch when an earlier source/authority refusal prevented reaching it. Sign alternate malformed originals with the actual disposable competent key where authentication is part of the proof. Raw corruption tests remain separately labeled.

## Source boundaries and delivery requirements

The source inventory is an exact Git-byte baseline, not a waiver. `AssignmentRule.php` is the only previously protected runtime path proposed for version dispatch/exact scope tuple validation; preserve its v1 functions and selection meaning. Prefer separate successor classes. All other protected runtime paths, generic Profile contracts, old fixtures, pinned persistence writers, founding/R4 owners, Composition and CI remain byte-identical. Ordinary owner adapters listed in the inventory may change only for stated C1/C2 obligations. If another protected seam is needed, stop that extension and document the concrete source incompatibility; do not quietly broaden scope.

New tests and helpers may construct explicit disposable native roots and in-memory signing keys, authentic institutional evidence and offline external-port doubles. Mark each capability/access/base/cognition/tariff/tokenizer fact as synthetic or unavailable; no fabricated production originals. Do not load a serialized accepted state or copy populated runtime fixture roots. No additional agents are authorized.

Public packet: committed source/evidence ZIPs; full file/mode/byte/SHA-256 manifests; exact baseline→tested→final diffs; bounded Git bundle with published prerequisite; tested/final identities; launch/exits/timing/JUnit/guard original logs; independent public verifier; external packet SHA-256. Exclude private keys, fixture roots, installed files, vendor/cache, credentials and untracked environment material. Preserve failed checks and their original traces. A post-test executable delta needs fresh validation.

Use locked dependencies without scripts, the unchanged eight-partition full test enumeration and eleven guards, source digest before/after every partition, exact case/file coverage and native exits. Linux symlink and complete race proof must run, not be relabeled a Windows pass. Fresh hosted workers must meet the unchanged 30-minute limit; do not alter partitioning, guards, CI or contracts to manufacture green results. Prior PPC9 CI does not validate this successor. No complete R3 claim until every named proof obligation and receiving gate is satisfied.

**Countdown stays 3–4; decrement zero for preparation or approval.** R3 remains one estimated complete implementation/receiving outcome, R5 one–two, R6 one. Report a split/re-estimate explicitly if the work cannot fit the planned R3 batch. All flags false; `DEFER_ENROLLMENT`; retry allowlist `[]`.
