# Principal Activation Decision Authority Provenance Remediation Batch 6 adversarial audit

## Result

BATCH_6_ADVERSARIAL_AUDIT_PASSED

The read-only audit challenges the immutable combined production winner against
aggregate eligibility, exact successor identity and one-field scope
preservation, separate lifecycle activation, issuance-authorization lineage,
decision and single-use authority shape, expiry, revocation, exact replay,
changed-evidence contention, and before/after-commit recovery.

The audit recursively refuses credential secrets, credential bytes, API keys,
tokens, passwords and process-local capabilities. It also fails closed if any
provider-executor-principal activation, provider-binding activation,
activation-authority consumption, credential/capability handling, provider
invocation, external action or continuing authority appears.

The audit writes no record, repairs no record, issues or consumes no authority,
activates no principal or binding and performs no external I/O.

Iron Gate and Lazaretto remain closed. UNKNOWN_REPLAY_PROHIBITED remains binding.
