# Local isolation package — terminal local audit

LOCAL_ISOLATION_PACKAGE_READY_AWAITING_OWNER_ACTION.
DEPLOYMENT_ISOLATION_UNPROVED_OPERATOR_SETUP_REQUIRED.

Batches 0–2 are complete. This is implementation-agent local self-audit, not
independent acceptance, actual-account isolation or a completed real mission.
The owner has an executable setup, probe, signing and resume package. Batches
3–5 remain pending actual owner action and authentic authorization.

Audited code commit: 47bcc44a8fc0540bef855d27ba71abaebb452c84.
Audited tree: 176246497348d0f71be5cdedd6c404d8d523b44e.
Baseline clean main/selection merge: ad9518acb095f69bbea4fef19cf383a44286c077.
Later completion evidence/runbook refinements are documentation only, not separately
tested executable code. The final documentation commit SHA is returned in the task.

## Exact audit results

| Check at audited commit | Result |
|---|---|
| Full PHP suite | 2677 tests / 52713 assertions; zero skips; 7:44.435 |
| Focused protected/amendment/package | 28 / 419; zero skips |
| Same focused suite using packaged PHP and minimal ini | 28 / 419; zero skips |
| Windows PowerShell disposable route | LOCAL_ISOLATION_DISPOSABLE_REHEARSAL_PASSED |
| Existing amendment proof using packaged PowerShell | MISSION_AMENDMENT_POWERSHELL_PASSED |
| Final scripts | PowerShell parsed without errors |
| Package byte/file-set comparison | 6467 files match; extra/missing/hash/reparse checks supplied |
| Owner installer dry run | WhatIf only; bad digest rejected before setup |
| Actual owner installer/account execution | NOT RUN; owner boundary |

Packaged PHP was exercised with the same minimal ini except its extension directory
relocated to the disposable package path. This validates the binary/modules/route;
the fixed deployed environment still requires owner measurement. The WhatIf account
resolution inputs were existing inventory SIDs, never claimed deployment roles.
No skipped or same-user case was counted as actual Windows account separation.

The first PowerShell attempt failed independent verification because timestamp
strings were parsed/reformatted. The next attempt exposed exact slash spelling
in its fixture repository argument. Both refusals are retained. DateKind String
and exact repository argument fixed the harness; matching was not weakened.
The c6f1cd8 full-suite attempt was interrupted before completion to strengthen
redundant public receipt-field checks. It is not counted as passed. Final tests
ran after the correction was committed. No required acceptance assertion was removed.

Symfony regenerated config/reference.php comments during tests. The exact final
diff is retained under var/local-isolation-evidence, inspected as comments only,
and restored to the clean baseline. No user work was reset or stashed.

## Package and target

Installable candidate directory:
E:/htdocs/imperium/var/local-isolation-evidence/package-47bcc44a.

Package-manifest SHA-256:
8DAB48F98D147A6ABA9E89260D25082BA5B91AD2C72638F1F10FFB9A449CF6AE.

The full public manifest is docs/local-isolation-package-manifest.json. It binds
code, autoloader and every dependency, PHP binary/modules/ini, PowerShell runtime,
minimal inspection target and inert draft. Earlier package-preaudit and
package-reviewed directories are preserved historical candidates; do not install them.

Target commit a1fc4f27634319f2a22df2e6a1b370f70cdb98bf, tree
21780452f5815d4342f8f5c96923b2b276d9930a: 15 paths, 23 original-ID objects,
239613 blob bytes, 448476 accepted inflated bytes including repeated tree reads.
All 15 blob IDs additionally matched independent local git ls-tree output.
Fresh compressed files were generated without hardlinks or shared object storage.
No target source fetch, checkout, hook, repack/unpack command or remediation ran.
Git itself printed an automatic packing notice during the corrective local commit;
that was outside any real mission. All prior branch refs remain verified unchanged.
No journal was reset, no branch deleted and no implementation was published.

The installed runtime candidate, inspected historical target and later evidence
commit are deliberately separate identities. The staged target is not claimed
read-only under actual Runtime/caller tokens until owner ACL setup and probes pass.

## Receipt and analytical limits

The disposable rehearsal inspected its own new real Git fixture, not the owner's
selected target. It observed AUTHORIZED → ADMITTED → INSPECTING → COMPLETED,
verified all 15 committed fixture blobs, reconstructed authorization/generation/
commission/receipt bindings and capability signatures, and checked the complete
target file manifest unchanged. Derived and completed sessions resumed without
replay. A simulated uncertain-consume marker refused without journal mutation.
Native canary write denial and absent-file classification passed under the same user.

INSPECTING is persisted after the synchronous byte read, not before it or as live
percentage progress. Separate timing records measure calls. The receipt is not
independently signed; Runtime/issuer provenance and measured installation matter.
Public consistency verification does not exclude a compromised trusted Runtime
fabricating a self-consistent transcript. No mechanical receipt certifies semantic
truth or completeness. docs/local-isolation-report-template.md is explicitly a
pending report specification, not a fabricated real result or a zero-findings claim.

## Historical evidence and remaining owner actions

All previous test methods/branches remain. Two new PHP methods and a new disposable
PowerShell/real-Git fixture were added; old protected/amendment/canonical tests were
not changed. AM01/AM02 negative and fresh-amendment positive proofs passed again.
Historical 2675/52696 remains tied to 2a2b746; new totals are independently reported
(legacy conditional race assertions can vary, as documented in that audit).
Four earlier transcript hashes and the original branch inventory were rechecked.

Execute docs/local-isolation-owner-runbook.md in owner-controlled terminals:

1. Review package/digest and proposed PmaRuntime/PmaCaller standard accounts; create
   them privately, record actual SIDs, then apply only the fresh fixed-path installer.
2. Verify installed manifest and protected PHP/environment; run complete probe plans
   as each actual identity, record token memberships and Runtime/caller startup results.
3. Independently confirm public trust; enroll from the Runtime owner terminal; repeat
   journal-handle probes. Supply the public measured evidence and owner readiness record.
4. Prepare a fresh exact dossier/expiry, review every numbered line and sign in the
   independently held-key environment. Return only the authentic signed response.

Then this task can continue the already authorized Batches 4–5 within the exact
scope, without redundant permission. A failed or unknown probe is a stop, not a
completion. Preserve partial installs and incidents; never erase markers/journals
or reuse old authority to recover. Public artifact content was inspected before
commit; private raw exchange/authority/test state remains outside Git.

No real account creation, installation apply, key/password custody, real trust,
real inspection, provider, external service, target mutation, push, PR, main merge
or branch deletion occurred. Windows-only production, loose SHA-1 support, bounded
state/journal capacity, absent migration, trusted OS/Runtime/signing assumptions and
unmeasured hardware power-loss behavior remain explicit.
