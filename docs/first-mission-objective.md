# First bounded mission: inspect the O5 CLI contract

Status: scoped, not launched. This is a mission brief and installation preflight, not an execution authorization or proof of commissioning.

Objective: identify at most one actionable contradiction or missing requirement in the O5 operator CLI specification against the canonical onboarding/continuation contracts. A valid outcome may be NO_ACTIONABLE_DEFECT_FOUND or INSUFFICIENT_INPUT. Do not force a finding.

Inputs: exact public specification from preparation commit `a1f227a6e0951b82c1e958b133bbcc94e4e1138a`, path `docs/provider-onboarding-o5-cli-preparation.md`; canonical `contracts/provider-onboarding.md` and `contracts/provider-onboarding-continuation.md` from the later verified, accepted execution baseline. Record original hashes and selected line ranges before preparing any provider payload. The preparation commit is not a deployed-runtime identity.

Review questions: Can preview mutate state or reserve identity? Can status infer authority from unrelated facts? Can resume dispatch again? Can a historical receipt be presented as current readiness? Are command identity, reason codes and exits consistent? Findings must cite supplied original lines, describe a triggering scenario, and suggest the smallest correction. No code execution, repository edit, external publication or arbitrary browsing belongs to this mission.

Target route: genuine Courtyard intake → bounded understanding → explicit drafting and mission approvals → legitimate child Curia and receiving acceptance → separately authorized mission execution → retained attributable result. Existing approvals should not be repeated when the runtime can prove they remain applicable. Approval of this brief does not synthesize missing institutional records.

The existing bounded source-review executor is a possible execution component after its integration and prerequisites are verified. Its documented direct operational entry is not a substitute for demonstrating Courtyard intake and handoff. Do not invoke it as an alternative and call that end-to-end Courtyard acceptance.

Proposed effect bound: one provider request, no automatic retry, static review only. The existing source-review component declares a maximum 120-second client duration, 4,000 output tokens and tariff-based USD 1 preflight ceiling. These are source-declared ceilings, not current tariff verification, a spending authorization, guaranteed remote cancellation or provider billing guarantees. Exact provider/model, tariff, payload and installed authority must be verified before launch. Unknown outcome retains exposure and ends the run without redispatch.

Current blockers: installed running source is unverified; genuine trust, institutional competence, appointments, custody and transport evidence have not been supplied to this session. O4 acceptance remains unresolved and deferred; O5 CLI implementation remains blocked. Do not restart either campaign from this brief. The illustrative interface and documentation do not supply these prerequisites.

Success for this immediate handoff is a small, accurate preflight report identifying the earliest real blocker and exact next technical action. It is not a successful mission. Live execution success later requires authentic intake/handoff, authority, claim, provider-response and result references on the approved chain, with no invented records.
