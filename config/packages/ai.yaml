ai:
    platform:
        # No external AI platform is configured in the Core runtime.
        # Sortie-only provider configuration lives under config/packages/sortie/.

    agent:
        # No cognitive agent is exposed in the Core runtime.

    store:
        # No shared AI memory/store is configured.
