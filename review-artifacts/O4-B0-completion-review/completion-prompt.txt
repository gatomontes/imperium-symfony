Complete the existing O4-B0 implementation. This is continuation of the same selected batch, not a new preparation campaign. Ad Imperium.

Start from submitted final 4711ea5def32148335069f396fc86ab6abc82df4 (tree 68ec1d4f305d8a66a280680c0cdf358d33b54934), which contains the partial AssessmentResolver work. Preserve the original preparation baseline f544b696a0ebdf8c25a71f3a369e5a93738074e9 and all submitted evidence. Record the actual clean continuation entry. Use an isolated worktree if needed; never reset earlier work. No subagents.

Read the accompanying review-report.md, contracts/provider-onboarding-assignment-application.md, contracts/provider-onboarding-assignment-selection.md, contracts/provider-onboarding-continuation.md, contracts/provider-onboarding-ledger-custody.md, docs/next-campaign-provider-onboarding-o4-assignment-application.md, the current decision/evidence card, and the submitted O4 report/design/proof matrix. The report accurately says incomplete: none of the application, persistence or recovery acceptance criteria has been waived.

Finish the already authorized engineering directly. A missing writer, state version, policy-mode adapter or consumer is an implementation task, not a reason to ask for real installed authority or repeat settled policy questions. Use synthetic temporary-root authority and exact mock transport. Do not substitute a revised plan or another partial resolver for the working batch.

1. Implement explicit bounded v4 state/migration and complete structural/history validation for assessment views and applications. Preserve prior schema behavior, signed/raw originals, history and consumed rights; refuse malformed or unsupported versions. Migration is explicit and idempotent, not an incidental read/preview effect.

2. Build the exact original-backed assessment view from real selected W1/W2/W3 outcomes, claims, custody/envelopes, frozen inputs, current semantic-evidence and Profile-specific predicate validation. Use the existing typed selector, exact approved capacity rule, complete FIT coverage, lower-middle tiers, deterministic ties and coupled-pair refusal. Return no fallback choice. Implement exact assessed_assignment_set derivation; never promote a ParsedResponse, view or caller-supplied boolean into authority.

3. Implement the owning CommandLedger application path: historical command recognition before fresh checks; fresh expected-head/predecessor, current trust/policy/act, exact Profile/binding generations, source/fence and one-use checks; then one FormationJournal commit for both role settings, generations, view, receipt, command/completion and authority/slot consumption. Preserve exact contract record bodies. Any pre-admission refusal leaves settings, generations, IDs and consumption untouched. No second journal or partial role publication.

4. Close the actual policy/act compatibility seams. Existing Policy accepts mode A and APPLY acts require a policy reference. Preserve the selected A route and the distinct B signed-act boundary required by the contract; implement any compatible extension explicitly without altering prior signed/approved bytes or turning a policy-derived result into an act. A later operator replacement requires fresh compatible authority over the exact whole pair/current predecessor. Never reuse the consumed initial application slot, invent an unscoped authority effect, or treat a new command/policy ID as fresh permission. Test the declared authority variants and reject unsupported combinations.

5. Implement the production persistent model-settings resolver and actual consumer boundary, plus explicit authorized whole-pair operator change/revalidation. Persisted settings are separate from personnel appointments and session/lease/dispatch authority. Preserve FormationPersonnel, FormationSessionAuthority, FormationSessionLeaseService and FormationCognition authority checks. Missing/unavailable/no-longer-current input blocks affected use while retaining settings/history. No automatic reassignment, fallback, catalogue expansion, credential-triggered substitution or Augur replacement. Fence genuine alternate writers to the same settings namespace.

6. Preserve the useful AssessmentResolver work, but do not call its public inspect-owning resolve() from inside changeAtHead. Share bounded internal original verification under the existing owning lock; public methods must not accept caller-supplied trusted state. Recompute current evidence at the application boundary. Keep AugurAdapter classifier exception behavior, R1 mapping limits and R2 pure bounded parsing/canonicalization semantics intact.

7. Complete every row of the existing seven-row contract proof matrix. Positive integration must create authentic synthetic history via actual O1/O2/O3/O4 producers, then select/apply/resolve exact tuple/Profile/configuration identities. Include zero/sole/multiple/rank/tie/coupled refusals, both declared authority forms, stale/revoked/expired/wrong-scope/generation/original/fence failures, exact replay and same-ID conflicts, cross-ID duplicate refusal, competing whole-pair writers, pre/post-publication faults, fresh-process restart/recovery, explicit migration preservation, authorized replacement and historical replay after replacement/expiry/revocation. Assert zero extra HTTP requests on application/replay/change/revalidation and zero partial writes or consumed rights on refusal. A new object in the same process is not a fresh-process persistence test.

Preserve original tests/fixtures, approved policy/contracts, dependencies, service configuration, frozen inventories and prior reviewer regressions. Narrow indispensable runtime changes are authorized within the existing contract, with compatibility/atomicity proof. Keep all five operational flags false and the actual retry allowlist empty. No real provider requests, credentials, installed-state reads/writes, live enrollment/appointment/activation, OAuth, existing-installation cutover, O5 CLI or subagents.

Run meaningful new and existing required selections, changed PHP lint, non-debug kernel smoke, static specification checks and the unchanged full vendor/bin/phpunit tests gate on PHP >=8.4 with locked dependencies. Preserve the 30-minute workflow allowance. Record commands/exits/full logs and tested PHP snapshots. Disclose local timeout/failure accurately; selected tests and inherited O3 CI do not replace fresh full hosted O4 CI. Fix concrete in-scope failures without weaker assertions or gates.

Commit the completed bounded implementation and update documentation/proof matrix after implementation. Return provider-onboarding-o4-b0-completion-all-deliverables.zip plus individual report/instructions. Include exact original-preparation-to-final and continuation-entry-to-final patches, incremental Git bundle with declared prerequisites, entry/tested/final commits/trees, all tested PHP bytes/hashes, canonical changed files, protected-original comparisons, public logs, manifest, inner review ZIP with SHA-256, and README. No outer checksum, recursive historical archives, dependencies, private evidence or runtime state. Preserve earlier reports as historical evidence instead of rewriting their incomplete verdict.

Stop for source review and fresh complete hosted CI before push/merge. Do not integrate the partial or final implementation locally or start O5. If an actual external dependency prevents completion, report its exact source/evidence and the completed code; an unimplemented in-scope seam alone is not an external blocker.
