# O4 CI completion: continue from the reviewed V2 correction

Continue the existing implementation task from local `c2dc4cb647877e4057214355f6c4d33d9f365c5f`, tree `c7f9f5b66698f7450ddcb1661fd8a0595da7b598`. The exact GitHub equivalent is `dc9c815ae3e903c016809a604ff538c86aafbc22` on `codex/provider-onboarding-o4-b0-ci-v2-checkpoint`. Preserve existing untracked work. Do not pull the older #798 source over this newer correction or cherry-pick both equivalent histories.

The source review found no blocking correctness defect, and independent selected validation passed 45 tests / 6,415 assertions. The full local gate still timed out at 1,800.219 seconds. Complete the performance correction and unchanged full gate; do not stop at another descriptive profile if implementation can continue.

1. Reuse the existing corrected largest-workflow profile. Its `other` bucket records 9,543,137 CanonicalJson::encode calls / 65.326 exclusive seconds, 12,204,227 StrictJson::canonical calls / 38.151 exclusive seconds, and journal decode / 45.553 exclusive seconds. Attribute the dominant pure encoding work by immediate caller and bounded shape: scalar, reference, unsigned record, complete record, ledger/state or frame. Count scope-active/inactive, no indexed candidate, count mismatch, strict-value mismatch, cache hit and budget bypass. Avoid dumping values, secrets, or full argument stacks; use bounded counters or sampling where instrumentation overhead is material.
2. Identify a concrete avoidable computation from those measurements. Retain both existing corrections unless new evidence identifies a defect. Develop and measure the correction at the relevant owning boundary. Do not broaden caching or remove validation merely because call counts are high. The rejected further direct-string reference encoder did not establish an end-to-end improvement and should not be repeated unchanged.
3. Preserve exact bytes, errors, JSON depth semantics, signed zero, reference/mutation behavior, private bounded storage and exception/nesting cleanup. Preserve fresh trust, time, revocation, signature, generation and chain checks and their order. Do not retain validity/permission/currentness/signature results, rely on identity/digest equality for validity, introduce caller trust flags, a second journal, nested locks, or provider I/O under a lock.
4. Keep every existing test and fixture, original reviewer probe, R1 cases, pure-computation differential cases, source inventories, approved/signed bytes, service configuration and dependency locks. Add the included JournalReviewerFreshReadTest.php to the next candidate's tests/Imperium/Runtime directory so the independently passed history-corruption check remains covered. All operational flags remain false and the actual retry allowlist remains empty. No live providers, real credentials, installed-user state, activation, merge or O5.
5. Compare the new candidate against this V2 source under the same runtime, quiet serial conditions, inputs and instrumentation. Use repeated/alternating real-workflow runs and retain rejection evidence. Prefer the existing largest consumer and currentness-after-consumption cases; extend measurement only to resolve a concrete remaining bottleneck. Distinguish diagnostic overhead from quiet execution and avoid treating one-workflow savings as a suite-wide percentage.
6. After meaningful differential/adversarial checks and measured improvement, freeze exact source and run the unchanged `C:/php/php-8.4/php.exe vendor/bin/phpunit tests` within 1,800 seconds. Preserve complete output, runtime/environment, exit status, elapsed time and before/after tracked-PHP hashes. Do not split, filter, skip, weaken or extend the acceptance gate. Do not repeat the unchanged V2 full run just to rediscover its timeout. If needed, gather test-specific timing separately as diagnosis, clearly distinguished from the acceptance command.
7. Continue addressing measured remaining causes until the complete local gate passes. Then deliver `provider-onboarding-o4-b0-ci-completion-v3-all-deliverables.zip` with exact source/commit/tree, reconstructible patches, source comparisons, meaningful tests, controlled measurements, complete successful gate evidence, review/proof matrix, inner review ZIP/hash and payload manifest. Separate post-test documentation from the frozen tested source. Fresh independent full hosted CI is still required before O4 acceptance.

If a concrete blocker prevents completion, return a clearly named V3 INCOMPLETE checkpoint with exact implemented/rejected source, measurements, failure evidence and the next technical action. A timeout is unresolved work, not proof that completion is impossible. The two missing historical pre-fix diagnostic files do not require rerunning old prototypes; accurately retain their documented limitation.

Optional recovery into a new isolated checkout, only if the tested local source is unavailable:

```powershell
git fetch origin refs/heads/codex/provider-onboarding-o4-b0-ci-v2-checkpoint
git worktree add ..\imperium-o4-ci-v3 -b local/o4-ci-v3 FETCH_HEAD
```

Do not merge diagnostic PR #802 or copy its selected workflow. It is closed unmerged. Integration PR #798 remains on HOLD and retains its earlier head until a suitable acceptance candidate exists.
